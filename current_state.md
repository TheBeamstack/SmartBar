# current_state.md — RebarConfig (SmartBar) cross-agent / cross-session handoff

> **Purpose.** This file is the **single source of truth passed between agents and sessions**.
> Each phase of `v1.0_imp_plan.md` is implemented by a **separate agent that starts cold**. To
> stop the next agent from re-deriving context, guessing, or building on false assumptions, the
> agent who finishes a chunk of work **must append a handoff entry** (newest first, in §9) that
> records *in detail* what they did, what they decided and why, what is true *now*, and exactly
> what the next agent should do next. Keep §1 (progress) and §9 (log) truthful and current.
>
> **This file travels with the code (committed to GitHub).** It is the cross-machine handoff —
> see `cross_projects_policy.md §9` (box-local, one level up at `/home/devuser/projects/`).

---

## 0. Who works on this & how we sync

- **Amer** — dev agent on the owner's **local Windows PC**.
- **Zayd** — dev agent on the **Hetzner dev box** (Linux; this machine).
- **Owner (Ssi Daoudi)** — the human. Directs the work; **not steeped in low-level technical jargon**,
  so when you write here or talk to him: **explain technical terms in plain language** the first
  time (e.g. *"a monorepo = one git repo holding several sub-packages that depend on each other"*),
  lead decisions with *what it means / why it matters*, and **ask before any real design decision**
  (don't silently pick architecture or scope; pure-convention defaults are fine — just flag them).

**Sync model:** we take turns. **Pull before you start, push when the owner asks.** Trust
`git log` / the actual diff over memory. If this file conflicts on merge, keep **both** sessions'
log entries and flag it to owner.

**Plain-language: what this project is.** RebarConfig is a **web app for detailing the steel
reinforcement ("rebar") inside concrete elements** — columns, beams, slabs, stairs, piles. The
structural engineer already knows *how much* steel each zone needs (from other software); this
tool lets them lay out the *actual bars*, see them in 3D, get instant "is this buildable & code-
compliant?" feedback (French **BAEL**, European **Eurocode 2**, Moroccan seismic **RPS**), and
export the on-site paperwork (bar-bending schedule, DXF drawings, PDF sheet). **v1.0 runs 100%
in the browser — no server, free hosting.**

---

## 1. Progress at a glance

| Phase (plan = spec milestone) | Status | By |
|---|---|---|
| **P0–P4b / M0–M4b** — pure engine + 8 elements + segment-grammar + BAEL/EC2 packs + RPS seismic overlay (headless) | ✅ **DONE** (code) · ⏳ G-BAEL/EC2/RPS/TOL unsigned | Zayd, 2026-06-24 |
| **P5 / M5** — Section/Coupe engine + BBS + DXF + PDF + export-lock + `.rcfg` I/O + autosave + SPA wiring | ✅ **DONE** (code) · ⏳ G-COUPE unsigned | Amer/Zayd, 2026-06-25 |
| **P6 / M6** — hardening (coverage 95% · i18n · perf · code-split) + control fixes 8a/8b/8c + all 8 elements + seismic picker in the SPA | ✅ **CODE DONE** · ⏳ a11y axe · docs · sign-off pending | Zayd, 2026-06-25 |
| **P7** — multi-element project model (instances + takeoff + `.rcfg` v1.1 + migration) | ✅ **CORE DONE** (code) | Zayd, 2026-06-25 |
| **v1.0.1** — Feature C (namespaced BBS · combined PDF + per-project lock · DXF batch · elevation *fiche* · reorder) + A (ViewCube + persp/ortho + a11y views) + B (3D coupe handle) | ✅ **CODE DONE + tested + PUSHED** (`d27a950`) · ⏳ A/B GPU widgets need the owner's browser to verify (D-V101-1/2) | Amer, 2026-06-25 |
| **v1.0.2 P1 (F7)** — 2D section picker + face-relative bar labels + 3D bar pickability; Supplements rebound onto the picker | ✅ **CODE DONE + tested** (D-V102-1) · ⏳ owner GPU pass (3D click + picker feel) | Zayd, 2026-06-27 |
| **v1.0.2 P2 (F2)** — column/beam cross-ties rebuilt on real bar engagement (anchored épingles, Auto/presets, element-level hook angle, real `longBarsEngaged`, legacy migration) | ✅ **CODE DONE + tested** (D-V102-2) · ⏳ owner GPU pass · G-RPS | Zayd, 2026-06-27 |
| **v1.0.2 P3 (F3)** — sticky per-zone verification readout (pinned As,prov/As,req/d per zone + overall chip; pure `perZoneReadout` selector; all 8 elements) | ✅ **CODE DONE + tested** (D-V102-3) | Zayd, 2026-06-27 |
| **v1.0.2 P4 (F4)** — workspace re-layout: Verif+Project+BBS as one collapsible/expandable right column; Coupes stays in the bottom dock | ✅ **CODE DONE + tested** (D-V102-4) | Zayd, 2026-06-27 |
| **v1.0.2 P5 (F5)** — user transverse regions (per-region cadre spacing via the transverse-segment seam; 3D/coupe/BBS/fiche reflect them; seismic WARN-tightens end zones; legacy→1 region) | ✅ **CODE DONE + tested** (D-V102-5) · ⏳ owner GPU pass (region table feel) · G-RPS | Zayd, 2026-06-27 |
| **v1.0.2 P6 (F6)** — façonnage editor + shape catalog (Z/double-crank/stepped manifests + per-group shape/params/end-hooks → 3D/coupe/BBS/DXF; cutLength invariant guarded; legacy default) | ✅ **CODE DONE + tested** (D-V102-6) · ⏳ owner GPU pass (sketch feel) | Zayd, 2026-06-28 |
| **v1.0.2 P7 (F1)** — in-plane view roll (pure `rollUpVector`; session `rollRad` not in `.rcfg`; ↺/↻ buttons + roll slider; Home/named-view re-level) | ✅ **CODE DONE + tested** (D-V102-7) · ⏳ owner GPU pass (roll feel) | Zayd, 2026-06-28 |
| **v1.0.3 P1 (G1)** — true bar geometry: `placeBars` renders each longitudinal bar's real bent `centerline3D` (façonnage/cranks/relevés), the spiral as ONE member-length coil, the stair main bar along waist+landing; straight `DROITE` byte-identical | ✅ **CODE DONE + tested** (D-V103-1) · ⏳ owner GPU pass | Amer, 2026-06-30 |
| **v1.0.3 P2 (G6)** — stirrup cadence continuity across region boundaries (no short stub) + coupe section dims & cover on both faces (PDF now draws `view.dimensions`) + elevation region-length dims | ✅ **CODE DONE + tested** (D-V103-2) | Amer, 2026-06-30 |
| **v1.0.3 P3 (G5)** — cross-tie/épingle unification: ONE cross-tie model (épingle dropped from scheme catalogs), the épingle hook now rendered in the centreline (open shapes), legacy supplement-épingles migrate to `crossTies`, no centred épingle anywhere | ✅ **CODE DONE + tested** (D-V103-3) · ⏳ owner GPU pass | Zayd, 2026-06-30 |
| **v1.0.3 P4 (G2)** — addressable bars: per-bar overrides (shape/hooks/Ø/unique length+axial pos/remove), independent extra bars + section levels, façonnage editor targets a specific bar; flows to 3D/coupe/PDF/DXF + BBS via a pipeline `longBars[]` channel (gated on presence → legacy byte-identical) | ✅ **CODE DONE + tested** (D-V103-4) · ⏳ owner GPU pass · per-bar As is group-based (note) | Zayd, 2026-06-30 |
| **v1.0.3 P5 (G3)** — beam **two supports V1/V2** (asymmetric chapeaux + per-support anchorage + width, each a validated zone), **first-class relevés** (bent-up bottom bars via the G2 addressable channel — rendered + scheduled), **auto-seeded editable stirrup regions** from the supports; legacy single-`chapeau` beam migrates to symmetric V1=V2 | ✅ **CODE DONE + tested** (D-V103-5) · ⏳ owner GPU pass · G-BAEL · representative render note | Zayd, 2026-07-01 |
| **v1.0.3 P6 (G4)** — **lap splices / couplers**: pure `spliceBar` (segments incl. `code.l0` overlap, cutLength invariant preserved) + `autoSplice` (stock-length), doc `splices?`/`autoSplice?` on column-long/beam-span, BBS schedules **segments + coupler tally**, `lap_stagger` WARN + seismic `lap_in_critical_zone`; legacy unspliced byte-identical | ✅ **CODE DONE + tested** (D-V103-6) · ⏳ owner GPU pass · G-BAEL/EC2 (provisional `l0`) | Zayd, 2026-07-01 |
| **v1.0.3 P7 (G7)** — **shop-drawing PDF/DXF**: a pure `shopDrawing()` annotation model (leader lines `mark·nØd·l=`, per-element sequential marks, coupe markers A-A, stirrup-zone `count×spacing` notation, support labels V1/V2) + a **bending table** (one row per distinct shape · sketch · Ø · cut · count/element · ×quantity total), shared by PDF + DXF; existing render goldens held (structural); **support width/anchorage now wired into the app PDF/DXF exports** (D-V103-7b, Amer) | ✅ **CODE DONE + tested** (D-V103-7 · 7b) · ⏳ owner GPU pass | Zayd · Amer, 2026-07-01 |
| **v1.0.3 P8 (G8)** — **3D fidelity**: transparent concrete now carries a crisp `<Edges>` outline (box + cylinder) so the volume reads as a solid block; **E-STR-01 renders a stepped stair** concrete (per-step boxes from `g`/`r`/`n_steps`/`flight_width`) instead of the flat rect box — viewport-only, no engine/core change | ✅ **CODE DONE + tested** (D-V103-8) · ⏳ owner GPU pass | Amer, 2026-07-01 |
| **v1.0.3 P9 (G9)** — **camera fixes**: killed the ViewCube auto-spin (roll is now an on-demand effect, not a per-frame `useFrame` loop) + **roll auto-levels on orbit-start**; added a **toolbar hand-pan toggle** (`navMode`, LEFT-drag pans when active, orbit on RIGHT-drag, zoom always) — session-only, not in `.rcfg` | ✅ **CODE DONE + tested** (D-V103-9) · ⏳ owner GPU pass | Amer, 2026-07-01 |
| **v1.0.4 spec — COMPLETE single-tenant product** — `v1.0.4_spec.md` restructured: façonnage (H1–H19) + Tracks A–E (A compliance truthfulness · B detailing depth · C site-ready output · D 3D fidelity · E data/interop). Vertical/multi-tenant (Track H) deferred. Split of duties: `v1.0.4_owner_tasks.md` (owner manual work) · `roadmap_directions.md` trimmed to F/G/H | 🟡 **SPEC DRAFTED** (docs only, not pushed) | Zayd, 2026-07-05 |
| **v1.0.4 prep phase executed** — baseline+severity verified (H1 crash + H2 no-op confirmed live), adapter safety net (characterization + `coverage:adapter` 96%), manifest-data table, design spikes, fixture inventory, `v1.0.4_impl_plan.md` (façonnage phasing) | ✅ **PREP DONE** · ⏳ owner design decisions (`owner_tasks §A`) · ⏳ engineer sign-off program (`owner_tasks §B`) | Zayd, 2026-07-03/05 |
| **v1.0.4 Phase 1 (H1, H3, H4)** — façonnage crash + data-loss fixes: per-bar override inherits the group façonnage (no more BAIONNETTE crash) + axial/removed-only edits skip regen; store `withDoc` keep-last-good guard + non-blocking solve-error banner (owner A-6); core-level `cutLength>0` guard in `generateBarShape` (BBS/DXF/splice protected); FaconnageEditor buffer resync on `[shapeId, memberLength, committedKey]`. Owner decisions A-2/A-4/A-5/A-6/A-7 recorded in `owner_tasks §A` | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-05 |
| **v1.0.4 Phase 2 (H11, H9, H2)** — façonnage capability: manifest `default`s for every open-shape param + shared `defaultParams` (drops the positional seed guess); guarded `pickShape` (validates the seed before committing); `totalLengthParam` (A-2 legs) on the 9 open shapes + adapter `applyUniqueLength` inverts `totalLengthExpr` so a unique `length` drives the principal leg → `cutLength == length` on EVERY open shape (was DROITE-only). New `ShapeArchetype.totalLengthParam` + integrity guard. | ✅ **CODE DONE + tested** · NOT pushed | Amer, 2026-07-06 |
| **v1.0.4 Phase 3 partial (H15, H6)** — backend↔frontend parity: independent extra bars now expose the full model (shape + reused FaconnageEditor + length + axial pos, not just u/v/Ø); collision-free `x{n}` extra-bar ids. Audit confirmed splices/supports/relevés/ties/regions/seismic all already wired — remaining gaps are H7 (extras on picker/coupe) + A3/H13 (EC2 picker). | ✅ **CODE DONE + tested** · NOT pushed | Amer, 2026-07-06 |
| **v1.0.4 Phase 3 done (H7) + A3/H13** — extras selectable on the SectionPicker (distinct marker + keyboard list) via a new `selectedExtraId` channel, editor highlights on pick; coupe already showed them (confirmed). **EC2 now reachable:** `doc.codePack` (BAEL/EC2, additive + `.rcfg` round-trip), `packFor()` threads the active pack through solveDoc/applyUniqueLength/FaconnageEditor, BAEL↔EC2 picker in the Projet/Code tab. BAEL↔EC2 = same structure, different numbers (tie-Ø verdict flips). Closes the last backend-ahead-of-frontend gaps. | ✅ **CODE DONE + tested** (green gate below) · NOT pushed | Amer, 2026-07-06 |
| **v1.0.4 Phase 4 (H8)** — geometric validity for the **addressable channel** (per-bar overrides + independent extra bars), core-only: pure `validateAddressableBars` (`validation/predicates.ts`) with three tiered predicates over the resolved `longBars[]` — `addressable_axial_extent` 🔴 (bar leaves `[0, memberLength]`), `addressable_section_bounds` 🔴 (extra `(u,v)` breaks the `cover+Ø/2` envelope → out of concrete), `addressable_clear_spacing` 🔴 below `max(Ø, dg+5, 20)` / 🟠 if merely tight (owner **A-5 tiered**, dg=20/k1=1/k2=5). Wired in `pipeline/element.ts` after `buildLongBars` (a `runExtent` reads the local-frame run); self-gates to real addressable content so a legacy doc + a benign Ø-only override are byte-identical. **Note:** the (outdated) `impl_plan` Phase 4 said spacing WARN-only; as-built follows the owner's A-5 **tiered** ruling. | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-06 |
| **v1.0.4 A2 review + verdict-fixes + B2 + B3(first slice)** — Amer's adversarial review of A2/H8 found two reachable wrong-🟢 in the addressable channel + fixed them: **Finding 1** (removals below the column minimum now FAIL `min_bars`/`face_min_bars` — real placed count/faces threaded, corner-aware; was: nominal layout → a 3-bar column exported green) + **Finding 2** (`addressable_section_bounds` now judges Ø-overrides, so an oversized override that eats the cover → 🔴; was: extras-only). **B2** (beam two supports): per-support **axial placement** (right chapeau at `L−extension` via `ElementLongInput.axisStart`) + per-support **anchorage** (`support_anchorage:left/right` vs hooked `l_bd`; `hooked?` on `AnchorageArgs`, BAEL 0.4 / EC2 0.7). **B3** (supplement anchoring, first slice): `resolveSupplement` now anchors SIDE_FACES/CORNER_DIAGONAL/INTERIOR_DIAMOND (+`angleDeg`) → `placeBars` renders them in-position (skin/diagonal longitudinal, diamond rotated 45°) instead of centred at origin; SKIN multi-bar fanout + supplement→A2 accounting deferred to the next slice. All legacy byte-identical; extra→zone convention + supplement 3D fidelity flagged for A1/owner-GPU. | ✅ **CODE DONE + tested** · NOT pushed | Amer, 2026-07-07 |
| **v1.0.4 A2 steel accounting (+H5)** — every steel add/remove now feeds §7. `pipeline/element.ts` reconciles each longitudinal zone's **As,prov = Σ(π/4)·Øᵢ²** and area-weighted-centroid **d** over the REAL placed set (per-bar Ø overrides, removed bars, standalone extras) via new `computeZoneGeometryWeighted` (`layout/rect.ts`); exact for mixed Ø + mixed levels. **Owner decision 2026-07-06:** an independent extra is real steel — it contributes to As,prov of the zone whose tension region it sits in (region rule) + enters `d` (re-baselined `independent_bars` + `addressable_bars` intentionally). Column threads exact As via optional `ColumnZoneInputs.asProvExact` (grouped → byte-identical). **A2 finalized:** per-region **Asw** checked on the governing (widest) tie/stirrup region (`governingAswSpacing`, D-V102-5) + clear-spacing **fold** (H8's predicate now judges every FOCUS bar — standalone extra OR Ø-override — vs the real placed set, so an override's enlarged Ø crowding the grid FAILs where the face check passed). `per_region_asw` + `clear_spacing_addons` green. | ✅ **A2 COMPLETE + tested** · NOT pushed | Zayd, 2026-07-06 |
| **v1.0.4 B3 (2nd slice) + C2 + C1 (station-aware slice)** — **B3:** SKIN multi-bar **fanout** (`resolveSupplement` → `anchors[]` = `count_per_side` × both lateral faces; adapter emits one input/anchor → N rendered+scheduled skin bars) + supplements now feed **A2 accounting** (a SKIN bar's steel joins the zone's As,prov; an interior DIAMANT tie adds Asw/m to the transverse zones at the derived `cos(45°)` orientation factor — ⚠ leg-crossing convention flagged G-BAEL/EC2). **C2:** spliced-segment BBS rows now draw the segment's OWN centreline (`segmentArcBounds` + `sliceCenterline2D`, not the whole-bar fallback) + robust leader anti-overlap (rail stretched to a legible min-pitch, no stacking at density); flows through pdf/dxf unchanged. **C1:** `defaultCoupeFor` **station-aware** — seeds the default cut at the richest station (most longitudinal bars), gated to the addressable channel so grouped docs keep mid-length byte-identical; new pure `suggestCoupeStations`. All legacy byte-identical (no golden moved). | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-07 |
| **v1.0.4 façonnage Phase-6 (H16/H17/H18/H19/H12) + H14 + B1** — **Phase-6 UI batch:** H16 FaconnageEditor honours manifest `max`/`step` (new `ShapeParam.step`); H17 rich invalid feedback (real reason + retained last-valid sketch + `aria-invalid`); H18 AddressableBars on the shared i18n bundle (`t(lang).addressable`, not inline `tr()`); H19 per-leg `min>0` (core generator rejects a non-positive leg + UI floors at 1 mm); H12 DROITE length coupled to member (read-only) with a custom-length toggle (owner **A-7**). **H14+B1 (per-bar splices):** overrides/extras carry `splices?`/`autoSplice?`; `buildLongBars` computes each bar's `SpliceResult` (explicit + H14 auto-split at PROVISIONAL 12000 stock); BBS schedules segments + shop-drawing slices each; new pure `evaluateLapStagger` (sourced EC2 §8.7.2 ≤½/section within 0.3·l0) → per-zone `lap_stagger` PASS/WARN; per-bar seismic laps. UI auto-split toggle. Two pre-existing test assertions updated (H3↔H19 message; the new H12 checkbox); no golden moved. | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-07 |
| **v1.0.4 B3 (3rd slice) + E1 + E2 (canonical `.rcfg` + migration)** — **B3:** the A2 supplement-accounting fold now credits an OPEN along-member add-on — SKIN **or** CORNER-DIAGONAL — to the zone's As (was skin-only); closed diamond → Asw unchanged. **E1:** new `reinforcementFromResult(result)` (exporters) maps the solved `groups[]` → canonical §10 `ReinforcingElement[]` (element-agnostic: role→distribution/placement); `SolvedGroup` carries `params`/`supplementId`; `docToRcfg` now **populates** `baseGroups`/`supplementalGroups` (was empty) by solving the doc (try/catch → empty fallback); `.rcfg` bumped to **v1.2** (additive, idempotent version migration). **E2:** `migration_chain` corpus (every fixture migrates + idempotent + future-version pass-through + unknown-kind survival). **Validated all 8 elements** doc→rcfg→recover round-trip (backend↔frontend lossless). **C1 slab-exactness DEFERRED** (needs a geometry re-model + G-COUPE acceptance — flagged). No golden moved. | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-07 |
| **v1.0.4 C1 slab-coupe exactness + D1/D2 (3D fidelity) + H10 + cover WARN tier** — **C1 (slab re-model):** a slab/stair-waist DISTRIBUTION linear bar now runs ACROSS the width at SPAN stations (`solveSlabDistributionBars` + `BarPosition.across`/`axial`; `placeBars` cross-width line; `sectionAt` §9.5.3 nearest-behind generalised to the distribution layer → transverse coupe = main dots + true distribution line). Welded MESH keeps the representative panel (`isDistributionLinear` gate); per-metre As unchanged; **no golden moved** (BBS distribution count kept — reconciliation flagged for sign-off). **D1:** pure `steppedStairSpec` + the top `landing_L` slab modelled (was ignored). **D2:** welded MESH renders as its true wire grid (`meshGridBars`) not a mis-oriented outline. **H10:** hook edit validated/committed against last-valid params (never dropped mid-invalid-param). **Cover:** §7.12 comfort-target WARN tier (`coverItem`, pack band, uniform across validators). +32 tests. **→ SUPERSEDED in part by Zayd's 2026-07-08 review (row below): cover WARN reverted to FAIL/PASS (F1); the distribution BBS count reconciled to the drawing (F2); the joist mesh coupe stray-lines gated out (F3).** | ✅ **CODE DONE + tested** · NOT pushed | Amer, 2026-07-08 |
| **v1.0.4 adversarial review of Amer's C1/D1/D2/H10/cover work + 3 fixes** — **F1** cover comfort-band WARN reverted → cover at/above the code minimum is 🟢 PASS (the default column & beam had silently flipped to amber "À vérifier"); shared `coverItem` helper kept. **F2** the slab/stair DISTRIBUTION fabrication **count** now = the span-distributed set actually placed (`solveSlabDistributionBars(...).length`) so BBS + bending table + 3D/coupe agree (default one-way slab 13→20, stair 5→16; the previous width-derived count under-counted the takeoff ~35–69%). **F3** `sectionAt` guarantee gated on `isDistributionLinear` so a welded topping MESH no longer draws stray representative coupe lines (joist 5→0). D1/D2/H10 verified correct as-built. | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-08 |
| **v1.0.5 Phase 1 (M1) — placement defects D1/D2/D3** — **P1a** single placement path: `solveElement` forces the per-bar `longBars[]` for any element whose faces carry >1 longitudinal zone (`facesCarryMultipleZones`), so a default two-chapeau beam draws **both** chapeaux at their stations (was: only the left — D1/D2); single-zone-per-face (column/slab) stays grouped, byte-identical; BBS byte-identical (bars re-merge). **P1b** placement↔validation **decouple** (audit A2): new `hasUserAddressableContent` gates the As/`d` reconciliation, `face_min_bars`, per-bar splices, `validateAddressableBars` + the station-aware default coupe — NOT the bare `longBars` presence — so the default beam's validation list + default coupe are byte-identical. **P2** per-bar **curtailment** replaces the inert `continuedToSupport` fraction: `start/endStation` + per-end `anchorage` on `LongBarOverride`/`ExtraLongBar`/`PlacedLongBar`; a curtailed bar is shorter in 3D + BBS (`clipToStations`); §7.7 `end_support_anchorage` now counts the bars that really run through (`runsThroughAsProv`). Legacy `continuedToSupport=f` migrates to per-bar curtailment (`migrateBeamContinuation`, innermost `round(f·n)` run through; PROVISIONAL inset 0.1·L, G-BAEL). Dead Sidebar control removed. Proven end-to-end (core→solve→adapter→3D/coupe→BBS/DXF/PDF→`.rcfg`). | ✅ **CODE DONE + tested** · NOT pushed | Amer, 2026-07-10 |
| **v1.0.5 Phase 2 (M2) — canonical `PlacedBar` model + shared placement pass on all 8 elements** — **P-A** new `types/placed-bar.ts`: the canonical **`SingleBar`** family + `PlacedBarInput` union (M3 adds `BarRow`/`Bundle`/`Layer`, additive/forward-compat), stable id (D-P3-4); `EndAnchorageChoice`/`BarEndAnchorage` moved here (types layer). **P-B** new `section/resolvePlacedBars.ts`: the shared per-bar pass (`clipToStations`+`barSplice` primitives + `resolveSingleBar`) — the **single long-steel truth** for free bars. `element.ts`'s extra-bar loop delegates to it (byte-identical); the four generic shims (`circular/slab/stair/joist`) gain `placed?` → resolve → append `longBars`+`hasUserAddressableContent`; RECT base reconstruction stays in `buildLongBars`. `placeBars`+`computeBBS` made **additive** (a RECT element replaces base steel via `longBars` = every base bar covered → byte-identical; a generic pipeline KEEPS its base mat + appends only FREE standalone bars). B3 two modes: free `(u,v)` section-agnostic vs override section-aware (position from `layoutBars`). Adapter: `GenericDoc.placed?` → `buildPlacedBars`→`SingleBar` threaded into all 4 generic inputs; `.rcfg` round-trips. **No golden moved** (M2 adds no placed bars to any legacy doc). Proven: a free bar renders+schedules+coupe on each of the 8. | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-10 |
| **v1.0.5 Phase 3 (M3) — rows / layers / bundles / skin + coupe fidelity** — **P-C** `BarRow` (a `count` OR a `spacing` over an extent, one shape/Ø/curtailment) → **N `SingleBar`s**, one merged BBS mark (count N); count↔spacing equivalent. **P-D** `Layer` (an explicit 2nd/3rd flexural layer, offset `Ø+layerGap` inboard of a face) → its bars feed the **area-weighted `d`** so a 2nd bottom layer **lowers `d` exactly** (verified). **P-E** `Bundle` (2–4 touching) → real schedule count/one mark, **As = n·area**, `code.bundleEquivDiameter(φ,n)=φ√n≤55` (new BAEL+EC2 pack extra + core `CodePackExtras`). **P-F** skin = a `BarRow{skin}` on LEFT/RIGHT faces — first-class (rendered+scheduled+curtailable). **P-G** each new type reads at its plane in the **coupe** (bundle=n touching dots, layer=distinct row at its depth, skin=side-face dots; a missed/curtailed bar correctly absent). One expander in `resolvePlacedBars` fans out the union (`PlacedBarInput += BarRow/Bundle/Layer`, additive/forward-compat); RECT `ElementSolveInput.placed` feeds the A2 As/`d` reconciliation; generic shims pass `sectionDims` for layers; adapter `PlacedBarDoc` union (row/bundle/layer docs) round-trips `.rcfg`. **No golden moved.** | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-10 |
| **v1.0.5 Phase 4 (M4) — validation completeness + honest tiers (Track V)** — the NEW placed steel (M2/M3) is now JUDGED with the 🔴-block/🟠-warn split. New pure `validation/placedBarRules.ts` (`validatePlacedBarRules`) wired into the RECT pipeline + all 4 generic shims: **V-B** `bundle_max` (>4, >3-at-lap → 🔴) + `bundle_cover` (equivalent Ø `φₙ` breaches cover → 🔴, approaching → 🟠); **V-C** `skin_minimum` (depth-triggered per pack; under/absent-where-mandatory → 🟠, never blocks); **V-D** `layer_clear_spacing` (between-layer clear < floor → 🔴 clash / 🟠 tight); **V-E** `curtailment_anchorage` (a bare interior cut → 🔴, run too short to develop → 🔴/🟠, §7.7 via `code.lbd`); **V-A** the element-agnostic geometry (`placed_axial_extent` + `placed_section_bounds`, radial for CIRCULAR) generalised to the generic pipelines (RECT already runs `validateAddressableBars`). **F1 guarantee baked into every band** (`tierByMargin` PASS at exactly the code value; amber sits strictly below). **Affected-bar id (W4):** each FAIL emits the offending resolved placed-bar id(s) — a bundle/layer reddens all its members, a single/curtailed bar exactly one — flowing through `placeBars`→`failingGroupIds` unchanged. **Correctness fix:** a legitimate bundle's touching members are now exempt from the clear-spacing check (`bundleId` tag on `PlacedLongBar`→`AddressableBarView`) — they no longer trip a spurious 🔴. New pack extras (BAEL+EC2, JSON-sourced, ⚠ PROVISIONAL G-BAEL/EC2/TOL): `bundleMax`/`bundleMaxAtLap`/`skinReinforcement`/`curtailmentHookedFactor`. **No golden moved** (no placed content → no Track-V item; legacy byte-identical). | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-10 |
| **v1.0.5 Phase 5 (M5) — splice / coupler reach (Track S)** — splicing extended off the RECT-only path. **(1) Circular/pile pipeline** now threads `splices?`/`autoSplice?` on `CircularLongInput`: a long cage's native pitch-circle zone auto-splits at the PROVISIONAL 12 m stock (`spliceBar`/`autoSplices`), attaches `SolvedGroup.splice` (scheduled as **segments + coupler tally** by the existing BBS path), and raises the group-level `lap_stagger:<zone>` WARN. **(2) Any freely placed bar** already carried its own `splices`/`autoSplice` through `resolveSingleBar`→`barSplice` (M2/M3); M5 makes the **per-bar stagger** apply everywhere by extracting `analyzePlacedBarLaps` (shared, `resolvePlacedBars.ts`) out of `element.ts` and calling it from all four generic shims (circular/slab/stair/joist). **(3) slab/mesh distribution stays unspliced** — no splice threading on the per-metre mat inputs (only a user's FREE placed bar can lap on a slab). Adapter: `ZoneEdit` gains `splices?`/`autoSplice?`, threaded into `circularInput`'s longitudinal zones; `.rcfg` round-trips (rides `meta.app_document`). RECT column/beam splices + their seismic `lap_in_critical_zone` are unchanged (already shipped). No golden moved. | ✅ **CODE DONE + tested + PUSHED** (`36ba036`) | Amer, 2026-07-10 |
| **v1.0.5 Phase 6 (M6) — BAEL / French façonnage schedule (Track O)** `[OWNER-DEP §O-1]` — a **pure labelling layer** over the exporters (no engine/geometry/`cutLength` change; `shapeArchetypeId` stays the data key). New `exporters/faconnageCodes.ts` — `faconnageFor(id)` over a **PROVISIONAL** archetype-id→French-designation table (16 shapes; unknown id → raw-id fallback, forward-compat; owner-supplied nomenclature §O-1, `provisional:true`). `BbsLine.faconnage`/`BendingRow.faconnage` (additive) → **PDF** BBS table re-laid to the French columns (repère/nombre/Ø/longueur de coupe/façonnage/masse) rendering the label + **DXF** façonnage table gains an ASCII `Fac.` code column + **on-screen `BbsPanel`** re-laid, i18n `bbs.faconnage`. Bundles/rows/curtailed bars schedule count+cut+designation; PDF/DXF == on-screen by construction. No golden moved. | ✅ **CODE DONE + tested + PUSHED** (`5b80092`) | Zayd, 2026-07-10 |
| **v1.0.6 Phase N6 (Track U5) — the EDITABLE elevation (curtailment · laps · zones · bend-up)** — the last + largest v1.0.6 phase; the `ElevationDock` (read-only since N4) becomes a **canvas over the v1.0.5 per-bar station model**. **(1) Pure `engine/elevation.ts`:** the fit transform extracted from N4's inline `ElevationView` (`fitElevation` + its `toDrawing` inverse), `stationFromDrawing` (inverts `orientPoint`), `snapStation` (clamp `[0,L]` + grid), `screenToStation` (the ONE pointer→snapped-station seam), and `selectedBarStations`/`memberRunLength` doc readers — all headless-unit-tested (spec §0.4). **(2) Store station actions** (each committed by BOTH the on-canvas drag AND its numeric twin, §0.3.3): `curtailSelectedBar(end,station?)` (start/end curtailment; `undefined` clears → legacy byte-identical via `stripUndef`), `setSelectedBarAnchorage` (runs-through/anchorage at a support), `add/removeSelectedBarSplice` (drop a lap/coupler), `setReleveBend` (bend-up station), `moveStirrupRegionBoundary` (drag a zone edge — reuses `normalizeRegions`, == the `RegionEditor` table). A private `patchSelectedBar` routes to the group override (by index) or the extra (by id) via the existing setters. **(3) Editable `ElevationDock`:** renders the read-only fiche + overlay handles — draggable curtailment end-caps (+splice markers) for the selected bar, draggable stirrup/tie zone boundaries — wired through `screenToStation` (pointer via `getScreenCTM`, owner-GPU-verified; null under jsdom → no-ops in tests) + a live station readout. **(4) Numeric twins (headless-tested a11y path):** `Inspector` gains a curtailment/anchorage/splice fieldset for the selected bar; the `Sidebar` relevé editor gains a bend-station field. **(5) Adapter:** `ReleveZone.bendStation?` (new doc field) read by `releveExtraBars` to parameterise the existing `RELEVE` shape's bottom leg — `apps/web`-adapter only, **no core geometry change**. New i18n `elevation` group (FR+EN) + CSS. `apps/web`+new pure helper only; **no core/exporter/manifest/golden touched.** **Deferred (flagged, per §7): row/bundle/layer curtailment awaits the N5 selection reaching the unified `selection`; the relevé/splice on-canvas drag *handles* are minimal (numeric twin is the tested path). Needs the largest owner GPU pass (D-9).** | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-11 |
| **v1.0.6 Phase N5 (Track U4) — the tool palette + place-by-pointing** — "add any bar" becomes a visible tool, not a buried numeric sub-panel (UI review U8). **(1) Modal toolbar (`ui/ToolPalette.tsx`, in the section dock):** Select · Add bar · Add row · Add layer · Add bundle · Link · Measure — each `aria-pressed`, a keyboard shortcut (v/b/r/l/u/k/m, guarded against typing in fields), **`Esc`→Select**. **(2) Palette:** for an `add-*` tool, a façonnage shape (`LONGITUDINAL_SHAPES`) + a Ø + a **typable `(u,v)` coordinate twin** + a Place button. **(3) Place-by-pointing:** a click on the section drops the shape as the matching canonical **`PlacedBar`** at the mapped `(u,v)` (pointer via `getScreenCTM`, GPU/owner-verified; the typed-coord + Place is the headless-tested a11y twin, §0.3.3). **(4) Snap:** pure `engine/placement.ts` — `snapSection` clamps into the cover envelope (`cover+Ø/2`) + a 5 mm grid; `buildPlacedBar(kind)` builds a single `AddressableBar` / a 3-bar `row` / a 2-bar `bundle` / a 2nd `layer` (face by `v` sign); `freshPlacedId` = first free `p{n}`. **(5) All new steel lands in the v1.0.5 `doc.placed` model** — the adapter (`solveDoc`) already threads `placed` to the engine on **all 8 elements**, so a placed bar renders + schedules + is validated for free (verified: a placed single solves clean → a `standalone` longBar). **(6) Store:** `SectionTool` extended (`add-single/row/bundle/layer` + `measure`); `paletteShape`/`paletteDiameter`/`placeCoord` + `setPlaced`/`placeInSection` (session-only, NOT in `.rcfg`, reset on `reset()`). **(7) Placed-bar list** in the palette (step count/n + remove). `apps/web`+`placement.ts`-only; no core/exporter/golden. **Measure is a coordinate readout only (full distance-measure deferred to N6); deeper per-field row/bundle/layer editing via the N3 Inspector is a thin follow-up (flagged). Needs owner GPU pass.** | ✅ **CODE DONE + tested** · NOT pushed | Amer, 2026-07-11 |
| **v1.0.6 Phase N4 (Track U1) — the workspace shell (3D-primary + editable 2D docks)** — the drawing-board shell. **(1) Shell (`App.tsx`):** the app-body is now `sidebar (inspector region) │ workspace │ right column`; the `workspace` is a flex column of `workspace-center (viewport always-on + section dock)` over the `elevation dock`. The live 3D `Viewport` stays central + always-mounted. **(2) `SectionDock.tsx`:** a first-class resizable/collapsible 2D panel to the RIGHT of the 3D hosting the ONE unified `SectionCanvas` (N2 pick surface, column/beam) + the `CoupePanel` — it **subsumes the old F4 Coupes bottom dock** (`BottomPanel.tsx` deleted; "the coupe IS the editable section"). **(3) `ElevationDock.tsx`:** a resizable/collapsible panel BELOW the 3D showing a **read-only** longitudinal `ElevationView` (SVG) built from the SAME pure `buildElevationFiche(result)` that feeds PDF/DXF — so screen == paper; **editing the elevation (drag ends/laps/zones) is deferred to Track U5 / Phase N6** (flagged in the dock note). **(4) Left region = inspector:** `SectionCanvas` moved OUT of the Sidebar into the section dock; the Sidebar keeps `ZoneReadout` + `ElementSetupStrip` + `Inspector` + the "Avancé" tabbed-form fallback. **(5) Store:** new session-only `docks: {section,elevation}: {open,size}` + `toggleDock`/`resizeDock` (min-size floored, NOT in `.rcfg`, NOT reset by `reset()`); `bottomPanel`/`setBottomPanel` removed; Navbar Coupes button → `toggleDock("section")`. Resize + collapse each have keyboard twins (a11y §0.3.3). `apps/web`-only; no core/exporter/golden. **6 canvas-coupled specs updated to render `<SectionDock/>` (mechanical; assertions unchanged); `workspace_layout.spec` de-bottom-dock'd; new `workspace_shell.spec`. Needs owner GPU pass.** | ✅ **CODE DONE + tested** · NOT pushed | Amer, 2026-07-11 |
| **v1.0.6 Phase N3 (Track U3) — contextual inspector + unified selection + advanced-form fallback** — the drawing-first editing surface. **(1) Unified selection (§0.3.4):** one `selection` (`bar` / `extra` / `crosstie` / `alert` / null) + a single `select()` router folds the two old channels (`selectedBars`, `selectedGroupIds`) into ONE addressable selection — mutually exclusive, keeping the legacy highlight arrays in sync so the 3D/2D viewport is untouched; `pickSectionBar`(select), `pickSectionExtra`, `selectGroups`(alert rows), the new cross-tie rows, and the **3D bar click** (single-select now) all route through it. **(2) `Inspector.tsx`:** a selection-driven panel showing ONLY the selected object's fields — a longitudinal bar (Ø/shape via reused `FaconnageEditor`/length/axial/remove/reset), an independent extra (u/v/Ø/shape/length/axial/remove), a cross-tie (pair + remove), or an alert (read-only highlight); empty-state hint otherwise. Writes the SAME store as the form (neither authoritative). **(3) `ElementSetupStrip.tsx`:** a compact always-visible strip (element id + code pack + b/h + H/L + cover) so element setup is no longer a tab-flip away (answers U6-IA). **(4) Advanced fallback:** the full tabbed form kept behind an **"Avancé"** toggle (`advancedForm`, session-only, default **ON** = no regression); collapsing it leaves the drawing-first surface (canvas + inspector) live. `SectionCanvas` moved from inside the scheme controls to the always-visible region (still exactly ONE). `ZoneReadout` preserved. `apps/web`-only; no core/exporter/golden. **row/bundle/layer inspection + selecting them lands with the N5 placement palette (per §7).** | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-10 |
| **v1.0.6 Phase N2 (Track U2) — one unified section canvas + tool routing** — the section diagram was embedded **three times** (bar-by-bar / cross-tie / supplement), each with its own pending-pick state. N2 collapses them into **ONE persistent `SectionCanvas`** (top of the column/beam scheme controls) whose behaviour is governed by a store **`sectionTool`**: `select` → a bar click sets the addressable-bar target (`selectedBars`); `link` → two clicks make a cross-tie **or** bind the armed supplement (the driving panel arms `beginLink({kind})`; the store router `pickSectionBar` completes it). The three panels lost their embedded pickers → `AddressableBars` reads `selectedBars`, `CrossTieEditor`/`SupplementsPanel` gained a "link two bars" arm/cancel button. `SectionPicker` stays the pure SVG renderer (+ its keyboard/a11y list, invariant §0.3.3). Tool state is **session-only** (not in `.rcfg`). `apps/web`-only; no core/exporter/golden. **Select + Link first (per §7); add-bar/row tools are N5.** | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-10 |
| **v1.0.6 Phase N1 (Track U6) — control-type pass** — right widget per parameter (UI review Finding U4). New `ui/Stepper.tsx` (integer **− N +** with an editable numeric/keyboard twin) for **counts** (bars-per-face, chapeau/montage/span/relevé/generic counts, seismic zone) + new `ui/NumberInput.tsx` (plain number box, **no slider, no `dragMode`**) for **design inputs** (As,req/Asw,req/provided areas) and non-geometry precision values (spacing, anchorage, chapeau length, support width, splice station, material grades, cross-tie angle). The slider+`dragMode` `NumberField` is now kept **only** on dimensions genuinely tuned against the live 3D (b/h/H/L/cover + generic geometry). Diameter stays a `<select>` (already correct). `apps/web`-only (Sidebar + AddressableBars/CrossTie/RegionEditor rewired; FaconnageEditor left on `NumberField` — its shape params ARE live-3D-tuned). Purity held; no core/exporter/golden touched. | ✅ **CODE DONE + tested** · NOT pushed | Zayd, 2026-07-10 |
| **v1.0.5 Phase 7 (M7) — lossless canonical file (Track E)** — the `.rcfg` canonical arrays are now **lossless for interchange** (a future server/IFC/other tool reads the real bars from them alone, not the private `meta.app_document` blob). **Contract extension (audit B4):** new core `PlacedBarElement` kind `"PLACED_BAR"` on the `ReinforcingElement` union (+ `isPlacedBarElement` guard) — expresses a bundle/row/layer/curtailed bar (id·role·shapeArchetypeId·Ø·position·axisStart·cutLength·start/endStation·anchorage·placedKind/parentId·splice tally) that a group-based `BarGroup` cannot. **`exporters/canonical.ts`:** new `placedBarsFromResult(result)` maps each resolved `result.longBars` (`PlacedLongBar`, skips `removed`) → a `PLACED_BAR`; the group-based `baseGroups`/`supplementalGroups` (transverse + scheme summary) are unchanged. **`rcfgDoc.ts`:** the canonical `reinforcement.placedBars[]` (new, additive on `RcfgReinforcement`) is populated; **`seismic` is now written** from `doc.seismic` (was hardcoded `null`, D-P4b-5) via `seismicOf`; `codePack` already followed the picker; **double-solve stopped** — `docToRcfg` takes an optional live `result` (reused verbatim, re-solves only when absent), threaded through `projectToRcfg(resultsById)` + `useAutosave` (active instance's store `result`). **`rcfg.ts`:** `CURRENT_RCFG_VERSION` **1.2 → 1.3** (additive/idempotent; new `"1.2"→"1.3"` step + existing steps re-pointed). `app_document` stays as a **convenience carry**, no longer the sole truth. Forward-compat preserved AND newly exercised for our OWN kind (an old reader round-trips a `PLACED_BAR` file). No golden moved. | ✅ **CODE DONE + tested** · NOT pushed | Amer, 2026-07-10 |

**Where things stand (2026-07-11).** The app is **feature-complete through v1.0.4** (single-tenant product:
8 elements, façonnage, true bar geometry, addressable bars, beam two-support model, splices, shop drawings,
canonical `.rcfg`) and **ALL of v1.0.5 (M1 → M7) is now done — the release is code-complete.** — the placement defects D1/D2/D3 are killed (`D-V105-1`), **free placement (any single bar, any position)
works on all 8 elements** through one canonical model + one shared pass (`D-V105-2`), **the four detailer
capabilities beyond single bars — counted/spaced rows, multiple flexural layers, bundles, side-face skin — place,
schedule, feed As/`d`, and render faithfully in the coupe** (`D-V105-3`), **every one of those new kinds of steel
is VALIDATED with the honest 🔴-block / 🟠-warn split** — bundle count/cover, skin minimum, multi-layer spacing,
curtailment anchorage, plus the element-agnostic geometry generalised to all elements, with the F1 guarantee and the
affected-bar-id red (`D-V105-4`), **splice / coupler reach extends off the RECT-only path** — a > 12 m
circular/pile cage auto-splits its native zone at stock (segments + coupler tally + group stagger WARN) and **any**
freely placed bar splices with the per-bar stagger check on every element, while slab/mesh distribution stays
unspliced (`D-V105-5`), and now **the BBS + shop drawing speak the French façonnage convention** — a pure labelling
layer that renders each shape under its French designation (Droite/Chapeau/Étrier…) on the French BBS columns
(repère/nombre/Ø/longueur de coupe/façonnage/masse) across PDF/DXF/on-screen, with bundles/rows/curtailed bars
scheduling faithfully; the nomenclature ships **PROVISIONAL** until the owner supplies the table (`D-V105-6`,
`[OWNER-DEP §O-1]`; see the seven newest §9 entries), and finally **the `.rcfg` canonical arrays are lossless for
interchange** — a new `PLACED_BAR` reinforcing-element kind expresses every freely-detailed bar (bundle/row/layer/
curtailed) in the canonical model, the live `seismic` regime + `codePack` are written, the double-solve on save is
gone, and forward-compat is proven for our own new kind (`D-V105-7`). **v1.0.6 (the drawing-board UI) is now
CODE-COMPLETE (N1 → N6).** Phases **N1 (Track U6, control-type pass, `D-V106-1`)**, **N2 (Track U2, one unified
section canvas + tool routing, `D-V106-2`)**, **N3 (Track U3, contextual inspector + unified selection,
`D-V106-3`)**, **N4 (Track U1, the workspace shell, `D-V106-4`)**, **N5 (Track U4, the tool palette +
place-by-pointing, `D-V106-5`)** and now **N6 (Track U5, the editable elevation, `D-V106-6`)** are all done: N1 gives
the form the right widget per parameter; N2 collapses the three embedded section pickers into ONE canvas whose active
tool decides what a bar click means; N3 makes editing selection-driven; N4 restructures the shell into the
drawing-board (always-on 3D flanked by a `SectionDock` + an `ElevationDock`, the `Inspector` on the left); N5 makes
"add any bar" a visible modal tool + shape/row/bundle/layer palette that drops the matching canonical `PlacedBar` in
`doc.placed` on all 8 elements; and **N6 makes the elevation EDITABLE — drag a bar end to curtail it, drag a
stirrup-zone boundary, drop a lap/coupler, set an end anchorage, set a relevé bend-up — each committed through the
v1.0.5 per-bar station model with a numeric + keyboard twin (inspector fields, the `RegionEditor` table, the relevé
bend field), no engine/validation/schedule/`.rcfg` change.** A detailer now **works on the drawing** (spec §9
"complete"). v1.0.1 + v1.0.3 were **pushed**; v1.0.2, v1.0.4, **all of v1.0.5 M1–M7 are on origin** (M7 = `d853188`),
**N1+N2** (`ea68924`) and **N3** (`5989f5f`) are on origin; **N4 + N5 + N6 are CODE DONE + green but NOT yet pushed**
(hand back — owner to say when to push). Detail per version is condensed in the §9 **Archive**; the load-bearing
decisions are the §8 ledger.
**New agent:** read `core_logic.md` + `architecture_breakdown.md`, then `current_state.md` §1/§7/§8/§9, then the
active spec (`v1.0.6_spec.md` — now fully implemented N1–N6; v1.0.5 code-complete M1–M7). **There is no N7 — v1.0.6
is done.** The next NEW work is a fresh spec (a v1.0.7 point release, or the **v1.1 server jump** per `core_logic
§9.2` — accounts / cloud / IFC on top of the unchanged pure engine). Near-term, the outstanding work is **people-gated,
not code**: the owner's GPU/visual acceptance sweep across the whole v1.0.6 arc (`owner_tasks.md §D`, D-9 the editable
elevation being the largest) and the last product call — the **drawing-first default** (`advancedForm`, `owner_tasks
§A-8`). The one thin code follow-up flagged from N6: wiring N5's placed **rows/bundles/layers** into the unified
`selection` so the elevation/inspector station edits reach them too (the store actions already key off `selection`).
**⚠ Owner IA/default choice STILL open (flagged, not guessed):** `advancedForm` defaults **ON** (drawing-first
surface ABOVE the full tabbed form) — the spec's end-state is drawing-**first** (form as fallback, default OFF).
With N5 (place-by-pointing) **and** N6 (editable elevation) both present, flipping the default now strands nothing —
it's a one-liner (`advancedForm` init `true`→`false`), but it changes the app's first impression, so it stays the
owner's call (`owner_tasks §A-8`). **Recommend the owner decides now** (v1.0.6 is otherwise complete). Standing
people-gated items (engineer sign-offs, owner data, owner GPU visual passes) are unchanged — consolidated in the new
spec-independent `owner_tasks.md` (see §D there) and the §9 flags.

**Current green gate (v1.0.6 Phase N6, Hetzner dev box — Zayd, NOT pushed):** `npm run check` ✓ — **830 tests /
152 files** (+1 file = `ui/elevation.spec.tsx` +14; over N5's 816/151); `npm run build:web` ✓ (pre-existing three.js
chunk-size warning only). **No golden snapshot moved** — N6 is `apps/web` + the new pure `engine/elevation.ts`
(transform/station/snap helpers); no core/exporter/manifest change, so purity + every existing golden hold. **One
existing spec updated** — `workspace_shell.spec` flipped its N4 "elevation read-only, editing deferred to N6"
assertion to the now-editable `.elevation-svg-edit`. **N5 (816/151) is the prior baseline — N4, N5 and N6 are all
unpushed on this branch.** Did not run `coverage` (UI + a small pure helper, above the ≥90 core/adapter thresholds;
`check` is the honest gate). **M7 coverage baseline (unchanged):** core **96.59 %** stmts / 99.4 % functions (≥ 90);
adapter **97.15 %** — N6 adds only UI + a small pure helper + a one-line adapter read, above thresholds.
**Run the two coverage jobs SEQUENTIALLY** — they share `coverage/.tmp/`; `npm run check` alone is the honest gate. ⚠ **Known Windows-box flake:** under `npm run coverage` (v8 instrumentation) the M3 perf case
`perf_heavy.spec.ts:199` reads **21 ms > 16 ms** live-solve budget — a coverage-timing artefact, NOT a runtime
regression (it passes green under `npm run check`, the non-instrumented honest gate; documented at M5/M6 as the
Windows-box flake). Coverage % above captured by excluding that one timing test.

**Standing open items (acceptance / people-gated, NOT code):** engineer sign-offs G-BAEL/EC2/RPS/COUPE/TOL
(constants ship `_provisional`; **`code.bundleEquivDiameter` = φ√n≤55** and the **new M4 limits** —
`bundleMax`/`bundleMaxAtLap`, `skinReinforcement` depth-trigger 1000 mm + per-face min 0.1 %·b·h + spacing ≤ 300,
`curtailmentHookedFactor`, and the between-layer floor `max(Ø,20)` with `dg` not folded — all ride G-BAEL/EC2/TOL
provisional, sourced in `*-constants.json` for easy ratification); the owner GPU/visual passes (the headless gate
can't see WebGL — ViewCube, persp⇄ortho, coupe handle, 3D bar click, cross-tie render, stepped stair/mesh grid are
code-complete but await a visual pass on the owner's Windows GPU: `cd apps/web && npm run dev`, port 5180); the
owner-gated data items (materials, façonnage codes, **stock length 12 000 mm — M5 `[OWNER-DEP §C]`**, curtailment
point). Still open: the **pre-existing `beamLayoutBars` ↔ engine-layout mismatch** (SUM vs MAX `nTop`, cross-tie
mis-bind — Amer's M1 flag 1; NOT touched by M2–M5, out of core-placement scope). **M5 flag — seismic × circular:**
the RPS overlay is composed on RECT column/beam only (D-P4b-2), so `lap_in_critical_zone` is **inherently N/A on the
circular/pile pipeline** — a spliced spiral-column cage in a seismic zone gets the stagger WARN but no critical-zone
lap check; wiring an overlay onto E-COL-02 is a separate owner-gated decision (see the newest §9 entry). **Track-V
DONE for M4's scope** (bundle/skin/layer/curtailment tiers + generic geometry + affected-bar id + F1). **Remaining
Track-V slice (deferrable):** (a) **generic (non-RECT) placed bars still do NOT feed per-metre As/§7** — rows/bundles
feed As only on RECT via the A2 reconciliation (extending the As-credit + slab/circular spacing predicates to placed
bars on generic elements is the last slice of V-A); (b) **bundle-to-neighbour clear spacing uses bare Ø, not φₙ**
(the `bundle_cover` envelope check is the shipped V-B geometry rule); (c) a **`Layer` on a CIRCULAR section is
skipped** (no face). The **skin BarRow supersedes** the old SKIN-supplement fanout for new detailing, but that legacy
supplement path is **left in place** (not ripped out) — see the §9 entries, flags.

---

## 2. Authoritative companion docs (read these first, every time)

- **`v1.0-Spec.md`** (revision **r3**) — the build contract: *what & why*. The `[REF-…]` tags and
  `§` numbers are the stable references everything else cites. **If as-built reality diverges from
  the spec, say so in your log entry.**
- **`v1.0_imp_plan.md`** — the 7-phase plan (P0–P6 = M0–M6): *task-by-task how*, with each phase's
  steps, tests, Definition-of-Done, and a §V validation matrix mapping plan↔spec.
- **`current_state.md`** (this file) — the *as-built* reality + the handoff log.

> The plan records the *intended* reality; this file records the *actual* reality. Trust this file
> for "what exists now", trust the spec for "what it should ultimately be".

---

## 3. Hard rules every agent obeys (do not violate without recording a decision in §9)

1. **Everything structural is DATA.** Elements, shapes, schemes, supplements, code rules are JSON
   manifests + code-pack functions. **Adding an element/shape/scheme/code must need NO engine
   rewrite** (spec §0.1, §1). If you're about to write `if (elementType === …)` or
   `if (codePack === …)` inside `packages/core`, **stop** — it belongs in a manifest or a registered
   generator. (Plain language: the engine is a generic machine; the "knowledge" lives in data files.)
2. **Engine purity.** Nothing under `packages/core/` may import React, `three`, or touch the DOM
   (`window`/`document`). Enforced by `npm run check:purity` + `tests/engine-purity.spec.ts`. This
   is what lets the same engine power the UI, the exporters, the tests, and the future 1.1 server.
3. **Determinism.** The solver is pure functions: same input → identical output. No `Date.now()`,
   no `Math.random()` in `packages/core`.
4. **SI internally, convert at the edge.** Engine stores mm, mm². UI/IO converts to the French
   display units (geometry mm, steel area cm² or cm²/m, weight kg).
5. **Code-pack agnosticism.** Archetypes/schemes/validator call `code.*` named functions
   (`CodePack` interface, `packages/core/src/types/codepack.ts`). BAEL vs EC2 = swap the
   implementation behind the names, nothing else.
6. **`.rcfg` forward-compat (NORMATIVE, spec §10).** A reader must **preserve unknown fields and
   unknown `ReinforcingElement.kind`s on round-trip** — so a future file containing `TENDON`s
   survives a v1.0.0 load. Already locked + tested in P0; don't regress it.
7. **Engineering correctness is gated by a human.** Numeric code constants (BAEL/EC2/RPS) and the
   reference cases are **signed off by a qualified structural engineer** before that math is "done"
   (spec §11, §14). See §6 below. Until signed, ship constants flagged `"_provisional": true`.
8. **Every phase ends green:** `npm run check` (purity → manifests → typecheck → tests) passes,
   plus the new phase's tests. Update §1 and append a §9 entry.

---

## 4. How to run (commands, on the dev box)

```bash
npm install            # once; npm workspaces (Node 20, npm 10 on this box; no pnpm)
npm run check          # FULL gate: purity → manifests → typecheck(core) → typecheck:web → tests
npm run test           # vitest run (both projects: core=node, web=jsdom)
npm run check:manifests# JSON-Schema + cross-ref validation of apps/web/manifests
npm run check:purity   # engine-purity scan of packages/core
npm run typecheck      # tsc --noEmit on the engine + tests (root tsconfig)
npm run typecheck:web  # tsc --noEmit on apps/web (DOM/React tsconfig)
npm run build:web      # vite production build of the SPA (proves browser-bundleability)

# Run the SPA (P2):
cd apps/web && npm run dev    # vite dev → http://127.0.0.1:5180  (SmartBar's port; 5173 is Planitor)
```

The SPA exists now (P2): pick `E-COL-01`, edit geometry/material/cover/Ø/spacing/counts, see
live 3D + live alerts, failing bars turn RED. Run it via `vite dev` (port **5180**). Headless
boxes can't see WebGL — prove it with `npm run build:web` + the `web` vitest suites instead.

---

## 5. Repo map (as built in P0)

```
package.json              # root; npm workspaces ["packages/*","apps/*"]; scripts; devDeps
tsconfig.base.json        # shared compiler options (ES2022, moduleResolution "Bundler", strict)
tsconfig.json             # root typecheck (noEmit) over packages + tests + scripts
vitest.config.ts          # test runner; include tests/**/*.spec.ts
scripts/
  check-manifests.ts      # CI gate → calls core.checkManifestDir(apps/web/manifests)
  check-engine-purity.mjs # CI gate → scans packages/core/src for React/three/DOM
packages/core/            # @rebarconfig/core — the PURE ENGINE (no DOM/React/three)
  package.json            # main/types → src/index.ts (dev-time TS entry; dist build added later)
  schemas/*.schema.json   # JSON Schemas: shape, element, scheme, supplement, rcfg
  src/types/              # the 6 frozen contracts (see §6 of this map below)
  src/integrity/index.ts  # manifest-integrity gate (ajv + cross-ref + expr-scope check)
  src/geometry/           # P1: expr.ts (mathjs frozen scope) + segment-grammar.ts generator
                          #   P4a: registry.ts (generateShape seam) + bespoke/{helix,mesh}.ts (§5.2.1g)
  src/layout/             # P1: rect.ts — corner-share layout + computed d/d' (computeZoneGeometry)
                          #   P4a: circular.ts (EQUAL_PERIMETER pitch circle) + slab.ts (per-metre)
  src/validation/         # P1: index.ts (validateColumn, tierFor, …) + P3: profiles.ts
                          #   (VALIDATION_PROFILES: BAEL_COLUMN/BAEL_BEAM — D-P3-1; +P4a CIRCULAR_COLUMN/
                          #   SLAB_ONEWAY/SLAB_TWOWAY) + P4a predicates.ts (§7.13 slab/corner-torsion)
  src/scheme/             # P3: resolve.ts — scheme→zone map, placement+supplement resolvers,
                          #   nearestBarIndex, computeCurtailment (§5.3–5.7, D-P3-4)
  src/pipeline/           # element.ts: generic solveElement (D-P3-1) + solve.ts: solveColumn shim
                          #   P4a: circular.ts (solveCircular) + slab.ts (solveSlab) — section pipelines
                          #   P5: every result now carries member: MemberPlacement (D-P5-1, §9.5)
  src/section/            # P5: Section/Coupe engine (§9.5) — place.ts (placeBars: world geometry),
                          #   sectionAt.ts (sectionAt → CoupeView + defaultCoupeFor), types.ts,
                          #   convention.ts (provisional coupe conventions → G-COUPE)
  src/index.ts            # public surface: types + geometry/layout/validation/profiles/scheme/pipeline
packages/codepacks/       # @rebarconfig/codepacks — packs behind core's CodePack iface (P1, P4a)
  src/bael/bael-constants.json  # ⚠ PROVISIONAL BAEL constants, "_provisional": true (G-BAEL)
  src/bael/index.ts       # makeBaelPack(): BaelPack — code.* impls + PackExtras (bands, ls, slab, …)
  src/ec2/ec2-constants.json    # ⚠ PROVISIONAL EC2 constants, "_provisional": true (G-EC2) [P4a]
  src/ec2/index.ts        # makeEc2Pack(): Ec2Pack — same code.* names, EC2 forms (§7.1–7.8) [P4a]
packages/exporters/       # @rebarconfig/exporters — P5 export engines + project I/O (depends on core)
  src/bbs.ts              # computeBBS(result) → §9.1 schedule (unitMass, marks, merge, summary) + nestCuts
  src/dxf.ts              # DxfBuilder (R12) + buildDxf1 (elevation+default coupe) / buildDxfCoupes (DXF-2)
  src/pdf.ts              # buildPdf(result) → Uint8Array (cartouche + views + BBS + stamp); pdf-lib; 🔴-locked
  src/export-lock.ts      # canExport/assertExportable/ExportLockedError + statusStamp (§7.9/§7.12)
  src/rcfg.ts             # serializeRcfg/parseRcfg (+migrate), section_cuts, AutosaveManager + IndexedDB store
  src/index.ts            # barrel (BBS/DXF/PDF/export-lock/.rcfg). PURE except pdf-lib + browser IndexedDB glue
apps/web/                 # @rebarconfig/web — the SPA (P2): React 18 + Vite + Zustand + R3F
  index.html  vite.config.ts  tsconfig.json  vitest.setup.ts
  manifests/              # the real data (P3): shapes/ (10) elements/ (2) schemes/ (4) supplements/ (7)
  src/
    main.tsx App.tsx styles.css
    engine/               # adapter (NOT engine code): document.ts (ElementDoc = column|beam + factories),
                          #   manifests.ts (full registry), solveDoc.ts (doc→solveElement, 2-pass supplements)
    store/                # useStore.ts — Zustand; element/scheme catalog + supplements + expert; re-solves sync
    viewport/             # Viewport.tsx (R3F Canvas) + Rebar.tsx + rebarProps.ts (PURE mapping, both elements)
    ui/                   # Navbar Sidebar SupplementsPanel AlertsPanel NumberField derived.ts
    i18n/strings.ts       # FR/EN bundles (typed)
    *.spec.ts(x)          # P2 web suites (run under the jsdom vitest project)
tests/                    # P0 contract tests + P1 engine suites + fixtures (valid/ + invalid/)
vitest.config.ts          # core project config (node env)
vitest.workspace.ts       # P2: two projects — core (node) + web (jsdom + react plugin)
```

**The six frozen contracts (P0 deliverable, spec §12 M0)** — `packages/core/src/types/`:
1. `shape.ts` — Shape Archetype segment grammar (spec §5.2.1).
2. `distribution.ts` — Distribution: count or segmented spacing (§5.1.1).
3. `reinforcing-element.ts` — `ReinforcingElement` supertype + `BarGroup` + `isBarGroup` guard
   (§0.2.1, §10).
4. `layout.ts` — Layout-solver I/O incl. computed effective depth `d`/`d'` (§6.1 [REF-SYS-611]).
5. `codepack.ts` — the `code.*` `CodePack` interface + `RuleResult` (§7.11).
6. `rcfg.ts` — the `.rcfg` envelope (§10).
Manifest shapes are pinned by JSON Schema (`packages/core/schemas/`), not TS.

---

## 6. Open engineering sign-off gates (human dependency — track, don't skip)

These block *acceptance*, not coding. Code against **provisional** constants (flagged in JSON) and
keep moving, but a phase is not "accepted" until its gate is signed (spec §11, §14).

| Gate | Blocks | What must be ratified |
|---|---|---|
| **G-BAEL** | P1 acceptance | BAEL 91-99 constants table (spec §7.11) + reference cases (§14.7) |
| **G-TOL** | P1 / P4 | per-rule WARN-vs-PASS tolerance bands (§7.12, §14.8) |
| **G-EC2** | P4a | EC2 reference cases |
| **G-RPS** | P4b close | RPS-2011 ⚠ cells: `l_c`, critical-zone spacing per ND class, min tie ø, required confinement, zone enum + `a_g` map (§7.10b, §14.6) |
| **G-COUPE** | P5 (coupe) | coupe drawing conventions (§9.5, [REF-EXT-GAP-7], §14 items 14–17): near-parallel angle threshold, default look-behind depth, cutting-line/tag style, default coupe station |
| **Sign-off** | release | full BAEL/EC2/RPS reference-case suites signed by the nominated engineer (§14.9) |

**Owner action pending:** §14 open items 1–13 (starter scheme list, default materials, diameter
set, cartouche fields, RPS defaults, the constants tables, WARN bands, curtailment defaults, cover
special cases, corner-torsion trigger, leg-counting policy, and *nominate the structural engineer*).
None block P1 *coding*, but G-BAEL must be signed before P1 is *accepted*.

---

## 7. Environment / gotchas (read before coding)

1. **Node 20.20 / npm 10.8 on this box; no pnpm.** Use `npm` workspaces. `package-lock.json` is
   committed.
2. **`@rebarconfig/core` `main`/`types` point at `src/index.ts` (raw TS) — and that's now proven in
   the browser too.** vitest/tsx transpile on the fly; **Vite** also consumes the raw TS (the
   workspace packages are in `optimizeDeps.exclude`). So **there is still no `dist` build of core**,
   and P2 did NOT add one — `vite build` succeeds against the raw source. See **D-P2-2**. The one
   change P2 needed was moving the Node-only integrity gate off the runtime barrel (**D-P2-1**), so
   the browser graph is free of `node:*`. Don't add a core `dist` build unless a *Node* consumer
   needs compiled JS.
3. **ajv draft 2020-12:** the integrity gate imports `ajv/dist/2020.js` (not the default `ajv`
   entry, which is draft-07). Keep that if you add schemas.
4. **Purity/scan scripts strip comments before matching** so prose like "JSON document." is not a
   false positive. If you extend the forbidden list, keep the comment-stripping.
5. **`npm audit` shows vulnerabilities** in the dev toolchain (esbuild/vitest transitive). Not
   shipped to users (dev-only), safe to ignore for now; revisit at P6 hardening.
6. **Work lives on branch `feat/p1-m1-engine`** (tracks `origin/feat/p1-m1-engine`), pushed through
   v1.0.1 + the v1.0.2 docs (latest `d0c66f1`). Per `cross_projects_policy.md` §10, **commit/push only
   when the owner asks.**

---


## 8. Key decisions ledger (don't re-litigate; one line each — full rationale is in git history + the specs)

**Standing invariants (the load-bearing ones — break these and the architecture breaks):**
- **D-P0-2** `.rcfg` forward-compat via JSON-Schema if/then: unknown `kind`s + unknown fields are preserved on round-trip. **Never tighten to reject unknown kinds.** Every later persisted field (section_cuts, project elements[], v1.0.2 cross-ties/regions/shapes) rides this.
- **D-P1-1** `cutLength = Σ schedule legs + Σ hookExt − Σ bendDeduction` (NOT the polyline sum / rounded centreline); the generator cross-checks vs `totalLengthExpr` and **throws on disagreement**. Don't "simplify" it (v1.0.2 F6 must keep this).
- **D-P1-3** code pack is dependency-injected; **`packages/core` never imports `@rebarconfig/codepacks`**. PackExtras read through an optional `CodePackExtras` surface.
- **D-P1-5** effective depth `d`/`d'` is a first-class per-zone output (`computeZoneGeometry`), area-weighted from real bar centroids — never `0.9h`.
- **D-P3-1 / D-P4a-2** one generic `solveElement` + a validation-profile registry; section dispatch (rect/circular/slab) + profile id, **never `if (elementType===…)`** in core. `solveColumn`/circular/slab/stair/joist are thin shims.
- **D-P3-4** supplements/cross-ties bind by **STABLE bar indices, never coordinates** (`nearestBarIndex` backs both 3D-click and keyboard); a base change re-solves them.
- **D-P4a-1** the shape registry is the single geometry seam; only `SPIRALE_HELICE` + `TREILLIS_MESH` are bespoke; everything calls `generateShape`. New shape = new manifest, no pipeline change.
- **D-P4b-1** the seismic overlay is a SECOND injectable contract (`SeismicOverlay`), parallel to `CodePack`; `makeRpsOverlay` rides on either base pack. **Don't fold seismic constants into the base packs.** Constants provisional → G-RPS.
- **D-P5-1** world placement is a first-class core output (`SolveResult.member` = axial prism); `placeBars` is the single source for 3D + coupes + the fiche.
- **D-P5-4** the export engines are a separate pure package `@rebarconfig/exporters` (only `pdf.ts` pulls pdf-lib, lazy; `rcfg.ts` touches IndexedDB behind an injected `KeyValueStore`). Keep core lean.

**Shipped-phase decisions (terse; superseded notes inline):**
- **D-P0-1/3/4/5** distribution-mode vs placement-principle split · `isBarGroup` guard · `code.*` arg shapes (goodBond/asReqOverProv/d/NEd/fire) · archetype expr symbols ∈ params ∪ math ∪ ENGINE_GLOBALS (extend in `integrity/index.ts` only).
- **D-P1-2** `code.bendDeduction` is a provisional geometric model (→ G-BAEL); `l_s` computes ≈44φ vs tabulated 40φ — a flagged gap.
- **D-P2-1** integrity gate is a Node-only subpath `@rebarconfig/core/integrity`; the runtime barrel stays browser-pure. **D-P2-2** no `dist` build of core — Vite transpiles raw TS. **D-P2-3** the SPA is a dumb renderer; `rebarProps` is a pure mapper. **D-P2-4** the store always re-solves on commit; `dragMode` only degrades render + defers alerts. **D-P2-5** superseded (all 8 elements now wired, D-P6-3).
- **D-P3-2** beam = one representative cross-section (span-bottom / top-support / shear); left/right support split deferred. **D-P3-3** `computeCurtailment` feeds chapeau cutLength. **D-P3-5** UI = `ElementDoc`, two-pass supplement solve, scheme switch preserves geometry/material/cover/exposure. **D-P3-6** supplement 3D placement was centred-for-presence — **expanded by v1.0.2 F2/F7 (anchored at the engaged bars)**.
- **D-P4a-3** profiles pack-agnostic (`BAEL_` prefix is legacy naming); families CIRCULAR_COLUMN / SLAB_ONEWAY / SLAB_TWOWAY. **D-P4a-4** EC2 pack behind the same `code.*` names (G-EC2). **D-P4a-5** slabs per-metre + spacing-driven; topological predicates pure. **D-P4a-6** helix/mesh closed-form; 3D mesh render indicative.
- **D-P4b-2** overlay composes AFTER the base profile in `solveElement`; seismic segments (END/MIDDLE) are DATA, column/beam only. **D-P4b-3** stair + joist are slab-family sections. **D-P4b-4** `MARCHE_PALIER` is a generic polyline; re-entrant-corner risk is a predicate. **D-P4b-5** hook/lap/engagement facts are overlay INPUTS; `longBarsEngaged` was a placeholder — **made real by v1.0.2 F2**.
- **D-P5-2** `sectionAt` is pure plane∩geometry; coupe conventions provisional (G-COUPE). **D-P5-3** coupe exact for axial members, representative for slab-family. **D-P5-5** DXF is a hand-rolled deterministic R12 writer on the 4 strict layers. **D-P5-6** `.rcfg` forward-compat + `section_cuts[]` need no core-contract change; export lock on 🔴 FAIL. **D-P5-7** SPA `.rcfg` round-trips via `meta.app_document`; canonical §10 `reinforcement[]` mapping deferred.
- **D-P6-1** 3 control defects fixed (8a column symmetry selector · 8b beam full-length top bars · 8c brins auto-draw — **8c's centred cross-tie is the bug v1.0.2 F2 replaces**). **D-P6-2** ViewCube specced → shipped (D-V101-2). **D-P6-3** all 8 elements + seismic picker wired into the SPA; the viewport consumes core `placeBars`/`member` (RECT box / CIRCULAR cylinder).
- **D-P7-1/2/3** multi-element project model: `Project` = ordered `ElementInstance[]` with a checkout model, `quantity` scales TOTALS only (one solve per type), quantity-aware steel takeoff, `.rcfg` v1.1 envelope + legacy-1.0→1-instance migration; elevation *fiche* specced → shipped.
- **D-V101-1** v1.0.1 Feature C is pure exporter/UI: `computeBBS markPrefix` (namespaced); shared `exporters/fiche.ts` (orientation + marks + tie callout + dims) drives PDF **and** DXF; `buildProjectPdf` = sheet-per-type + summary + per-project export-lock; `exportProject{Pdf,Dxf,BbsJson}` + Navbar "Projet complet"; `moveInstance` reorder. Project-level shared settings deferred.
- **D-V101-2** v1.0.1 Features A/B are camera/interaction only — NO engine/`.rcfg`/validation change. `viewport/cameraState.ts` (26 named views + element-aware member rotation), `ViewCube.tsx` (drei gizmo + persp⇄ortho + Home + a11y named-view list), `coupeHandle.ts` + `CoupeOverlay.tsx` (drei `DragControls` handle → `updateCut`). View/roll state is **session-only, not in `.rcfg`**. Filename note: widget is `CoupeOverlay.tsx`, pure helpers `coupeHandle.ts` (case clash on Windows). **GPU widgets need the owner's visual pass.**
- **D-V102** v1.0.2 specced + planned: F1 in-plane view roll · F2 column cross-ties rebuilt on real bar engagement (+ Auto-code, real `longBarsEngaged`) · F3 sticky per-zone readout · F4 expandable right-column layout (Verif+Project+BBS) · F5 user transverse regions (per-region spacing) · F6 façonnage editor (catalog + params + end-hooks + Z/double-crank/stepped) · F7 2D section picker (replaces raw index binding). Build order **F7→F2→F3→F4→F5→F6→F1**. F2+F5 add core capability via existing seams (placement anchor; transverse segments) — no element branching. No new sign-off gate (F2-Auto/F5-seismic ride G-RPS). Owner decisions in `v1.0.2-Spec.md`; phases/tests/DoD in `v1.0.2_impl_plan.md`. **P1(F7)+P2(F2) now implemented — see D-V102-1/2.**
- **D-V102-1** (P1/F7 — 2D section picker, implemented) Bindings are still **stable bar indices** (D-P3-4); F7 adds only the missing UI. Pure `engine/barLabels.ts` labels each bar **face-relative** `T1/B2/L1/R3` (owner choice: TOP/BOTTOM ranked left→right by `u`, LEFT/RIGHT bottom→top by `v`). `ui/SectionPicker.tsx` draws SVG dots (+ a keyboard/a11y button list — both call the same `onPick`); `ui/useBarLink.ts` keeps the pending first-pick **local** to each editor (so the supplement picker and the cross-tie picker never cross-fire) while mirroring it to the store's new `selectedBars` for the 3D highlight. `PlacedBar.barIndex` (new, core) + `buildScene(..., selectedBars)` give **per-bar** 3D highlight; `Rebar` longitudinal meshes take an `onPick` (pointer addition — GPU-verified). `SupplementsPanel` dropped the `bar1/bar2` number inputs for the picker (rebind now re-binds to the 2 selected bars). Tests: `bar_labels`, `section_picker`; `binding_keyboard`/`leg_count_asw` (core, unchanged) stay green.
- **D-V102-2** (P2/F2 — cross-ties rebuilt, implemented) The broken `nLegs`/`crossTieZones` (épingles centred at the section centre) is **replaced**. A column tie / beam stirrup now carries `crossTies: {barA,barB,diameter?}[]` + an **element-level `crossTieHookAngle`** (owner choice: user types/picks one angle 90/135/180, applied to ALL the element's cross-ties). **Core seam, no element branching:** `MemberPlacement.transverse[]` + `ElementTransInput` gained an optional `anchor {u,v,angleDeg}`; `placeLoop`/`placeBars` rotate+translate an anchored loop to the bar-pair midpoint (non-anchored groups byte-identical → goldens safe). The adapter (`solveDoc`, web — `isColumnDoc?tie:stirrup` lives HERE not in core) builds one anchored épingle per cross-tie via `resolveBarPairPlacement` (now returns `angleDeg`), recomputes **Asw leg count = 2 + 2·nCrossTies**, and feeds the **real `longBarsEngaged`** (4 corners + distinct engaged bars; no-cross-tie case = 4). The épingle **hook angle is param-driven**: the generator applies an optional `params.hook_angle` override (additive; `epingle.json` gained a `hook_angle` param) keeping the `cutLength`/`totalLengthExpr` cross-check (D-P1-1). **Auto (code)** = engage **every intermediate bar** (owner choice) via pure `engine/crossTies.ts autoCrossTies`; per-direction presets filter by orientation. **Seismic engagement is WARN-only, never blocks export** (owner: "indicate but don't block" — `crosstieEngagement` ND3 changed FAIL→WARN; rides provisional **G-RPS**). **Legacy `nLegs` migrates** to `(nLegs−2)/2` auto-engaged cross-ties in `migrateDoc` (applied at the `rcfgToDoc` chokepoint → covers file-load + autosave-restore; idempotent; no data loss). Scope note: **supplement** épingles still render centred (presence-only) — anchoring them is a small D-P3-6 follow-up; F7 only changed how they're *bound*. Tests: `tie_legs_geometry` (anchored-not-centred + Asw + seismic-engaged + migration), `epingle_hook_angle`, updated `confinement_required`.
- **D-V102-7** (P7/F1 — in-plane view roll, implemented) **Camera/interaction-only, no engine/`.rcfg` change** (spec §1.3, approach a). Pure headless helper `viewport/cameraState.ts rollUpVector(viewDir, rollRad) → Vec3`: the level up = world-up projected ⟂ the view axis (Z-fallback when near-vertical), rotated about the view axis by `rollRad` (Rodrigues). Store gained session `rollRad` + `setRoll`/`rollBy` (**NOT** in `.rcfg`, like `projection`/`viewRequest`); **`requestView`/`homeView` re-level it to 0** (roll is a temporary tweak on top of an orientation), and `reset()` clears it. `viewport/Viewport.tsx RollController` (a `useFrame` at priority 1, i.e. AFTER drei OrbitControls' update) post-multiplies the rolled `camera.up` + re-`lookAt(target)` when `rollRad≠0`, and restores world-up **once** when the roll clears — orbit (X/Y) + zoom untouched. UI (`ViewCube.tsx ViewControls`, the HTML overlay = a11y/keyboard path): **↺/↻ buttons (±90°, Shift = fine ±5°)** + a **roll slider** (−180..180°). The drei curved drag-ring around the gizmo is deferred GPU polish; the buttons+slider are the testable/keyboard equivalents. Tests: `view_roll.spec` (pure `rollUpVector` 0/90°/360°/near-vertical + RTL buttons→`rollRad`, Shift fine-step, Home/named-view re-level). GPU feel verified on the owner's machine.
- **D-V102-6** (P6/F6 — façonnage editor + shape catalog, implemented) **Data-driven, minimal core change** (spec §6). Three new **shape manifests** (`zbar`/`double_crank`/`stepped`, registered in `SHAPES`) — pure segment-grammar, pass the integrity gate, 16 shapes total. Each **longitudinal group** gains an optional `faconnage {shapeParams?, hooks?}` (column `longitudinal` + beam `span` wired; chapeau/montage/generic are a follow-up); `solveDoc` passes the user `shapeParams` (else the computed default) + maps `hooks` to the generator → flows to 3D/coupe/BBS/DXF/PDF unchanged (the engine already accepts any shape+params). **Hooks independent of base shape:** `generateBarShape`/`generateShape` gained an optional `opts.hooks` (per-end `UserHook` = `"none" | {angle:90|135|180, extFactor?}`) that OVERRIDES the manifest `endHooks`; absent → byte-identical to pre-F6. To let any open shape accept user hooks, the open longitudinal manifests' `totalLengthExpr` were migrated to the hook-agnostic `… + hookAllowances − bendDeductions` form — **byte-identical** when no user hooks (verified: all BBS/DXF/fiche/unit-mass goldens green). **cutLength invariant (D-P1-1) guarded:** the generator's `totalLengthExpr` cross-check throws on an impossible shape; `ui/FaconnageEditor.tsx` catches it (and rejects a non-positive/non-finite cutLength), shows a clear error, and **never commits invalid params** (a local edit buffer keeps the field; the store/solve only ever sees a valid shape). The editor: a catalog `<select>` (DROITE/CROCHET_L/U_BAR/BAIONNETTE/RELEVE/ATTENTE/Z_BAR/DOUBLE_CRANK/STEPPED), param `NumberField`s seeded from geometry, start/end hook selects, a live SVG centreline sketch + cutLength. `.rcfg` round-trips for free (`faconnage` rides `meta.app_document`; legacy → DROITE default). **Deferred (spec §6.6):** lap-splice/coupler scheduling between elements. Tests: `tests/facade_shapes` (3 manifests valid + cutLength==totalLengthExpr + hook override), `apps/web/.../faconnage` (adapter + hooks + legacy byte-identical + rcfg), `ui/faconnage_editor` (RTL pick/edit/hook/sketch + invalid rejected).
- **D-V102-5** (P5/F5 — user transverse regions, implemented) Added via the **transverse-segment seam, no element branching** (spec §5.3). New core type `TransverseRegion {from,to,spacing}`; `ElementTransInput`, `MemberPlacement.transverse[]` and `solveColumn`'s tie gained an optional `regions?`. Pure `regionStations(regions, length)` (place.ts) emits stations region-by-region with the global end margins, **de-duping shared boundaries**; a single full-length region **delegates to `transverseStations` → byte-identical** (goldens safe). `regionStationCounts` gives per-region totals. `placeBars` instances loops per region; **BBS** `groupCount` sums region stations; the **elevation fiche** emits one `Ø e=spacing` callout per region (uniform set unchanged); **coupe snap stations** (`coupeHandle`) are region-aware. **Seismic reconciliation is WARN-only** (`region_crit_spacing:{zone}` in `applySeismicOverlay`): a user region overlapping an end critical zone but looser than `s_crit` warns — it **tightens/indicates, never silently overrides**, never blocks export (rides provisional **G-RPS**, consistent with F2's "indicate but don't block"); absent regions → no item (existing seismic goldens untouched). Web: `document.ts` adds `regions?` to `tie`/`stirrup`/generic transverse `ZoneEdit`; `solveDoc` threads them onto the cadre **and the cross-tie épingles** (they densify together); pure `engine/regions.ts` (`normalizeRegions` contiguity auto-fill over 0..L, `symmetricEndsRegions` quick-fill, `uniformRegions`, `memberAxisLength`); `ui/RegionEditor.tsx` (a contiguous from→to+spacing table with add/remove, a uniform reset, and a symmetric-ends quick-fill seeded from the seismic `l_c`) wired under the column tie + beam stirrup controls. **`.rcfg` round-trips for free** (regions ride `meta.app_document`; legacy files have none → uniform). **Scope:** the Asw/clear-spacing checks still use the representative `tz.spacing` (per-region Asw verification deferred); boundary **drag** on the elevation is deferred — numeric table is the a11y baseline (owner GPU follow-up). Tests: `tests/transverse_regions` (stations/counts, placement, BBS totals, fiche callouts, seismic WARN), `apps/web/.../transverse_regions` (helpers + adapter + legacy byte-identical + rcfg round-trip), `ui/region_editor` (RTL). Kept `rps_segment_injection`, `bbs_golden`, `elevation_fiche` green.
- **D-V102-4** (P4/F4 — workspace re-layout, implemented) **UI/layout-only, no engine/`.rcfg` change** (spec §4.3). The right strip (was a single always-on `AlertsPanel`) is replaced by `ui/RightColumn.tsx` — a flex column stacking **three collapsible, independently-scrollable sections**: **Verification** (`AlertsPanel`), **Project** (`ProjectPanel`), **BBS** (`BbsPanel`). Each section has a header button (caret, `aria-expanded`) toggling its body; the **same** open/collapse state is driven from the navbar (the BBS/Project/Vérifications buttons now call `toggleRightPanel`, not the old bottom-dock toggle). New store state: `rightPanels {verification,project,bbs}` (default `verification:true`, others collapsed — preserves "checks always visible") + `expandPanels` (an **Expand** toggle that widens the column to 60% over the 3D via the `.right-column.expanded` class; the 3D stays mounted, just narrower). **The bottom dock now hosts only Coupes** (`BottomPanel` → `CoupePanel`; `bottomPanel` narrowed `"coupes"|"bbs"|"project"|null` → `"coupes"|null`). `AlertsPanel` lost its own `.alerts-head` chrome (the section header supplies the title; the deferred "… recalcul" badge moved into the body). Layout prefs are **session-only** (not reset on element `reset()`, not in `.rcfg`). New i18n `workspace {expand,collapse,expandHint}` (note: `layout` was already taken by the layout-principle group). Tests: `workspace_layout` (3 sections render + collapse, expand sets flag/class, Coupes-only bottom dock); `export_menu`/`smoke` kept green (the latter renders `AlertsPanel` standalone — its header-less refactor is transparent to it).
- **D-V103-1** (P1/G1 — true bar geometry, implemented) `placeBars` (core, `section/place.ts`) no longer draws longitudinal bars as straight 2-point axis lines. **Longitudinal:** each bar now renders the group's real bent `centerline3D` oriented onto the member frame at the bar's `(u,v)` — convention `worldX = u`, `worldY = axisStart + localX`, `worldZ = v + localY` (the generator emits a flat `[localX, localY, 0, …]`; `localX` = run along u+, `localY` = lateral in v). **A straight `DROITE` is byte-identical** (`localY≡0`, `localX` 0..L) → **every existing render/BBS/fiche/DXF golden held, no re-baseline was needed** (the goldens are structural, not byte-snapshots). A bent shape (baïonnette/relevé/Z) now bends into the section **depth** plane (±Z). `axisStart` is plumbed (default 0) for G2 unique-length bars. **Continuous coil:** a transverse set whose shape carries `coilLength` (the bespoke helix) is detected via `isContinuousCoil` (mirrors `bbs.ts`) and placed **ONCE** as a single member-length helix (`placeCoil` maps the helix frame `[u, axis, w]` straight to world `[X,Y,Z]`, axis 0..length) — never instanced per station + flattened. **Stair:** the `MARCHE_PALIER` main bar rides the same longitudinal mapping automatically (flight→axis, landing bend→depth). A short-polyline fallback keeps a straight run when a group has no centreline. The coupe engine + elevation fiche + PDF/DXF consume `placeBars`, so true geometry flows everywhere for free. Tests: `tests/{true_geometry,coil_placement,stair_bars}.spec.ts` + `perf_heavy` extended with two `placeBars` perf cases (dense 120-turn helix + heavy column, both < 16 ms). **No core branching, purity held.**
- **D-V103-2** (P2/G6 — cadence continuity + coupe/elevation dims, implemented) **(a) Cadence continuity:** `regionStations` (core, `section/place.ts`) multi-region path now **carries the running station across a boundary** — a new region's first cadre is placed ONE new-spacing step past the previous region's last cadre, NOT reset to `region.from` (the `from` only marks where the new spacing takes over) — removing the illogical short stub (e.g. a 2 cm gap) at a boundary. The single-full-length-region path still delegates to `transverseStations` → **byte-identical** (goldens safe); `regionStationCounts` still partitions by containing region. **(b) Coupe dims (PDF):** `sectionAt` `buildDimensions` now emits **WIDTH + HEIGHT + an enrobage COVER on EACH face** (measured from the concrete face to the nearest bar centroid in that half, from the real section circles; falls back to the first zone's `d'` when the cut crosses no bars). `pdf.ts drawCoupe` now **renders `view.dimensions`** (it previously ignored them — the gap); the DXF coupe (`coupeToDxf`) already drew them, so PDF + DXF now agree. **(c) Elevation region dims:** `fiche.ts buildElevationFiche` adds one `FicheDim` per stirrup **region length** under the overall-length line (drawn by both `pdf.drawElevation` + `dxf.elevationToDxf`, which already iterate `fiche.dims`). Tests: `tests/{region_cadence,coupe_dims}.spec.ts`; existing `transverse_regions`/`elevation_fiche`/`dxf_coupe_golden`/`dxf1_golden`/`pdf_smoke` stayed green. **BBS/cutLength goldens unchanged** (rendering doesn't change cut lengths).
- **D-V103-4** (P4/G2 — addressable bars, section levels & per-bar façonnage, implemented) **Per-bar detailing via a pipeline `longBars[]` channel** (spec §2, [REF-SYS-530]). **Architecture (lowest-risk):** `ElementSolveInput` gained `longOverrides?`/`extraBars?`; `solveElement` emits a new `SolveResult.longBars[]` — an explicit per-bar list (each bar's own shape/Ø/axial start/removed + appended extra bars) — **only when overrides/extra bars exist** (else `undefined` → the grouped fast path, **byte-identical**, every golden held). `placeBars` (3D/coupe/PDF/DXF via the shared placement) and `computeBBS` (schedule) read `longBars` when present (skip longitudinal-role groups), so a façonné / unique-length / removed / independent bar appears everywhere + on the schedule. **The bar→group shape mapping mirrors `placeBars`** (zone match, else rect TOP/main); no `if(elementType)`. **G2a section levels** = an addressable bar at a chosen section depth `v` (incl. an intermediate U-bar level) — no separate level registry. **G2b overrides** + **G2c extra bars** are DATA on the doc (`barOverrides` on the column `longitudinal` / beam `span`; `extraBars` on the column/beam) → the adapter (`solveDoc.buildLongOverrides`/`buildExtraBars`) passes the FULL resolved shape+params (group default merged with the user edit + optional unique `L`). **G2d** `ui/AddressableBars.tsx`: a section picker selects ONE bar; the existing `FaconnageEditor` is reused for its shape/hooks, plus Ø/length/axial-pos/remove NumberFields, plus an independent-bars list (picker **and** numeric, a11y parity). New store actions `setBarOverrides`/`setExtraBars`. **`.rcfg` round-trips for free** (rides `meta.app_document`; additive; legacy → none). **Scope/limitation (engineer-gate, principle 7):** override/extra/removed bars are **detailing add-ons** — they change the render + schedule but **NOT the validation layout/As** (the count-group still drives the §7 checks); per-bar mixed-diameter As-weighting + per-bar `d` are a deferred refinement (not changed silently). `length` maps to the shape's `L` param (straight unique-length bars). Tests: `tests/{bar_overrides,independent_bars,section_levels}.spec.ts` (core: override/removed/unique-length/extra/levels + legacy `longBars` undefined), `apps/web/.../addressable_bars.spec.ts` (adapter + .rcfg + As-untouched), `ui/addressable_bars_ui.spec.tsx` (RTL pick→remove→extra). BBS/cutLength goldens unchanged.
- **D-V103-5** (P5/G3 — beam two-support model + relevés + auto-seeded regions, implemented) **Two supports V1/V2, data-driven, no core branch** (spec §3). `BeamDoc.chapeau{supportZone}` is **replaced** by `supports:{left,right}` where `SupportZone={chapeau{enabled,diameter,nTop,asReq,length},anchorage,width}` + `chapeauShapeId` + `releves?:ReleveZone[]`. The adapter (`beamInput`) emits **two chapeau longitudinal zones** `As_top_support_left/right`, each its own §7.7 `computeCurtailment(length)` → **asymmetric lengths are just data**, each independently validated (`provided_area`/`ratio_limits`/`clear_spacing` — the beam profile already iterates zones). **Representative section:** the layout TOP face carries `montage + max(nChapeauL,nChapeauR)` (the two supports never share a cross-section) — NOT the sum (which crammed 4 top bars into a 300 mm beam → clear-spacing FAIL). **Relevés ride the G2 `extraBars` channel** (a `RELEVE`-shaped bottom bar per `count`, `axisStart` near its support) so they **render bent (G1) + schedule** with their own cutLength, without touching the layout/As (detailing add-on). **`buildLongBars` fix (core, general):** the per-bar longBars enumeration now **distributes layout bars across the zones that share a face** (declaration order, by each zone's provided count) and **emits any under-seated zone** (the second support's chapeau, when relevés/overrides trigger the longBars path) as addressable bars — so both chapeaux always schedule; a single-zone face (column) is byte-identical. **Auto-seed:** pure `supportSeededRegions(L, leftZone, rightZone, spacing)` (dense ends ½·spacing, looser mid) + a store `seedStirrupRegions()` + a "Densifier aux appuis" button writes editable `stirrup.regions` — **solve path unchanged unless seeded** (goldens safe). **Legacy migration** (`migrateBeamSupports` at the `rcfgToDoc` chokepoint, BEFORE `beamLayoutBars` reads `supports`): a v1.0.2 `chapeau{supportZone}` → symmetric `left=right` + default anchorage 400/width 300 + no relevé; idempotent, lossless. UI: `SupportControls` (V1/V2) + `ReleveEditor` + the seed button in `Sidebar.BeamSchemeControls`. **Scope/limitation:** the second support's chapeau + the relevés render at a **representative axial position** (the precise per-support axial offset of the grouped-path chapeau is a rendering nicety, not a schedule/validation gap); the member-level `end_support_anchorage` check stays single (per-support anchorage is stored + drawn, G7). Tests: `apps/web/.../beam_supports.spec.ts` (asymmetric cutlengths + both validated, relevé bent+scheduled, region seeding densifies, legacy migration). Updated `element_catalog`/`beam_top_bars`/`per_zone_readout`/`beam_supplements` for the `As_top_support_left/right` zone split. **BBS/cutLength goldens unchanged.**
- **D-V103-6** (P6/G4 — lap splices / couplers, implemented) **Full lap/coupler scheduling** (spec §4). Pure core `geometry/splice.ts`: `spliceBar(run, splices[], code, {diameter,material,fractionLapped}) → {segments[], couplerCount, lapLength, totalCutLength}` — a **lap** extends the segment before it by `l_r = code.l0(...)`, a **coupler** adds no length but is counted; **the D-P1-1 accounting is preserved** (`Σ segment cuts = run + #laps·l_r`). `autoSplices(run, stock=12000)` splits an over-length run. Exported from `index.ts`. **Doc:** the column `longitudinal` + beam `span` groups gain `splices?:Splice[]` + `autoSplice?` (additive; absent → unspliced). **Pipeline:** `ElementLongInput` gains `splices?`/`autoSplice?`; `solveElement` splices each longitudinal group off its `shape.cutLength`, attaches `SolvedGroup.splice`, collects **lap extents** for the seismic overlay, and pushes a `lap_stagger:<zone>` **WARN** (group-level laps coincide → not staggered). **BBS:** a spliced group is scheduled as its **segments** (each × the group count) + a **`couplers` total** on the schedule; the seismic `lap_in_critical_zone` now fires off the real lap extents (FAIL under ND2/ND3 in `l_c`). **UI:** `Sidebar.SpliceEditor` (auto-split toggle + a station/kind list, picker AND numeric — a11y) on column + beam, riding the existing `setLongitudinal`/`setSpan` setters; `.rcfg` round-trips for free. **Scope:** splices are **group-level** (all bars of a group splice at the same station — hence the stagger WARN); per-bar splices via the addressable channel are a later refinement. Lap length rides the **provisional** BAEL/EC2 `code.l0` (G-BAEL/G-EC2). Tests: `apps/web/.../lap_splice.spec.ts` (spliceBar invariant + coupler + autoSplices; doc→schedule segments + coupler tally + stagger WARN; seismic lap-in-`l_c` FAIL; legacy unspliced byte-identical). **BBS/cutLength goldens unchanged** (no splices on the default docs).
- **D-V103-3** (P3/G5 — cross-tie / épingle unification + anchoring, implemented) **ONE cross-tie model** (spec §5). (a) **Catalog:** dropped `SUPP_EPINGLE_CROSSTIE` from the 4 scheme `supplementalCatalog`s — épingles are added only via the `CrossTieEditor` (F2); the supplement **manifest is kept** (core `resolveSupplement`/`placement_resolve`/`binding_keyboard`/`supplement_rebind` tests still load it; 7 supplements unchanged). (b) **Hook geometry (the D-V103-1 follow-up):** `generateBarShape` now appends/prepends the end-hook **return leg** to `centerline3D` for **OPEN** shapes (the join becomes a filleted bend), so the épingle crochet renders in 3D/coupe/PDF/DXF and reflects the resolved `hook_angle`; **CLOSED shapes (cadre/étrier) skip it** (welded loop, byte-identical), and the `cutLength`/`totalLengthExpr` cross-check is computed BEFORE the vertices are extended → **invariant untouched** (only `geomLength`/render change). A no-hook end (DROITE) is byte-identical. (c) **Anchored-only:** `placeBars` skips the centred-supplement fallback for `shape.archetypeId === "EPINGLE"` — épingles are *only* the anchored cross-tie on the bar-pair line (D-V102-2). (d) **Migration:** `migrateDoc` folds legacy `SUPP_EPINGLE_CROSSTIE` supplements into `crossTies` (dropped from `supplements`) at the `rcfgToDoc` chokepoint — idempotent, lossless, alongside the `nLegs` migration. Defaults already ship `crossTies:[]`. **Render goldens stayed green (structural, not byte-snapshots); BBS/cutLength unchanged.** Tests: `tests/epingle_anchored.spec.ts` (hook-in-centreline + anchored + no centred épingle), `tie_legs_geometry` (default-zero + supplement migration), `scheme_switch`/`beam_supplements` updated.
- **D-V103-7** (P7/G7 — shop-drawing PDF/DXF, implemented) **A pure `shopDrawing(result, opts) → {leaders, marks, coupeMarkers, stirrupZones, supportLabels, bendingTable}` annotation model** (`packages/exporters/src/shopDrawing.ts`, spec §7 [REF-SYS-930]) built ONCE over `buildElevationFiche` + `computeBBS` + `sectionAt` and **shared by PDF and DXF** so the two agree by construction. **Leaders:** one per distinct longitudinal group (aggregated from `placeBars`, mirroring the fiche marks), joined to its BBS line for the mark + cut length → `mark nØd l=cut` (e.g. `1 3Ø20 l=6000`), with a **basic anti-overlap** label column fanned along the member's drawing axis. **Sequential marks** = the BBS ordinals (bare `1,2,3…`; a project `markPrefix` still namespaces `P1-01`, §7.2 — both coexist). **Coupe markers** via `cuttingLineFiche` (default `defaultCoupeFor` + any user coupes → A-A/B-B on the elevation). **Stirrup-zone notation** `count×spacing` per region (uniform → `transverseStations`, regions → `regionStationCounts`, from G6). **Support labels V1/V2** derived from the presence of the G3 `As_top_support_left/right` chapeau zones on the result; bearing **width + bottom-bar anchorage** are optional drawing data supplied via `opts.supports` (NOT on the pure `SolveResult` — the beam-doc carries them, threaded through `PdfMetadata.supports`), so the labels emit regardless, annotated when supplied. **Bending table** = one row per distinct scheduled shape (= the BBS lines): mark · **2D centreline sketch** (from the source group's `shape.centerline3D`, flat `[x,y,0,…]`, looked up via the BBS `groupIds`, splice `#seg` suffix stripped) · Ø · cut length · **count/element** · **total = count × `opts.quantity`** (default 1; `buildProjectPdf` passes each type's fabrication quantity). **Wiring:** `pdf.ts` `drawElevation` now renders the shop overlay (leaders/coupe markers/zone notation/support labels, fit-bbox expanded over the annotation anchors) + a new `drawBendingTable` (with the mini sketch) below the BBS table; `dxf.ts` `shopDrawingToDxf` emits leaders (COTATION+TEXTE) + zone/support labels (TEXTE) + a `bendingTableToDxf` (TEXTE rows + ARMATURES **polyline** sketch — **no CIRCLE**, so the coupe-circle-count golden is untouched) into `buildDxf1`/`buildDxfCoupes` (coupe cutting-lines stay in `elevationToDxf`, not duplicated). **Purity held** (no DOM/three, no `if(elementType)` — support labels key off the zone name, data-driven); the render goldens are structural (4-layer/entity/regex checks) so **`dxf1_golden`/`dxf_coupe_golden`/`elevation_fiche`/`bbs_golden`/`pdf_smoke`/`project_pdf` all held — no re-baseline needed**; **BBS/cutLength goldens unchanged**. **Scope/limitation:** support width/anchorage need the caller to pass `opts.supports` (the web `exportActions` don't yet thread the beam doc's supports → a small follow-up; V1/V2 labels + positions render without it); the bending-table sketch is the group's centreline (spliced-segment rows fall back to no sketch). Tests: `tests/shop_drawing.spec.ts` (leaders `mark nØd l=`, sequential + prefixed marks, anti-overlap, coupe markers, uniform + per-region stirrup-zone notation, V1/V2 labels + width/anchorage, none for single-support, determinism), `tests/bending_table.spec.ts` (row-per-distinct-shape = BBS lines, count/element, ×quantity total, default quantity 1).
- **D-V103-8** (P8/G8 — 3D fidelity: concrete edges + stepped stair, implemented) **Viewport-only, no engine/core/`.rcfg` change** (spec §8, [REF-UI-840]). (a) **Crisp concrete edges:** `viewport/Viewport.tsx ConcreteVolume` now renders a drei `<Edges threshold={15}>` outline (light `#9aa3b2`) over both the RECT box and the CIRCULAR cylinder, so the transparent (opacity 0.3) volume reads as a solid block and the cage + G1's exiting endpoints stay legible — fixing the "half-empty" look. The shared transparent material was factored into a `concreteMaterial()` helper. (b) **Stepped stair:** a new `SteppedStair` component replaces the flat rect box **for E-STR-01 only** (a UI-level `doc.element === "E-STR-01"` check — this is the *adapter/viewport*, NOT `packages/core`, so the no-`if(elementType)` rule doesn't apply; the plan explicitly scopes it "UI, not core"). It builds `n_steps` **disjoint boxes** from the stair geometry (`g` going · `r` riser · `n_steps` · `flight_width` — read straight off the GenericDoc `geometry` map), each box spanning one going range along the member axis (+Y) and rising to its tread over the section-depth axis (+Z), solid-to-base → an unmistakable staircase silhouette; centred on the member frame to match the box it replaces. Each step carries its own `<Edges>`. **Scope/limitation (representative, owner-GPU-verified):** the true rise (`n·r`) far exceeds the flat-slab envelope `waist_t`, so the stepped mesh extends well beyond the old box in the depth plane and the waist bars (placed in the flat member frame) sit low in it — a faithful representative read, refined later if the owner wants the bars lofted onto the treads; the top **landing** (`landing_L`) is not modelled (kept aligned with the bar frame). Tests: none added (pure GPU render — build:web proves the drei `Edges` import bundles; the stair box math is trivial + owner-verified).
- **D-V103-9** (P9/G9 — camera fixes: roll / ViewCube / hand-pan, implemented) **Camera/interaction-only, session state, NOT in `.rcfg`** (spec §9, [REF-UI-811b/850]). (a) **Killed the auto-spin:** the F1 `RollController` (`viewport/Viewport.tsx`) no longer runs a per-frame `useFrame(…, 1)` that mutated `camera.up` + `lookAt` every frame (which, with `OrbitControls enableDamping`, precessed/auto-rotated the ViewCube). Roll is now a **discrete on-demand `useEffect`** keyed on `[rollRad, camera, controls]` — it applies `rollUpVector` (unchanged pure helper) once when the roll changes and re-levels `camera.up` to world-up when it clears (the camera-dep re-applies it after a perspective⇄ortho swap). (b) **Auto-level on orbit:** a second effect subscribes to OrbitControls' `"start"` event and **resets `rollRad` to 0** the moment the user begins any drag (removes the up-vector conflict; the cube stops rotating). (c) **Hand-pan tool:** new session store state `navMode: "orbit"|"pan"` + `toggleNavMode`/`setNavMode` (added to init + `reset()`, **not** persisted). The `Scene` derives `OrbitControls mouseButtons` from it — pan mode = `LEFT:PAN, RIGHT:ROTATE` (so orbit stays reachable), orbit mode = `LEFT:ROTATE, RIGHT:PAN`; MIDDLE dollies and the wheel zoom always. A toolbar **✋ toggle** in `ViewCube.tsx ViewControls` (`aria-pressed`, bilingual `view.pan`/`view.orbit` labels) flips it; keyboard-arrow pan is OrbitControls' built-in no-pointer path. Tests: `apps/web/src/viewport/nav_mode.spec.tsx` (default orbit; the button toggles navMode + aria-pressed; `setNavMode`/`toggleNavMode` store paths); `view_roll.spec` stays green (pure `rollUpVector` + roll buttons unchanged). The frame-loop removal + orbit auto-level + the actual pan feel are **owner-GPU-verified** (headless can't drive OrbitControls events).
- **D-V103-7b** (P7/G7 follow-up — beam support width/anchorage wired into the app exports, implemented) Closes D-V103-7 open item (b): the shop-drawing model already accepted `opts.supports` (bearing `width` + bottom-bar `anchorage` → the `V1 b=… l.a=…` labels), but the **web app never forwarded them**, so real PDF/DXF exports showed the V1/V2 tag with blank width/anchorage. Fixed end-to-end: (i) **DXF plumbing** — `dxf.ts buildDxf1`/`buildDxfCoupes` now accept `supports` and thread it into `shopDrawingToDxf` (previously hard-coded to none); (ii) **PDF per-type** — `ProjectPdfType` gained `supports?` and `buildProjectPdf` uses `t.supports ?? meta.supports` (per-beam, not one global set); (iii) **web adapter** — new pure `exportActions.beamSupportsMeta(doc)` extracts `{side,width,anchorage}[]` from a `BeamDoc` (`undefined` for non-beams), and `Navbar` passes it into `exportPdf`/`exportDxf` + every `ProjectExportType`. `exportDxf` gained an optional `supports` arg (back-compat). Tests: `apps/web/src/engine/export_supports.spec.ts` (extractor left/right; undefined for a column; the DXF output contains `b=300`/`l.a=400` when supplied, absent otherwise). **No engine/core change (all in exporters + web); `.rcfg` untouched; render goldens held** (the existing `shop_drawing`/`dxf1_golden` goldens don't assert the optional support extras, so no re-baseline).
- **D-V102-3** (P3/F3 — sticky per-zone readout, implemented) **UI-only, no engine/`.rcfg`/validation change** (spec §3.3): the per-zone provided area + `ZoneGeometry.d` already exist on `SolveResult`. New pure selector `ui/derived.ts perZoneReadout(result, doc, lang) → ZoneReadoutRow[]` — **one row per flexural zone** (`As,prov` vs `As,req`, `ok`, `d`, `perMetre`), driven off the validation `provided_area[:zone]` items + `result.zones` (the column's lone un-suffixed `provided_area` maps onto `result.zones[0]`; slab zones flagged `perMetre` → cm²/m). **Generic over all 8 elements**, no `if(elementType)`. The old single-aggregate `Badges` is **replaced** by `ZoneReadout` (`position: sticky; top:0` inside the scrolling `.sidebar`, full-width via negative margins; `role="status" aria-live="polite"`; per-zone rows green/red + an overall 🟢/🟠/🔴 status chip from `result.status`). Zone labels: generic zones use the doc's own `label_fr/_en`; column/beam use a new `i18n readout.zones` map (`As_total`/`As_span_bottom`/`As_top_support`/`As_top_montage`). Old `derived.ts` helpers (`asProvidedMm2` etc.) kept (still used by `store_resolve.spec`). Tests: `per_zone_readout` (column/beam/slab rows + FAIL flip + per-metre); `smoke` updated (queries `.readout`, readout survives a tab switch).
- **D-V105-1** (v1.0.5 Phase 1 / M1 — placement defects D1/D2/D3, implemented) **P1a single placement path:** `solveElement` emits the per-bar `longBars[]` whenever `facesCarryMultipleZones(input)` (any face with >1 longitudinal zone — a two-chapeau beam), not only on user overrides; single-zone-per-face elements keep the grouped fast path → byte-identical. The grouped path's TOP-face collapse (`topLong = longGroups[1]`) is now reached ONLY by single-zone faces, so a beam draws BOTH chapeaux (D1/D2 fixed); BBS byte-identical (bars re-merge to the same counts). **P1b placement↔validation decouple (audit A2):** new pure `hasUserAddressableContent(input)` + `SolveResult.hasUserAddressableContent` gate the As/`d` reconciliation, `placedCount`/`face_min_bars`, the per-bar splice/stagger block, `validateAddressableBars` and `sectionAt.defaultCoupeFor`'s station-aware seed — **decoupled from `longBars` presence** — so a forced-multi-zone default beam keeps its validation list + default coupe byte-identical. A forced-path base bar **inherits its zone's group splice** (BBS still schedules segments). **P2 per-bar curtailment (replaces the inert `continuedToSupport`):** `startStation`/`endStation` + per-end `anchorage` on `LongBarOverride`/`ExtraLongBar`/`PlacedLongBar` (+ web `BarOverrideEdit`/`AddressableBar`); `clipToStations` clips a bar's run (cut length follows, mirroring H2's leg inversion); §7.7 `end_support_anchorage` counts real `runsThroughAsProv` not a fraction (no per-bar list → falls back to full As,prov, byte-identical). **Migration:** `migrateBeamContinuation` maps legacy `span.continuedToSupport=f` → the `round(f·nBottom)` innermost bars run through, the rest curtail to `[inset, L−inset]` (⚠ PROVISIONAL `CURTAIL_INSET_FRACTION=0.1`, G-BAEL), keyed off a new `engineBeamLayoutBars` (mirrors the engine's `nTop = montage + max(chapeaux)`; **NOT** `beamLayoutBars`, which uses the SUM → different indices → a flagged pre-existing cross-tie-binding hazard, §9). Dead `continuedToSupport` Sidebar control removed. **No golden snapshot moved** (the DXF/PDF/BBS goldens use the single-chapeau `referenceBeam`); the change surfaces only as `v1.0.5 P1`-annotated test-assertion updates (default beam now emits `longBars`). Tests: `tests/{beam_two_support_placement,placement_validation_decouple,curtailment}.spec.ts`, `apps/web/.../curtailment_migration.spec.ts`; reworked `beam_curtailment`. Proven end-to-end (core→solve→adapter→3D/coupe→BBS/DXF/PDF→`.rcfg`).
- **D-V105-2** (v1.0.5 Phase 2 / M2 — canonical `PlacedBar` model + shared placement pass, implemented) **P-A canonical model:** new `types/placed-bar.ts` defines the first-class **`SingleBar`** (id + `(u,v)` + shape/params/Ø + curtailment stations + anchorage + splices + `removed` + an optional `overrideBarIndex`) and the `PlacedBarInput` union (M3 extends it with `BarRow`/`Bundle`/`Layer` — additive, forward-compat D-P0-2). `EndAnchorageChoice`/`BarEndAnchorage` **moved** from `element.ts` to this types-layer home (still reach the barrel via `types/index`; `element.ts` imports, does not re-export → no double-export). Binding stays id-based (D-P3-4). **P-B shared pass:** new `section/resolvePlacedBars.ts` is the **single long-steel truth** for free bars — it owns the per-bar primitives `clipToStations` + `barSplice` (moved out of `element.ts`) and `resolveSingleBar`/`resolvePlacedBars`. `element.ts buildLongBars` delegates its extra-bar loop to it (byte-identical `ExtraLongBar`→`SingleBar` conversion); the RECT base-bar reconstruction (face→zone distribution, two-chapeau expansion) **stays** in `buildLongBars` (the RECT section-aware resolver the plan lists). The four generic shims (`circular/slab/stair/joist`) gain `placed?: PlacedBarInput[]`, call the shared pass, and **append** `longBars` + set `hasUserAddressableContent`. **Additive `placeBars` + `computeBBS`:** the render/schedule now cover grouped base bars NOT represented in `longBars` — a **RECT** element replaces its base steel (every `bars[]` entry has a non-standalone `longBars` twin → `covered` → the additive loop emits nothing → **byte-identical**); a **generic** pipeline KEEPS its base mat in `bars[]` and appends only FREE (standalone) bars → base mat + free bars both flow (the coverage key: non-standalone longBar `barIndex`/`groupId`). **B3 two modes:** a free `(u,v)` bar resolves section-agnostically; an `overrideBarIndex` bar resolves section-aware (position from `ctx.layoutBars[idx]` — the section's own convention). **Adapter:** `GenericDoc.placed?` (`AddressableBar`) → `buildPlacedBars`→`SingleBar` threaded into all 4 generic inputs (column/beam free bars already ride `extraBars`); `.rcfg` round-trips (rides `meta.app_document`, additive). **No golden moved** (M2 adds no placed bars to any legacy doc — a no-placed result keeps `longBars===undefined`). **Scope (flagged):** generic free bars render+schedule but do NOT yet feed As/validation (Phase 4 / Track V-A); generic override-onto-existing-bar deferred to M3 (per-metre semantics + the row/layer model); Amer's `beamLayoutBars` cross-tie flag untouched (out of core-placement scope). Tests: `tests/{placed_bars_all_elements,resolve_placed_bars}.spec.ts`, `apps/web/.../placed_bars.spec.ts`, `perf_heavy` extended (60-bar slab solve+place < 16 ms). Naming: three distinct concepts coexist — `PlacedBarInput`/`SingleBar` (input), `PlacedLongBar` (resolved output on `SolveResult.longBars`), `PlacedBar` (world-space render in `section/types.ts`).
- **D-V105-3** (v1.0.5 Phase 3 / M3 — rows / layers / bundles / skin + coupe fidelity, implemented) **P-A union extension:** `types/placed-bar.ts` grows a shared `PlacedBarBody` (shape/params/Ø/hooks/role/curtailment/splice/removed — `SingleBar` now `extends` it, unchanged) + three new members: **`BarRow`** (anchor + `direction` "u"/"v" + `extent` + `count` OR `spacing` + `skin?`), **`Bundle`** (`position` + `n`), **`Layer`** (`face` + `layerIndex` + `count` + `inset` + `span` + `layerGap?`); `PlacedBarInput = SingleBar | BarRow | Bundle | Layer` (additive → an M2 reader survives via `meta.app_document`, D-P0-2) + `isBarRow`/`isBundle`/`isLayer` guards. **P-B/P-C/P-D/P-E/P-F one expander:** `resolvePlacedBars` now `expandPlaced`s each input to N `SingleBar`s before resolving — a row spreads `count` evenly over `extent` (or steps by `spacing`); a bundle lays `n` touching bars centred on `position` (centre-to-centre = Ø → coupe shows n dots); a layer places `count` bars across `span`, `layerIndex` steps (`Ø + layerGap`, default `max(Ø,20)`) inboard of the face (needs `ctx.sectionDims`). Each expanded bar keeps a derived id `${parentId}#i` and flows through the EXISTING `resolveSingleBar` (curtailment + splice + geometry), so **BBS merges identical row/bundle bars into ONE mark** (key = shape|Ø|cut, position-independent) with the real count, `placeBars`/`sectionAt` render each distinctly. **RECT wiring:** `ElementSolveInput.placed?: PlacedBarInput[]` (column + beam) joins `extraBars` in `buildLongBars`'s free-bar pass with `sectionDims:{b,h}`; `hasUserAddressableContent` now also true when `placed` present → the A2 As/`d` reconciliation credits rows/bundles/layers to the nearest zone (a 2nd bottom layer **lowers the area-weighted `d` exactly** — the P-D deliverable). **Generic wiring:** the slab/stair/joist shims pass `sectionDims` (`b=Ly/width/flight_width, h=t`) so layers place there too; circular has none → a face layer is skipped (flagged). **Codepack:** `code.bundleEquivDiameter(φ,n)=min(55, φ√n)` added to BAEL + EC2 `PackExtras` + core `CodePackExtras` (⚠ PROVISIONAL, G-BAEL/EC2); the rules that USE it (bundle >4/cover breach, skin min, between-layer spacing) are Track V/M4. **Adapter:** a doc-level `PlacedBarDoc` union (`AddressableBar`=single | `PlacedRowDoc` | `PlacedBundleDoc` | `PlacedLayerDoc`, `kind`-discriminated) + a shared `placedDocBody` mapper (H11/H2/P2/B1 conventions) → `buildPlacedBars` emits the core union; `GenericDoc.placed` broadened + `ColumnDoc`/`BeamDoc` gain `placed?`; `.rcfg` round-trips (rides `meta.app_document`, additive). **P-G coupe fidelity** falls out (all kinds become `PlacedLongBar`s `sectionAt` already reads): a bundle = n touching dots, a layer = a distinct row at its depth, a skin row = side-face dots, a curtailed bar present only where it runs. **No golden moved** (no placed bars on any legacy doc). **Scope (flagged, → M4):** honest validation of the new steel (tiers), generic (non-RECT) As-credit, CIRCULAR layers; the skin `BarRow` supersedes the SKIN-supplement fanout for new detailing but that legacy path is left in place. Tests: `tests/{bar_row,layers,bundle,skin_steel,coupe_placed_bars}.spec.ts` (23), `apps/web/.../placed_bars.spec.ts` +2 (doc row/bundle/layer render+schedule+`.rcfg`), `perf_heavy` +2 (140-bar row/bundle expansion < 16 ms). Gate: `check` 722/141 ✓, core coverage 96.23 %, adapter 97.06 %, `build:web` ✓ — **run the two coverage jobs sequentially** (shared `coverage/.tmp/`).
- **D-V105-4** (v1.0.5 Phase 4 / M4 — Track V validation completeness + honest tiers, implemented) The NEW placed steel (M2/M3) is now JUDGED. **New pure `validation/placedBarRules.ts` (`validatePlacedBarRules(placed, resolved, ctx, code)`)** — reads the placed-input union for the count/cover/skin/layer rules and the resolved `PlacedLongBar[]` for curtailment + generic geometry — wired into the RECT pipeline (`element.ts`, `includeGeometry:false`) **and all 4 generic shims** (`circular/slab/stair/joist`, `includeGeometry:true`). **V-B** `bundle_max` (n > `code.bundleMax`=4, or > `bundleMaxAtLap`=3 when the bundle laps → 🔴; at/under → 🟢) + `bundle_cover` (equivalent Ø `φₙ = code.bundleEquivDiameter` breaches the cover envelope → 🔴, approaching within the band → 🟠; radial for CIRCULAR). **V-C** `skin_minimum` (`code.skinReinforcement({b,h})` depth-trigger; provided per-face skin from the `BarRow{skin}` set < min, or absent-where-mandatory → 🟠 **judgement, never blocks**). **V-D** `layer_clear_spacing` (between-layer clear = `layerGap`, floor `max(Ø,20)` — the M3 default meets it exactly → 🟢; below → 🔴 clash / 🟠 tight). **V-E** `curtailment_anchorage` (§7.7 via `code.lbd`: a bare `none` interior cut → 🔴; run shorter than the summed end development → 🔴, marginally short → 🟠; a full-length or member-end bar → no item). **V-A** the element-agnostic geometry `placed_axial_extent` + `placed_section_bounds` (radial envelope for CIRCULAR) generalised to the generic pipelines (RECT already runs the richer `validateAddressableBars`). **F1 guarantee** is `tierByMargin` (PASS at exactly the code value; the amber band is `[required·(1−band), required)`, strictly below) — asserted per band + across BAEL/EC2. **Affected-bar id (W4):** each FAIL emits the offending resolved placed-bar id(s) via a parent→member-ids map — a bundle/layer reddens all `${id}#i` members, a single/curtailed bar exactly one — flowing through `placeBars`→`failingGroupIds` unchanged (no `rebarProps.ts` change needed; a standalone bar's `groupId` is already unique). **Correctness fix carried:** a legitimate bundle's touching members (centre-to-centre = Ø) no longer trip the clear-spacing FAIL — new `placedParentId`/`placedKind` on `PlacedLongBar` (set in `resolvePlacedBars`) → a `bundleId` on `AddressableBarView` exempts intra-bundle pairs in `predicates.ts finishSpacing`. **New pack extras** (BAEL+EC2, sourced in `*-constants.json`, ⚠ PROVISIONAL G-BAEL/EC2/TOL): `bundleMax`/`bundleMaxAtLap` (§8.9), `skinReinforcement` (§7.3.3/§9.2.4 analogue — depth-threshold 1000 mm, per-face min 0.1 %·b·h, spacing ≤ 300), `curtailmentHookedFactor` (BAEL 0.4 / EC2 0.7); core `CodePackExtras` mirrors them optionally. **No `if(elementType)`/`if(codePack)`; purity held; no golden moved** (no placed content → no Track-V item; every legacy doc byte-identical). Tests: `tests/placed_bar_validation.spec.ts` (23 — one case per rule at 🔴/🟠/🟢, F1 per band, affected-bar id, the bundle-spacing correctness fix, legacy-none). Gate: `check` **747/142 ✓** (+1 file over M3), core coverage **96.55 %**, adapter **97.06 %**, `build:web` ✓ — **coverage jobs SEQUENTIALLY** (shared `coverage/.tmp/`).
- **D-V105-5** (v1.0.5 Phase 5 / M5 — splice / coupler reach, Track S, implemented) Splicing extended off the RECT-only path with the SAME `spliceBar`/`autoSplices` machinery, no new geometry. **(1) Circular/pile native cage:** `CircularLongInput` gains `splices?`/`autoSplice?`; `solveCircular` splices each long zone off `shape.cutLength` (`autoSplice` at the PROVISIONAL 12 000 mm stock — pile cages routinely > 12 m), attaches `SolvedGroup.splice` (the existing BBS path already schedules a group `splice` as **segments** — `${groupId}#{seg}` lines × the cage count — **plus the coupler tally** `couplers`), and raises the group-level `lap_stagger:<zone>` **WARN** (mirrors `element.ts`'s RECT group-splice WARN). The D-P1-1 invariant rides in the segment cut lengths (Σ = run + #laps·l_r). **(2) Any freely placed bar** already carried `splices`/`autoSplice` on its `PlacedBarBody` and resolved them via `resolveSingleBar`→`barSplice` (M2/M3) → `PlacedLongBar.splice` → scheduled as segments by the BBS `longBars` loop; M5 makes the **per-bar STAGGER** apply everywhere by **extracting** the per-bar lap analysis (stagger PASS/WARN items + lap extents) out of `element.ts` into the shared pure `analyzePlacedBarLaps(longBars, code)` (`section/resolvePlacedBars.ts`) and calling it from RECT (behaviour-preserving — same items, same site, same seismic extents) **and all four generic shims** (circular/slab/stair/joist, gated on `longBars` → fires only when a placed bar actually laps → legacy byte-identical). **(3) slab/mesh distribution stays unspliced** (spec Part IV): NO splice threading on the per-metre `SlabZoneInput`/joist/stair mat inputs — only a user's FREE placed bar can lap on those elements. **Adapter:** `ZoneEdit` gains `splices?`/`autoSplice?`, threaded into `circularInput`'s longitudinal zones (BAEL+EC2 via the injected pack); `.rcfg` round-trips for free (rides `meta.app_document`, additive). **Scope/flags:** the seismic overlay is composed on the **RECT column/beam** pipeline only (D-P4b-2) — so `lap_in_critical_zone` applies to RECT placed/grouped splices (already shipped, unchanged) but is **inherently N/A on the circular pipeline** (piles/spiral columns get no overlay today; a spiral-column seismic overlay is a pre-existing gap, out of M5 scope); circular lap extents therefore have no consumer beyond the stagger WARN and are not collected. Stock length + `l0` (lap) ride **PROVISIONAL** BAEL/EC2 → G-BAEL/G-EC2 + `[OWNER-DEP §C]` stock length. Tests: `tests/splice_reach.spec.ts` (9 — pile auto-split segments + coupler tally + D-P1-1 invariant + group stagger WARN; a free bar splices on a pile/slab; a single free lap = stagger PASS, many-coincident = WARN; slab distribution unspliced + byte-identical). **No golden moved.** Gate: `check` **756/143 ✓** (+1 file over M4), core coverage **96.59 %** (99.4 % functions), adapter **97.06 %**, `build:web` ✓ — ⚠ raw `npm run coverage` exit polluted by the pre-existing `perf_heavy:199` timing flake under coverage instrumentation on this Windows box (passes uninstrumented + under `check`).
- **D-V105-6** (v1.0.5 Phase 6 / M6 — BAEL / French façonnage schedule, Track O, `[OWNER-DEP §O-1]`, implemented) The BBS + shop drawing now speak the **French façonnage convention** — a **pure labelling layer** over the exporters, no engine/geometry/`cutLength` change, `shapeArchetypeId` stays the merge key / data key everywhere. **New `packages/exporters/src/faconnageCodes.ts`** — a `FaconnageDesignation { archetypeId, code, label, provisional }` + a pure `faconnageFor(id)` over a PROVISIONAL archetype-id→designation `TABLE` (one entry per shipped shape manifest: DROITE→Droite, CHAPEAU→Chapeau, ETRIER→Étrier, EPINGLE→Épingle, SPIRALE_HELICE→Spirale, TREILLIS_MESH→Treillis soudé, …16). An **unknown/future id falls back to the raw id** (never throws → forward-compat). **⚠ The nomenclature is a labelling convention, NOT structural → OWNER-SUPPLIED** (spec §0.5 / §O-1); it ships flagged `provisional: true` until the owner delivers the ratified NF A 35-027 / BAEL table — swapping it in = editing `TABLE` in one place, nothing downstream changes, **no engineering sign-off gate attached** (a name, not a number). **Wiring:** `BbsLine.faconnage` (bbs.ts, additive — populated `faconnageFor(shapeArchetypeId)`) → `BendingRow.faconnage` (shopDrawing.ts, additive) → **PDF** main BBS table re-laid to the French columns *repère / nombre / Ø / longueur de coupe / façonnage / total / masse* rendering the designation **label** (was the raw id) + **DXF** façonnage table gains an ASCII **`Fac.` code** column (R12 is ASCII-only, so the code `D/CH/ET/…`, not the accented label) + **on-screen `BbsPanel`** re-laid to the same French order rendering the label (id in the cell `title`) with a new i18n `bbs.faconnage` header (FR "Façonnage" / EN "Shaping"). Bundles/rows/curtailed bars schedule with the **real count + correct cut** (unchanged from M3/M1) and now a faithful designation. **PDF/DXF agree with the on-screen BBS by construction** (both read the same `computeBBS`/`shopDrawing` `faconnage`). Tests: `tests/faconnage_schedule.spec.ts` (7 — every line carries the designation for its archetype + `provisional`; renders the French labels not the raw ids; 16-shape coverage + unknown-id fallback; a bundle/row/curtailed bar schedules count+cut+designation; the shop-drawing bending table's `faconnage` == the BBS line's per mark). **No golden moved** (`shapeArchetypeId` preserved; the BBS merge key + `.rcfg` canonical arrays unchanged). Gate: `check` **763/144 ✓** (+1 file over M5), core coverage **96.59 %**, adapter **97.06 %**, `build:web` ✓.
- **D-V105-7** (v1.0.5 Phase 7 / M7 — lossless canonical file, Track E, implemented) The `.rcfg` **canonical arrays are now lossless for interchange** — a future server / IFC / other tool reads the real per-bar steel from them ALONE, not the private `meta.app_document` blob (which stays as a **convenience carry**, no longer the sole truth, D-P5-7 lifted). **Contract extension (audit B4), core:** a new `PlacedBarElement` (`kind:"PLACED_BAR"`) joins the `ReinforcingElement` union (+ `isPlacedBarElement` guard) — it carries ONE resolved bar's as-built geometry (`id`·`role`·`shapeArchetypeId`·`diameter`·`position{u,v}`·`axisStart`·`cutLength`·`start/endStation`·`anchorage`·`placedKind`/`placedParentId`·`splice{segments,couplerCount,lapLength}`) so a **bundle / row / layer / curtailed bar** — none expressible by the `distribution`+`placement` `BarGroup` — has a faithful canonical home. Anchorage is **inlined** (`{start?,end?}` strings) to avoid a `reinforcing-element.ts`↔`placed-bar.ts` type cycle. **`exporters/canonical.ts`:** new pure `placedBarsFromResult(result)` maps each `result.longBars` (`PlacedLongBar`, **skips `removed`**) → a `PLACED_BAR`; the existing group-based `reinforcementFromResult` (→ `baseGroups`/`supplementalGroups`, carrying transverse steel + the scheme summary) is **unchanged** — the two are additive (a consumer prefers `placedBars` when present; both describe the element). **`web/rcfgDoc.ts`:** `reinforcement.placedBars[]` (new optional array on `RcfgReinforcement`) is populated; **`seismic` is now written** from `doc.seismic` (only column/beam carry it) via `seismicOf` — was hardcoded `null` with a stale D-P4b-5 comment; `codePack` already followed the picker (A3/H13). **Double-solve stopped:** `docToRcfg(doc, cuts, meta, result?)` reuses a passed-in live `result` verbatim (re-solves via `solveDoc` **only** when absent); threaded through `projectToRcfg(instances, meta, resultsById?)` + `useAutosave` (passes the active instance's store `result` → the on-screen element is never re-solved on autosave; non-active instances still solve, no cached result). **`exporters/rcfg.ts`:** `CURRENT_RCFG_VERSION` **1.2 → 1.3** — additive + idempotent (new `"1.2"→"1.3"` step; existing `1.0/1.0.2/1.1` steps re-pointed to 1.3; version-only, nothing dropped). Schema `rcfg.schema.json` gains a `placedBars` array prop (loose `$ref` to `reinforcingElement`; `PLACED_BAR` needs only `id`+`kind`, `additionalProperties:true` → forward-compat by construction, same as TENDON). **Forward-compat NORMATIVE + newly exercised for our OWN kind:** an older reader that predates `PLACED_BAR` sees it as an unknown kind (`isBarGroup` false → `UnknownReinforcingElement` path) and preserves it verbatim. Tests: `apps/web/src/engine/interchange_lossless.spec.ts` (all 8 elements: a freely placed bar survives in the canonical `PLACED_BAR` arrays with `app_document` STRIPPED; curtailment stations survive + shorten `cutLength`; live seismic written / gravity → null; codePack BAEL/EC2; the double-solve is gone — a passed result is used verbatim) + `tests/migration_chain.spec.ts` (extended: a `PLACED_BAR` file round-trips without dropping the kind or its per-bar detail; new fixture `placed-bar-forward-compat.rcfg.json` joins the corpus). **No golden moved** (M7 is additive — new optional array + new kind + version bump). Gate: `check` **781/145 ✓** (+1 file over M6), core coverage **96.59 %** (99.4 % functions), adapter **97.15 %** (up from 97.06 %), `build:web` ✓ — ⚠ raw `npm run coverage` still trips the pre-existing Windows-box `perf_heavy:199` timing flake (21 ms > 16 ms under v8 instrumentation; passes under `check`). **Scope note:** `rcfgToDoc` still reconstructs the editable doc from `app_document` (the SPA can't yet rebuild an `ElementDoc` from the canonical arrays — that reverse map is a future/other-tool concern, spec-acknowledged); the lossless guarantee is on the canonical arrays *carrying* the detail, proven by stripping `app_document`.
- **D-V106-1** (v1.0.6 Phase N1 / Track U6 — control-type pass, implemented) **`apps/web` presentation-only, no engine/`.rcfg`/validation change** (spec §0.3.1, §7 N1; UI review Finding U4). Two new controls join `NumberField`: **`ui/Stepper.tsx`** (integer **− N +**, the middle a still-editable number box = the numeric/keyboard twin, invariant §0.3.3; no `dragMode`) for **counts**, and **`ui/NumberInput.tsx`** (plain number box, **no slider, no `dragMode`**, optional `unit` suffix) for **design inputs + non-geometry precision values**. `Sidebar.tsx` reclassified: counts (`nTop/nBottom/nLeft/nRight`, symmetric vertical/horizontal faces, chapeau/span/montage `nTop`/`nBottom`, relevé count, generic zone count, **seismic zone** 1–4) → `Stepper`; As,req/Asw,req/provided-area + spacing/anchorage/chapeau length/support width/splice station/material `f_c28`·`f_e` → `NumberInput`; **b/h/H/L/cover + generic geometry KEEP `NumberField`** (slider+`dragMode` — the one place a live-3D sweep earns its slider); diameter already a `<select>` (untouched). `AddressableBars`/`CrossTieEditor` (hook angle)/`RegionEditor` (end-zone/spacings) rewired `NumberField→NumberInput` (removes their needless `dragMode`). **`FaconnageEditor` deliberately left on `NumberField`** — its shape params ARE tuned against the live 3D bar + SVG sketch (a legitimate slider). After N1, the only `dragMode`-toggling sliders are true geometry dimensions + façonnage params. New CSS `.stepper-btn`/`.field-num-wide`/`.field-unit`. Tests: `ui/controls.spec.tsx` (5 — counts render a stepper that drives the store; a stepper never toggles `dragMode`; As,req is a plain number with no slider and no `dragMode`; the `b` dimension keeps its range slider + `dragMode` on pointerdown/up; diameter stays a `<select>`). **⚠ Borderline flagged for the owner GPU pass:** stirrup/cadre **spacing** was moved off the slider to a plain number (it isn't a b/h/L/cover dimension) — if the owner wants a live-sweep slider back on spacing it's a one-line `NumberInput→NumberField` swap. Gate: `check` **786/146 ✓**, `build:web` ✓, no golden moved.
- **D-V106-2** (v1.0.6 Phase N2 / Track U2 — one unified section canvas + tool routing, implemented) **`apps/web` presentation-only, no engine/`.rcfg`/validation change** (spec §7 N2, [REF-UI-555]; UI review Finding U2). The section diagram was embedded **three times** (`AddressableBars` / `CrossTieEditor` / `SupplementsPanel`), each owning a `<SectionPicker>` + a local `useBarLink` pending pick with a different click-meaning. N2 collapses them into **ONE** persistent **`ui/SectionCanvas.tsx`** rendered once at the top of `ColumnSchemeControls`/`BeamSchemeControls`. **Store tool-routing (session-only, NOT in `.rcfg`, like projection/rollRad/navMode):** new `sectionTool: "select" | "link"`, `sectionLink: {kind:"crosstie"} | {kind:"supplement",supplementId,diameter} | null`, `pendingLinkBar`, and the router `pickSectionBar(index)` — **select** → `selectedBars=[index]` (the addressable-bar target + 3D highlight); **link** → first click arms, re-click de-selects, a second distinct bar completes into a cross-tie (dedup, on the active tie/stirrup) or `addSupplement` (store owns instance-id + group; the panel supplies the type + Ø via `beginLink`) — then stays armed for more. `setSectionTool`/`beginLink`/`cancelLink`/`pickSectionExtra` round it out. **`SectionPicker` stays the pure SVG renderer** (+ its keyboard/a11y button list, invariant §0.3.3); `SectionCanvas` is the smart container (tool status strip + `Esc`/Cancel). **Panels rewired:** `AddressableBars` dropped its picker (reads `selectedBars`/`selectedExtraId` from the shared canvas); `CrossTieEditor`/`SupplementsPanel` dropped their picker + `useBarLink` and gained a **"link two bars" arm/cancel button** (`crossTies.linkOnSection` / `supplements.linkOnSection`, new i18n + a `sectionCanvas` i18n group). `useBarLink.ts` kept (still exercised by `section_picker.spec.ts`, harmless). **Scope (per §7):** Select + Link only — the add-bar/row placement tools + the shape palette are **N5**; the panels fully becoming inspector *modes* + the unified `selectedBars`⊕`selectedGroupIds` selection are **N3**; the section/elevation **docks** are **N4**. New CSS `.section-canvas`/`.section-canvas-status`/`.crosstie-link`/`.supp-link`. Tests: `ui/section_canvas.spec.tsx` (6 — exactly one canvas mounts; select routes to `selectedBars`; arming from the editor routes two clicks into a cross-tie; the store router binds an armed supplement to the picked pair; `Esc` cancels; a11y-list parity) + 4 pre-existing specs updated to drive the shared canvas (`addressable_bars_ui`/`beam_supplements`/`faconnage_phase6`/`picker_shows_extras`). Gate: `check` **792/147 ✓**, `build:web` ✓, no golden moved. **⚠ Owner GPU pass** pending (headless can't judge the one-canvas feel; `v1.0.6_owner_tasks.md` to be created at N3/N4).
- **D-V106-3** (v1.0.6 Phase N3 / Track U3 — contextual inspector + unified selection + advanced fallback, implemented) **`apps/web` presentation-only, no engine/`.rcfg`/validation change** (spec §7 N3, [REF-UI-820]; UI review Finding U3 + §0.3.4). **(1) Unified selection (§0.3.4):** store gains `selection: {kind:"bar",index} | {kind:"extra",id} | {kind:"crosstie",index,barA,barB} | {kind:"alert",groupIds} | null` + a single `select(sel)` router that sets `selection` AND keeps the legacy highlight channels (`selectedBars`/`selectedGroupIds`/`selectedExtraId`) **mutually-exclusive + in sync** — so the viewport (`buildScene(...selectedGroupIds, selectedBars)`) is untouched. `pickSectionBar`(select branch)→`select({bar})`; `pickSectionExtra`→`select({extra})`; `selectGroups`(AlertsPanel rows)→`select({alert})` or clear; the `CrossTieEditor` list rows became **buttons** → `select({crosstie})` (highlights both engaged bars); **`Viewport` 3D bar click routes through `select` (now single-select, was multi-toggle)**. Session-only, cleared on `reset()`. **(2) `ui/Inspector.tsx`:** a selection-driven panel that renders ONLY the selected object's fields — a longitudinal **bar** (remove / Ø / `CoupledLength` / axialPos / reused `FaconnageEditor` / reset — writing the SAME `barOverrides` as `AddressableBars`), an **extra** (u/v/Ø/length/axial/façonnage/remove), a **cross-tie** (pair label + remove), or an **alert** (read-only — the highlighted group ids); an empty-state hint when nothing is selected. Both it and the advanced form mutate one store — neither authoritative. **(3) `ui/ElementSetupStrip.tsx`:** a compact **always-visible** strip (element id · code-pack `<select>` · b/h · H or L · cover) so element setup is no longer a tab-flip away (answers U6-IA). **(4) Advanced fallback:** the full tabbed `Sidebar` form (Schéma/Géométrie/Projet) kept behind an **"Avancé"** toggle — `advancedForm` (session-only, a layout pref like `rightPanels`; **NOT** reset on element `reset()`; **default ON** so nothing regresses); collapsing it leaves the drawing-first surface (`SectionCanvas` + `Inspector`) live. `SectionCanvas` **moved out of the scheme controls into the always-visible sidebar region** (rendered once, column/beam only → still exactly ONE canvas). `ZoneReadout` preserved. Exported `CoupledLength` from `AddressableBars` for reuse (no AddressableBars behaviour change → its specs untouched). New i18n `inspector` + `setup` groups (FR+EN), new CSS `.setup-strip`/`.inspector*`/`.advanced-toggle`/`.crosstie-name`(now a button)/`.crosstie-row-sel`. **Scope (per §7):** the inspector covers the objects reachable TODAY (bar / extra / cross-tie / alert) — **row/bundle/layer inspection + making them selectable lands with the N5 placement palette that first makes them creatable on the canvas**; the section/elevation **docks** (the shell that gives the inspector the left region) are **N4**. Tests: `ui/inspector.spec.tsx` (6 — selection is one system: a bar-pick clears an alert-pick + vice-versa; selecting a canvas bar opens the inspector + a remove there writes the doc; a cross-tie selection highlights its pair; the advanced form is default-present + toggles off/on with the drawing surface staying live; exactly one canvas; the setup strip drives the code pack). **3 pre-existing specs updated (behaviour-equivalent):** `smoke`/`controls` scoped to the `.tab-body` (the strip legitimately adds a code `<select>` + a second `b`), `beam_supplements` fixed after renaming the strip's `aria-label` off the colliding "Élément". Gate: `check` **798/148 ✓**, `build:web` ✓, no golden moved. **⚠ Owner GPU pass** pending (inspector/strip feel + the single-select 3D click); **⚠ owner IA/default choice** — `advancedForm` defaults ON (drawing-first-with-full-form, no regression); flipping the default to drawing-first-only is a one-liner once N5's creation tools land (flagged §9).
- **D-V106-4** (v1.0.6 Phase N4 / Track U1 — the workspace shell (3D-primary + editable 2D docks), implemented) **`apps/web` presentation-only, no engine/`.rcfg`/validation change** (spec §7 N4, [REF-UI-830]; UI review Finding U1 + owner decision). **(1) Shell (`App.tsx`):** `app-body = <Sidebar> │ <main.workspace> │ <RightColumn>`; the `workspace` is a flex COLUMN of `.workspace-center` (a flex ROW: `.viewport-pane` **always-mounted** + `<SectionDock>`) over `<ElevationDock>`. The live 3D stays central + always-on. **(2) `ui/SectionDock.tsx`:** a first-class resizable(width)/collapsible dock to the RIGHT of the 3D hosting the ONE unified `<SectionCanvas>` (the N2 pick surface — column/beam only, `drawingFirst`) + the `<CoupePanel>` — it **subsumes the old F4 Coupes bottom dock** ("the coupe IS the editable section"). `ui/BottomPanel.tsx` **deleted**; the Navbar's Coupes button now `toggleDock("section")`. **(3) `ui/ElevationDock.tsx`:** a resizable(height)/collapsible dock BELOW the 3D rendering a **read-only** longitudinal `ElevationView` SVG built from the SAME pure `buildElevationFiche(result)` (exporters) that feeds the PDF/DXF elevation → screen == paper by construction; **direct-manipulation editing (drag bar ends / laps / bend-ups / stirrup-zone boundaries) is Track U5 / Phase N6** — flagged in the dock's `.dock-note`. **(4) Left region = inspector:** `<SectionCanvas>` **moved OUT of the `Sidebar` into the section dock**; the `Sidebar` keeps `ZoneReadout` + `ElementSetupStrip` + `Inspector` + the "Avancé" tabbed-form fallback (the `Inspector` reads the unified `selection`, so it works whichever surface the pick came from). **(5) Store (session-only, NOT in `.rcfg`, NOT reset by `reset()` — a layout pref like `rightPanels`/`advancedForm`):** new `docks: Record<"section"|"elevation", {open:boolean; size:number}>` (section=width px, elevation=height px; init `{section:{open,300}, elevation:{open,200}}`) + `toggleDock(key)` / `resizeDock(key,size)` (min-size floor 120). `bottomPanel`/`setBottomPanel` removed. **a11y (§0.3.3):** each dock's resize handle is a `role="separator"` with **arrow-key** resize twins; collapse/re-open are labelled buttons. New CSS `.workspace`/`.workspace-center`/`.dock`/`.section-dock`/`.elevation-dock`/`.dock-head`/`.dock-body`/`.dock-resize*`/`.dock-reopen*`/`.elevation-svg`; old `.bottom-panel` removed. New i18n `workspace.{sectionDock,elevationDock,openDock,collapseDock,resizeDock,elevationReadonly}` (FR+EN). **Scope (per §7):** N4 is the shell only — the **placement tools + shape/row palette** (Add bar/row/layer/bundle, snap grid, typable coord twin) are **N5**; **elevation editing** is **N6**; the panels fully dissolving into inspector-only modes rides N5. **Because the ONE `SectionCanvas` moved out of the `Sidebar`, 6 canvas-coupled specs were mechanically updated to also render `<SectionDock/>`** (assertions unchanged): `inspector`, `section_canvas`, `addressable_bars_ui`, `beam_supplements`, `faconnage_phase6`, `picker_shows_extras`; `workspace_layout.spec` dropped its now-obsolete Coupes-in-bottom-dock test (covered by the new `workspace_shell.spec`). Tests: `ui/workspace_shell.spec.tsx` (5 — both docks mount around the 3D with the canvas+coupe in the section dock; a dock collapses to a re-open strip + re-opens; resize sets store size + inline style + floors the min; the elevation dock is read-only with a note; dock layout NEVER serializes into `.rcfg`). Gate: `check` **802/149 ✓**, `build:web` ✓, no golden moved. **⚠ Owner GPU pass** pending (dock resize/collapse feel, the 3D-primary shell, the read-only elevation render). **⚠ Owner IA/default** (`advancedForm` default ON) still open — confirm at/after N5 (D-V106-3, §9).
- **D-V106-5** (v1.0.6 Phase N5 / Track U4 — the tool palette + place-by-pointing, implemented) **`apps/web` + a new pure `engine/placement.ts`; no engine/`.rcfg`/validation/golden change** (spec §7 N5, [REF-UI-560]; UI review Finding U8). **(1) Modal toolbar (`ui/ToolPalette.tsx`, mounted at the top of the section dock body):** Select · Add bar · Add row · Add layer · Add bundle · Link · Measure — each an `aria-pressed` button + a single-key shortcut (v/b/r/l/u/k/m via a window keydown listener **guarded** so typing a letter in an INPUT/SELECT/TEXTAREA never flips the tool) + **`Esc`→Select**; the Link button/shortcut arms a cross-tie via the existing `beginLink({kind:"crosstie"})`. **(2) Palette (only for an `add-*` tool):** a façonnage-shape `<select>` (the exported `LONGITUDINAL_SHAPES`, the 9 open longitudinal shapes) + a Ø `<select>` + a **typable `(u,v)` coordinate twin** + a **Place** button. **(3) Store (session-only, NOT in `.rcfg`, reset on `reset()`):** `SectionTool` widened to `select|link|add-single|add-row|add-bundle|add-layer|measure` (+ `PLACE_KIND_BY_TOOL` map); `paletteShape`/`paletteDiameter`/`placeCoord` + `setPaletteShape`/`setPaletteDiameter`/`setPlaceCoord`; **`setPlaced`** (writes `doc.placed` on any doc type via `edit(...)`) + **`placeInSection(u,v)`** (the router: snaps → updates the `placeCoord` twin → if an `add-*` tool, builds the matching `PlacedBarDoc` and appends to `doc.placed`); `setSectionTool` now clears armed-link state for any non-`link` tool. **(4) Pure `engine/placement.ts`:** `snapSection(u,v,{b?,h?,cover,diameter,grid?})` clamps into the cover envelope (`dim/2−cover−Ø/2`; a non-rect section only grid-snaps) + a 5 mm grid; `freshPlacedId` = first free `p{n}`; `buildPlacedBar(kind,opts)` → a single `AddressableBar` (no `kind`) / a 3-bar `row` across the clear width / a 2-bar `bundle` at `(u,v)` / a 2nd `layer` (face by the `v` sign). **(5) Place-by-pointing on the canvas:** `SectionPicker` gained an optional `onPlace(u,v)` — a transparent hit-`rect` behind the bars maps the pointer via `getScreenCTM`/`createSVGPoint` (guarded → no-op under jsdom, so the **typed-coordinate twin + Place is the headless-tested a11y path**, invariant §0.3.3; the pointer drop is GPU/owner-verified). `SectionCanvas` passes `onPlace={placeInSection}` + a tool-aware hint only in an add-tool. **(6) All new steel rides the v1.0.5 `doc.placed` model** — `solveDoc` already threads `placed` to the engine on **all 8 elements** (column L529 / beam L666 / generic via `placedInput`), so a placed bar renders (3D/coupe) + schedules (BBS/DXF/PDF) + is validated (M4 Track-V) for free; verified a placed single solves clean (`solveError` null) → a `standalone` longBar. **(7) Placed-bar list** in the palette: each entry shows kind·shape·Ø with a count/n stepper (bundle n floored 2–4) + a remove ×. New i18n `tools.*` (FR+EN), new CSS `.tool-palette`/`.toolbar`/`.tool-btn`/`.palette*`/`.placed-*`/`.sp-place-area`. **Scope (per §7):** **Measure** is a coordinate-readout mode only (full two-point distance-measure → N6); the **row/bundle/layer editing via the N3 `Inspector`** (its reserved slots) is the remaining thin slice; **elevation placement/editing** is N6. Tests: `engine/placement.spec.ts` (8 — snap clamp/grid/non-rect, fresh-id reuse, build single/row/bundle/layer) + `ui/tools.spec.tsx` (6 — tool switching + `aria-pressed`; shortcuts + Esc + link-arm; palette shape/Ø drive the store; typed-coord + Place creates a snapped single that solves clean; each add-tool creates the right kind; the placed list steps n + removes). Gate: `check` **816/151 ✓**, `build:web` ✓, no golden moved, no existing spec touched. **⚠ Owner GPU pass** pending (the on-canvas pointer drop + snap feel + tool discoverability; owner to confirm the snap-grid density + default palette ordering, §8 owner item).
- **D-V106-6** (v1.0.6 Phase N6 / Track U5 — the editable elevation, implemented — **completes v1.0.6**) **`apps/web` + a new pure `engine/elevation.ts`; NO core geometry / `.rcfg` / validation / golden change** (spec §7 N6, [REF-UI-811]; UI review Finding U7). The `ElevationDock` (read-only since N4) becomes a canvas over the **v1.0.5 per-bar station model**. **(1) Pure `engine/elevation.ts`:** `fitElevation(fiche,{W,H,pad})` — the fit transform extracted verbatim from N4's inline `ElevationView` (`px`/`py`) + a `toDrawing(sx,sy)` inverse (so a pointer recovers the drawing point); `stationFromDrawing(att,dx,dy)` inverts `orientPoint` (VERTICAL column → axis is drawing-y; HORIZONTAL/FLAT beam/slab → drawing-x); `snapStation(station,L,grid=10)` clamps `[0,L]`+grids; `screenToStation(...)` = the ONE composed pointer→snapped-station seam every gesture uses; `selectedBarStations(doc,selection)` reads the selected bar's `{start,end,memberLen,anchorage,splices,curtailed}` from the doc (group override by index ⊕ independent extra by id; defaults to the full run `[0,L]`), `memberRunLength(doc)` = column H / beam L. `Selection` is typed locally (`{kind;index?;id?}`) so `engine/` never imports the store. **(2) Store station actions (session→doc→re-solve; each committed by BOTH the on-canvas drag AND its numeric twin, invariant §0.3.3):** `curtailSelectedBar(end,station?)` (writes `startStation`/`endStation`; `undefined` → cleared via a new `stripUndef` so an otherwise-untouched bar stays legacy byte-identical), `setSelectedBarAnchorage(choice?)`, `addSelectedBarSplice(at,kind)`/`removeSelectedBarSplice(at)` (dedup by station; empty → field cleared), `setReleveBend(id,station)`, `moveStirrupRegionBoundary(index,station)` (reads `effectiveRegions` → sets the boundary → `normalizeRegions` → `setTie`/`setStirrup` — **the SAME normalize path the `RegionEditor` table uses**, so drag == table). A module-level `patchSelectedBar(get,patch)` routes a bar/extra patch through the existing `setBarOverrides`/`setExtraBars`. **(3) Editable `ui/ElevationDock.tsx`:** renders the read-only fiche (concrete/bars/marks/callouts via the shared transform) + overlay handles — draggable curtailment end-caps (a green rail start→end) + splice markers for the selected bar, draggable dashed zone-boundary lines for the tie/stirrup regions — each `onPointerDown` opens a window pointermove/up that maps client coords → `screenToStation` → the store action + a live station readout; `getScreenCTM` is null under jsdom so the drag no-ops in tests (the pure helpers + store actions + numeric twins are the headless gate; the pointer drag is owner-GPU-verified). **(4) Numeric twins (headless-tested a11y path):** `ui/Inspector.tsx` gains a `StationEditor` fieldset (curtailment start/end `NumberInput`s + an anchorage `<select>` [runs-through/none/straight/hook] + a splices add-lap/add-coupler + list) rendered in the bar AND extra branches; `ui/Sidebar.tsx`'s relevé editor gains a bend-station `NumberInput`. **(5) Adapter, NO core change:** new `ReleveZone.bendStation?` (`engine/document.ts`) read by `releveExtraBars` (`solveDoc.ts`) to parameterise the existing `RELEVE` archetype's bottom leg (clamped to keep the bent bar inside the span) — replacing the hardcoded `L*0.25`. New i18n `elevation` group (FR+EN), new CSS `.elevation-svg-edit`/`.elev-handle`/`.elev-boundary`/`.elev-rail`/`.elev-splice`/`.inspector-stations`/`.inspector-splice*`. **Scope (per §7, flagged):** (a) **row/bundle/layer curtailment** — the store actions key off the unified `selection`, but N5's placed rows/bundles/layers are not yet IN `selection`, so the station edits target a longitudinal override or an extra only (wiring placed kinds into `selection` extends N6 to them for free — a thin follow-up); (b) the **splice-drop + relevé-bend on-canvas drag handles** are minimal (the numeric twin is the tested/primary path); (c) the relevé **up-leg** stays default (only the bend station is editable). Tests: `ui/elevation.spec.tsx` (14 — the transform `toDrawing` inverse; `stationFromDrawing` per attitude; `snapStation` clamp/grid; `screenToStation` mid→mid + clamp; the `selectedBarStations` reader defaults/override/extra/null; `memberRunLength`; every store action incl. curtail set/clear, anchorage, splice dedup/clear, region-boundary == the normalize path, relevé bend; the inspector twin round-tripping to the store == the drag; the dock renders the editable `.elevation-svg-edit`). One updated spec: `workspace_shell.spec` (its N4 read-only assertion flipped to editable). Gate: `check` **830/152 ✓**, `build:web` ✓, no golden moved. **⚠ Owner GPU pass (largest, `owner_tasks §D-9`)** pending — the on-canvas drag *feel* + handle placement can't be judged headless; also confirm the 10 mm station snap. **Companion owner-tasks doc restructured spec-independent** (`owner_tasks.md` replaces `v1.0.4_owner_tasks.md`, owner instruction 2026-07-11).

---

## 9. Handoff log (newest first — APPEND your entry here before you stop)

### 2026-07-11 — v1.0.6 **Phase N6 (Track U5 — the editable elevation)** + **owner-tasks doc made spec-independent** — **CODE DONE, tested, green — NOT pushed** — by **Zayd** (Hetzner dev box)

**Branch / sync.** On `1.0.5-&-up`. `git fetch && git checkout 1.0.5-&-up && git pull` → **already up to date** at `d93d8a3` (N4+N5 are unpushed in the working tree from Amer's turn — I built N6 on top of them). Read `core_logic.md` + `architecture_breakdown.md`, then `current_state.md §1/§7/§8/§9`, then `v1.0.6_spec.md`. Per the spec build order (§7: N1→…→N5→**N6**) N6 is next + last; depends on N5 (present). This **completes v1.0.6**.

**What N6 delivers (plain language).** The longitudinal **elevation** (the side view of the bar down its length — where a detailer spends most of their time) was *read-only* since N4. N6 makes it something you **work on**: select a bar, then **drag its end to cut it short** (curtailment) — the 3D and the schedule's cut length update live; **drag a stirrup-zone boundary** to move where the ties get denser; drop a **lap/coupler**, set a bar's **end treatment at a support** (runs through / straight / hook), and set a **bend-up point** for a relevé. And — the rule for the whole app — **every one of those drags has a typed number + keyboard twin**: the inspector shows the selected bar's cut-off stations / anchorage / splices as plain fields, the stirrup-zone table stays the numeric way to move a boundary, and the relevé has a bend-station field. Nothing about the engine, the checks, the schedule or the saved file changed — only *how you reach and edit* the bar's along-length detailing.

**How (files).** New pure **`engine/elevation.ts`** — `fitElevation` (the fit transform extracted from N4's inline `ElevationView`, with a `toDrawing` inverse), `stationFromDrawing` (inverts `orientPoint`: column runs up drawing-y, beam along-x), `snapStation` (clamp `[0,L]`+grid), `screenToStation` (the single pointer→snapped-station seam), `selectedBarStations`/`memberRunLength` doc readers. **Store (`store/useStore.ts`):** `curtailSelectedBar`/`setSelectedBarAnchorage`/`add·removeSelectedBarSplice`/`setReleveBend`/`moveStirrupRegionBoundary` + a private `patchSelectedBar` router (group override by index ⊕ extra by id) + a `stripUndef` (clearing a station removes the field → legacy byte-identical). **`ui/ElevationDock.tsx`** rewritten editable (fiche render + draggable curtail end-caps + splice markers + zone-boundary lines, wired via `screenToStation`; live station readout; pointer drag owner-GPU-verified, no-ops headless). **`ui/Inspector.tsx`** gains a `StationEditor` (curtailment/anchorage/splice numeric twins) rendered in the bar + extra branches. **`ui/Sidebar.tsx`** relevé editor gains the bend-station twin. **`engine/document.ts`** + **`engine/solveDoc.ts`**: `ReleveZone.bendStation?` read by `releveExtraBars` to parameterise the existing `RELEVE` bottom leg — adapter-only, **no core geometry change**. New i18n `elevation` group (FR+EN) + CSS. See **D-V106-6**.

**Gate (green, this dev box).** `npm run check` ✓ **830 / 152** (+1 file = `ui/elevation.spec.tsx` +14; over N5's 816/151). `build:web` ✓ (pre-existing three.js chunk-size warning only). **No golden moved** — N6 is `apps/web` + one new pure helper; purity held (no `packages/*` touched). One updated spec: `workspace_shell.spec` — its N4 "elevation is read-only, editing deferred to N6" assertion flipped to "editable canvas (`.elevation-svg-edit`)". `elevation.spec.tsx` covers the pure transform/station/snap helpers, the doc readers, every store action (curtail set/clear, anchorage, splice dedup/clear, region-boundary == the normalize path, relevé bend), and the inspector twin round-tripping to the store (== the drag). Did not run `coverage` (UI + a small pure helper, above thresholds; `check` is the honest gate).

**Owner-tasks doc — restructured on owner instruction (2026-07-11).** The owner asked to make the owner-tasks doc **spec-independent**. Created **`owner_tasks.md`** (living, version-spanning) and **`git rm`'d `v1.0.4_owner_tasks.md`** (its still-open rows §A–§E are carried forward, with a **Since** column preserving which version each arose in). It folds in the v1.0.5 provisional items (façonnage table §O-1 → B-12; stock length → C-8; M4 limits → B-11) and the v1.0.6 GPU passes (D-7 v1.0.5 new steel, D-8 N1–N5 drawing-board, **D-9 the N6 editable elevation — the largest**), plus the open product calls (A-8 drawing-first default, A-9 setup-strip IA, A-10 snap density). Note: `v1.0.6_spec.md §8/§17` still names a `v1.0.6_owner_tasks.md` companion — that role is now filled by the single `owner_tasks.md` (the specs are version-locked history; not edited).

**Flags / handoff for the next turn.**
1. **v1.0.6 IS NOW CODE-COMPLETE (N1 → N6).** The drawing-board is delivered: right widgets (N1), one section canvas + tools (N2), contextual inspector (N3), 3D-primary shell + docks (N4), place-by-pointing palette (N5), **editable elevation (N6)**. There is **no N7** — the next new work is a fresh spec (v1.0.7 or the v1.1 server jump per `core_logic §9.2`). Natural near-term: the owner GPU/visual acceptance sweep (`owner_tasks §D`), then decide the drawing-first default (A-8).
2. **N4 + N5 + N6 are ALL unpushed** on this branch — handed back (owner to say when to push). N1+N2 (`ea68924`) and N3 (`5989f5f`) are already on origin. Working tree = Amer's N4/N5 files + my N6 files (`engine/elevation.ts`, `engine/elevation.spec.tsx`, editable `ElevationDock.tsx`, the modified store/inspector/sidebar/document/solveDoc/i18n/css/workspace_shell.spec) + `owner_tasks.md` (new) − `v1.0.4_owner_tasks.md` (removed) + this doc.
3. **⚠ OWNER GPU PASS — now the whole v1.0.6 arc (people-gated).** `cd apps/web && npm run dev` (port 5180). N6's on-canvas **drag** (curtail an end, drag a zone boundary) can't be judged headless — the pure hit-test/snap + the store actions + the numeric twins ARE tested, but the *feel* of dragging + the handle placement on the real render need your eyes. Captured as **`owner_tasks §D-9`** (largest pass). Also confirm the **10 mm elevation station snap** (§A-10).
4. **⚠ OWNER IA/DEFAULT (`advancedForm`, A-8) — now fully unblocked + the last product call.** It still defaults **ON** (form + drawing-first surface both visible). With N5 (place-by-pointing) **and** N6 (editable elevation) both present, the spec's drawing-**first** end-state (form default OFF) strands nothing if flipped — it's a one-liner (`advancedForm` init `true`→`false`). I did NOT flip it unilaterally (it changes the app's first impression). **Recommend the owner decides now.**
5. **Scope deferred from N6 (flagged, per §7):** (a) **row/bundle/layer curtailment** — N5 creates them + a palette list, but they are NOT yet in the unified `selection`, so the elevation/inspector station edits target a longitudinal override or an independent extra only; wiring placed rows/bundles/layers into `selection` (a thin N5/N6 follow-up) extends N6 to them for free (the store actions already key off `selection`). (b) The **splice-drop + relevé-bend on-canvas drag handles** are minimal — the *numeric twin* (inspector splice add/list, sidebar bend field) is the tested/primary path; richer on-canvas splice/relevé dragging is a polish slice. (c) The relevé **up-leg** stays the default 45°/incline; only the **bend station** is editable (flagged for the owner if the up-leg should also be draggable). None block — all honest, all have a numeric path.
6. **Standing people-gated items unchanged** (engineer sign-offs, owner data, the other GPU passes) — now consolidated in the new spec-independent **`owner_tasks.md`** (see §B/§C/§D there), not a per-version file.

### 2026-07-11 — v1.0.6 **Phase N5 (Track U4 — the tool palette + place-by-pointing)** — **CODE DONE, tested, green — NOT pushed** — by **Amer** (owner's Windows PC)

**Branch / sync.** Same turn, immediately after N4 (owner said "Go for next phase"). On `1.0.5-&-up`; N4 + N5 both unpushed on the local branch. Per spec build order (§7: …N4→**N5**→N6) N5 is next, depends on N2–N4 (all present this turn).

**What N5 delivers (plain language).** Before, to add a bar you dug into a numeric sub-panel. Now there's a **toolbar** at the top of the Section panel — Select, Add bar, Add row, Add layer, Add bundle, Link, Measure — like a drawing app. Pick "Add bar" (or press **B**), choose a shape + diameter, then **click the section** to drop it exactly where you want (or type the u/v coordinates and hit **Place** — the keyboard/precision way). The bar **snaps** so it can't land outside the concrete cover. Rows, bundles and extra layers drop the same way. Every dropped bar is real steel — it shows in the 3D + coupe, goes on the schedule, and gets code-checked — because it feeds the same v1.0.5 model on **all 8 element types**. A little list under the palette lets you bump a row's count or remove a bar. Keyboard shortcuts + `Esc`→Select throughout.

**How (files).** New **`ui/ToolPalette.tsx`** (the modal toolbar + shape/Ø palette + typed-coordinate twin + Place + the placed-bar list; a guarded window-keydown for shortcuts + Esc). New pure **`engine/placement.ts`** (`snapSection` cover-envelope+grid clamp · `freshPlacedId` · `buildPlacedBar` per kind). Store (`store/useStore.ts`): widened `SectionTool` + `PLACE_KIND_BY_TOOL`; `paletteShape`/`paletteDiameter`/`placeCoord` + setters; **`setPlaced`** (writes `doc.placed`) + **`placeInSection`** (snap→drop router); `setSectionTool` clears link state for non-link tools; palette state reset on `reset()`. `ui/SectionPicker.tsx` gained an optional `onPlace(u,v)` (a guarded `getScreenCTM` pointer map + a hit-rect). `ui/SectionCanvas.tsx` passes `onPlace` + a tool-aware hint in add mode. `ui/SectionDock.tsx` mounts the palette. `ui/FaconnageEditor.tsx` exports `LONGITUDINAL_SHAPES`. New i18n `tools.*` (FR+EN) + CSS. See **D-V106-5**.

**Gate (green, Windows PC).** `npm run check` ✓ **816 / 151** (+2 files: `placement.spec.ts` +8, `tools.spec.tsx` +6; over N4's 802/149); `npm run build:web` ✓. **No golden moved. No existing spec needed a change** — the palette mounts in the section dock (already rendered by the 6 canvas-coupled specs since N4) and, in the default Select mode, renders only its toolbar (no extra combobox/list) so nothing collided. Verified a placed single solves clean (`solveError` null → a `standalone` longBar) so dropped bars genuinely render/schedule, not just sit in the doc.

**Flags / handoff for the next turn.**
1. **Next is v1.0.6 Phase N6 (Track U5) — the editable elevation** (`v1.0.6_spec.md §7`, the largest phase): make the read-only `ElevationDock` (built in N4) a canvas over the v1.0.5 per-bar station model — drag a bar end (curtailment), a runs-through handle (support anchorage), drop a lap/coupler (splice station), grab a bend-up point (relevé), drag stirrup-zone boundaries (`TransverseRegion`), each with a live typed value + the numeric `RegionEditor`/station controls kept as the a11y twin. Depends on N5 (present). **Largest owner GPU pass.**
2. **N4 + N5 are BOTH unpushed** — handed back (owner to say when to push). Working tree = N4's files + N5's new `ToolPalette.tsx`/`placement.ts`/`placement.spec.ts`/`tools.spec.tsx` + the modified store/canvas/dock/i18n/css/FaconnageEditor + this doc.
3. **⚠ OWNER GPU PASS (people-gated).** The on-canvas pointer drop (`getScreenCTM` map), the snap feel, and tool discoverability can't be judged headless — `cd apps/web && npm run dev` (port 5180). **Owner also to confirm (§8 owner items): the snap-grid density (default 5 mm) + the default palette/tool ordering.** Still recommend creating `v1.0.6_owner_tasks.md` to collect these (un-created since N3).
4. **Scope deferred from N5 (flagged, per §7):** (a) **Measure** is a coordinate-readout mode only — the two-point distance-measure is N6-adjacent; (b) **row/bundle/layer editing through the N3 `Inspector`** — N5 creates them + a palette list manages count/n/remove, but deep per-field editing via the Inspector's reserved slots is the remaining thin slice (they are NOT yet in the unified `selection`); (c) placement snap is exact for RECT (column/beam) — a generic (non-rect) section only grid-snaps (no rectangular envelope), which is honest but coarser; refine per element if the owner wants.
5. **⚠ OWNER IA/DEFAULT (unchanged, now actionable).** `advancedForm` still defaults **ON**. With N5's on-canvas creation tools now present, the spec's drawing-**first** end-state (form default OFF) is finally unblocked — **flipping the default is a one-liner** (`advancedForm` init `true`→`false`), but I did NOT flip it unilaterally (it changes the app's first impression). **Recommend the owner decides now.**

### 2026-07-11 — v1.0.6 **Phase N4 (Track U1 — the workspace shell: 3D-primary + editable 2D docks)** — **CODE DONE, tested, green — NOT pushed** — by **Amer** (owner's Windows PC)

**Branch / sync.** On `1.0.5-&-up`. `git fetch && git checkout 1.0.5-&-up && git pull` fast-forwarded `d853188 → d93d8a3` (N3's `5989f5f` landing + follow-ups). Read `core_logic.md`, `architecture_breakdown.md`, `current_state.md` §1/§7/§8/§9, then `v1.0.6_spec.md`. Took my turn opposite Zayd (who did N1/N2/N3). Per spec build order (§7: N1→N2→N3→**N4**→N5→N6) N4 is next, depends on N2+N3 (both present).

**What N4 delivers (plain language).** The app stops being "a form on the left, a 3D picture in the middle" and becomes a **drawing board**: the live 3D stays in the centre and always on, with a **Section panel** docked to its right (the cross-section you click bars on + the coupe preview — the old bottom "Coupes" strip folds into it) and an **Elevation panel** docked below it (the side view of the bar down its length). Both panels can be **resized (drag the divider, or arrow-keys)** and **collapsed** to a thin re-open tab. The left side is now the **Inspector** (edit the one bar you clicked) — the place the big form used to sit; the full form is still one "Avancé" click away. The Elevation panel is **read-only for now** — you can *see* the elevation but *dragging* bar ends/laps/zones on it is the next-next step (N6). Nothing about the bars, checks, schedule or file changed — only how the surfaces are laid out.

**How (files).** `App.tsx` restructured into the shell (`.workspace` = `.workspace-center`[`.viewport-pane` always-mounted + `<SectionDock>`] over `<ElevationDock>`). New **`ui/SectionDock.tsx`** (hosts the ONE `<SectionCanvas>` + `<CoupePanel>`; width-resizable; collapsible) and **`ui/ElevationDock.tsx`** (read-only `ElevationView` SVG from `buildElevationFiche(result)`; height-resizable; collapsible). `ui/BottomPanel.tsx` **deleted**; `Navbar` Coupes button → `toggleDock("section")`. `Sidebar.tsx`: `<SectionCanvas>` removed (it now lives in the dock) — Sidebar = readout + setup strip + `<Inspector>` + advanced form. Store (`store/useStore.ts`): session-only `docks` state + `toggleDock`/`resizeDock` (min-floor 120; NOT in `.rcfg`, NOT reset by `reset()`); `bottomPanel`/`setBottomPanel` removed. New i18n `workspace.*` dock strings (FR+EN); new CSS for the workspace + docks; old `.bottom-panel` CSS removed. See **D-V106-4**.

**Gate (green, Windows PC).** `npm run check` ✓ **802 / 149** (+1 file `workspace_shell.spec.tsx` +5 tests, −1 obsolete `workspace_layout` test, over N3's 798/148); `npm run build:web` ✓ (only the pre-existing three.js chunk-size warning). **No golden moved** — N4 is `apps/web` presentation-only (purity held, no `packages/*` touched). **6 canvas-coupled specs mechanically updated** (they render `<Navbar/><Sidebar/>` and query `.section-canvas`; since the ONE canvas moved into the section dock, each now also renders `<SectionDock/>` — assertions unchanged): `inspector`, `section_canvas`, `addressable_bars_ui`, `beam_supplements`, `faconnage_phase6`, `picker_shows_extras`. `workspace_layout.spec` dropped its Coupes-in-bottom-dock test (now covered by `workspace_shell.spec`). Did not run `coverage` (UI-only, above thresholds; `check` is the honest gate).

**Flags / handoff for the next turn.**
1. **Next is v1.0.6 Phase N5 (Track U4) — the tool palette + place-by-pointing** (`v1.0.6_spec.md §7`): a modal toolbar (Select · Add bar · Add row · Add layer · Add bundle · Link · Measure, `Esc`→Select) + a shape/row palette dropped on the **section dock's** canvas (snap to the cover envelope + a layer grid, a **typable coordinate twin** for a11y/precision), creating the right `PlacedBar` in the store on all 8 elements. N5 also makes **row/bundle/layer** creatable + selectable — N3's `Inspector` already reserved the field slots; N4's section dock is where they'll drop. **Needs an owner GPU pass.**
2. **N4 is NOT pushed** — handed back for the next turn (owner to say when to push). Working tree = the 3 new files + the modified shell/store/i18n/css/specs + this doc.
3. **⚠ OWNER GPU PASS (people-gated).** The dock resize/collapse *feel*, the 3D-primary shell reading right, and the read-only elevation render can't be judged headless — `cd apps/web && npm run dev` (port 5180). To be captured in `v1.0.6_owner_tasks.md` (still un-created — N3 flagged it; recommend the next agent creates it at N5).
4. **⚠ ELEVATION IS READ-ONLY IN N4 (deliberate, per §7).** The `ElevationDock` shows the elevation but has NO editing gestures yet — Track U5 / Phase **N6** owns drag-to-curtail / lap-drop / bend-up / zone-boundary-drag. The dock is the surface N6 will make editable; I did not add gesture stubs (they'd be dead code). Flagged so it isn't mistaken for a gap.
5. **⚠ OWNER IA/DEFAULT (unchanged from N3, still open).** `advancedForm` still defaults **ON** (drawing-first surface ABOVE the full form) — zero-regression while N5's on-canvas creation tools don't exist. The spec's end-state is drawing-**first** (form as fallback, default OFF); flipping is a one-liner but should wait until N5 gives the canvas its create affordances. **Recommend the owner confirms at/after N5.**
6. **Design choice I made (not owner-gated, flagged for transparency):** the spec calls both docks "editable"; per the §7 sequencing (N6 = editable elevation) I built the elevation dock **read-only** in N4 and put its data through the existing pure `buildElevationFiche` so screen == PDF/DXF for free. If the owner expected *some* elevation editing already in N4, that's an N6-scope conversation — I did not pull N6 forward.

### 2026-07-10 — v1.0.6 **N1+N2 re-validated** + **Phase N3 (Track U3 — contextual inspector + unified selection)** — **CODE DONE, tested, green — PUSHED (`5989f5f`)** — by **Zayd** (Hetzner dev box)

**Branch / sync.** On `1.0.5-&-up`. Read `cross_projects_policy.md` (devbox, one level up), `core_logic.md`, `architecture_breakdown.md`, `current_state.md §1/§7/§8/§9`, `v1.0.6_spec.md`. `git status` = **up-to-date with `origin/1.0.5-&-up` at `ea68924`**, working tree clean — so this session started *after* N1+N2 (owner note "continue implementing 1.0.6, not from scratch", `git pull` = already up to date). Took a turn opposite Amer.

**Part 1 — validated the last work (N1+N2, `ea68924`).** Ran the honest gate `npm run check` on this box: **792 tests / 147 files pass**, matching the N2 handoff's claimed 792/147 exactly. Inspected the commit: all 18 files are `apps/web` (+ `current_state.md`) — **purity invariant held, no `packages/*` touched, no golden**. Read the N2 store router (`pickSectionBar`/`beginLink`/`cancelLink` + the `sectionTool`/`sectionLink`/`pendingLinkBar` trio): select/link routing is correct, cross-tie dedup is right, supplement binding threads type+Ø from the panel while the store owns instance-id+group, tool state is session-only + cleared on `reset()`. `section_canvas.spec.tsx` (6) genuinely exercises one-canvas + select + link→crosstie + link→supplement + Esc + a11y-list parity — **not weakened**. **Verdict: N1+N2 VALIDATED — green, correct, additive, purity-clean.** (The owner GPU pass for the one-canvas *feel* remains people-gated, as flagged — unchanged.)

**Part 2 — built v1.0.6 Phase N3 (Track U3), the contextual inspector.** Per the spec build order (`§7`: N1→N2→**N3**→N4→N5→N6) N3 is next and depends only on N2 (present). It is the "work on the drawing, the form follows" pivot: editing becomes **selection-driven** and the two old selection channels fold into one. Fully headless/RTL-testable except the 3D-click + inspector *feel* (owner GPU).

**What N3 delivers (plain language).** Before, to change one bar you hunted for it in the long left-hand form. Now you **click the bar** (on the section drawing, in the 3D, or an alert row) and a small **Inspector** shows just *that* bar's settings to edit — shape, diameter, length, remove. Cross-ties are clickable too. A compact **strip** at the top always shows the element's core setup (size, cover, design code) so it's never a tab away. The whole old form is still there, one click behind an **"Avancé"** button — a complete fallback. And "what's selected" is now **one thing at a time**, consistent between the drawing, the 3D and the inspector (picking a bar drops an alert highlight, and vice-versa). Nothing about the bars/checks/schedule/file changed — only *how you reach and edit* an object.

**How (files).** Store (`store/useStore.ts`): new `Selection` union + `select()` router (folds `selectedBars`⊕`selectedGroupIds`⊕`selectedExtraId`, mutually-exclusive, viewport untouched) + `advancedForm` (session-only, default ON, NOT reset on element `reset()`); `pickSectionBar`/`pickSectionExtra`/`selectGroups` funnel through `select`. New **`ui/Inspector.tsx`** (selection-driven fields; reuses `FaconnageEditor` + the now-exported `CoupledLength`) and **`ui/ElementSetupStrip.tsx`** (compact always-visible setup). `ui/Sidebar.tsx` restructured: `ZoneReadout` → strip → (col/beam) `SectionCanvas`+`Inspector` → "Avancé" toggle → the tabbed form; `SectionCanvas` moved here from the scheme controls (still exactly ONE). `ui/CrossTieEditor.tsx` list rows → selectable buttons. `viewport/Viewport.tsx` 3D pick → `select` (single-select). New i18n `inspector`+`setup` (FR+EN), new CSS. See **D-V106-3**.

**Gate (green, this dev box).** `npm run check` ✓ **798 / 148** (+1 file = `ui/inspector.spec.tsx`, +6 tests, over N2's 792/147); `npm run build:web` ✓ (pre-existing three.js chunk-size warning only). **No golden moved** — N3 is `apps/web` presentation-only. **3 pre-existing web specs updated (behaviour-equivalent, not weakened):** `smoke.spec` + `controls.spec` now scope to the `.tab-body` (the setup strip legitimately adds a code-pack `<select>` above the tabs and a second `b` field, invalidating the old "first combobox"/bare-`getByLabelText` assumptions); `beam_supplements` needed no scope change once the strip's `aria-label` was renamed off the colliding "Élément" (`setup.title` → "Réglages de l'élément" / "Element setup"). Did not run `coverage` (UI-only, above the ≥90 core/adapter thresholds; `check` is the honest gate).

**Flags / handoff for the next turn.**
1. **Next is v1.0.6 Phase N4 (Track U1) — the workspace shell** (`v1.0.6_spec.md §7`): a 3D-primary shell with **editable 2D docks** (section dock + elevation dock flanking the always-on 3D, resizable/collapsible; the inspector takes the left region the form owned; the bottom Coupes dock subsumed by the section dock). N4 builds on N2+N3 (both present). **Needs an owner GPU pass.**
2. **⚠ OWNER IA / DEFAULT DECISION (flagged, not guessed).** `advancedForm` defaults **ON** — i.e. the drawing-first surface (strip + canvas + inspector) sits ABOVE the full tabbed form, both visible; this guarantees **zero regression** (all creation controls — counts, ties, supplements, splices, regions — stay reachable) while N5's on-canvas creation tools don't exist yet. The spec's end-state is drawing-**first** (form as the *fallback*, default OFF). Flipping the default is a **one-liner** (`advancedForm` initial `true`→`false`) but should wait until **N5** gives the canvas its create affordances, else collapsing the form strands creation. **Recommend confirming this with the owner at N4** (it pairs with the §8 owner item "which element-setup fields belong in the strip vs the advanced form").
3. **Scope deferred from N3 (per §7, flagged not skipped):** the inspector covers **bar / extra / cross-tie / alert** (what's selectable today); **row/bundle/layer inspection** waits for **N5** (the placement palette that first makes those creatable + selectable on the canvas). The panels *fully* dissolving into inspector-only modes is also part of the N4/N5 shell arc — for N3 they remain in the advanced form.
4. **Minor regression, flagged:** the 3D bar click is now **single-select** (was a multi-toggle) to drive the one inspector; and `SupplementsPanel`'s broken-binding "rebind to the 2 selected bars" path (`selectedBars.slice(0,2)`) can no longer gather 2 bars via single-select — the **link-arm add flow (N2) supersedes it**. Neither is load-bearing; both are cheap to revisit if the owner wants multi-select back.
5. **`v1.0.6_owner_tasks.md` still not created** — the per-phase GPU acceptance checklist the spec's companion note calls for. N2 deferred it to "N3/N4"; N3 didn't create it (kept scope tight). **Recommend creating it at N4** with the accumulated N1–N4 GPU checks (widgets feel, one-canvas select/link, inspector/strip, single-select 3D click, dock resize).
6. **PUSHED** — the owner said "Push for Amer to continue," so N3 is committed + pushed to `origin/1.0.5-&-up` (`5989f5f`, 12 files: 3 new, 9 modified — all `apps/web` + this doc). N1+N2 already on origin (`ea68924`). Amer picks up from N4.

### 2026-07-10 — v1.0.6 **Phase N2 (Track U2 — one unified section canvas + tool routing)** — **CODE DONE, tested, green — PUSHED** — by **Zayd** (Hetzner dev box)

**Continuation of the same session** (owner: "continue implementing then push"). Same branch `1.0.5-&-up`. Built N2, the second v1.0.6 phase, on top of N1 (below). **Pushed N1 + N2 together** on the owner's word (see the gate + git note at the end).

**What N2 delivers (plain language).** The cross-section diagram used to appear **three separate times** in the form — once for editing individual bars, once for cross-ties, once for supplements — each behaving differently. N2 makes it **one drawing**. A little tool switch decides what clicking a bar does: normally it **selects** that bar (so you can edit it); when you press "link two bars" in the cross-tie or supplement panel, the same drawing goes into **link mode** and your next two clicks connect a pair. This is the first real taste of "work on the drawing, not the form" — the whole point of v1.0.6. (Everything the bars/checks/schedule/file do is unchanged; only *how you point at bars* changed.)

**How (files).** New **`ui/SectionCanvas.tsx`** — the ONE canvas, rendered once at the top of the column/beam scheme controls; it reads the active tool from the store and shows a status strip (select vs link) + a Cancel/`Esc`. **Store (`store/useStore.ts`, session-only — NOT in `.rcfg`):** `sectionTool` (`select`/`link`), `sectionLink` (what a completed link makes — a cross-tie, or a supplement of a given type+Ø), `pendingLinkBar`, and the router **`pickSectionBar`** (select → `selectedBars`; link → arm/deselect/complete → cross-tie dedup-add or `addSupplement`) plus `beginLink`/`cancelLink`/`pickSectionExtra`. **`SectionPicker` unchanged** — it stays the pure SVG renderer + a11y button list; `SectionCanvas` wraps it. **Panels rewired:** `AddressableBars` dropped its embedded picker (now reads the shared `selectedBars`); `CrossTieEditor` + `SupplementsPanel` dropped their picker + `useBarLink` and gained a **"link two bars" arm/cancel button**. New i18n `crossTies.linkOnSection` / `supplements.linkOnSection` / a `sectionCanvas` group (FR+EN). New CSS for the canvas + link buttons. See **D-V106-2**.

**Gate (green, this dev box).** `npm run check` ✓ **792 / 147** (+1 file = `ui/section_canvas.spec.tsx`, +6 tests, over N1's 786/146); `npm run build:web` ✓. **No golden moved** — N1+N2 are `apps/web` presentation-only, purity untouched. Four pre-existing specs were updated to drive the shared canvas (`addressable_bars_ui`/`beam_supplements`/`faconnage_phase6`/`picker_shows_extras`) — behaviour-equivalent, not weakened.

**Flags / handoff for the next turn.**
1. **Next is v1.0.6 Phase N3 (Track U3) — the contextual inspector.** Select a bar/row/bundle/cross-tie → an inspector shows just that object's fields (reusing `FaconnageEditor` etc.); the current tabbed form stays as an **"Avancé / Advanced"** fallback; and the two selection channels (`selectedBars` for bars, `selectedGroupIds` for alert rows) fold into **one** unified selection (spec invariant §0.3.4). N3 builds directly on N2's `selectedBars` + `SectionCanvas`. **Needs an owner GPU pass.**
2. **Scope deliberately deferred from N2 (per spec §7 ordering), flagged not skipped:** the **add-bar / add-row / add-bundle / add-layer placement tools + the shape palette are N5**; the **section + elevation docks (the shell) are N4**; the panels *fully* becoming inspector modes is **N3**. N2 delivered "Select + Link first" exactly as the §7 table scopes it.
3. **`useBarLink.ts` is now unused by the app** but kept because `section_picker.spec.ts` still exercises it (harmless). It can be deleted when that test is folded into the canvas tests, or left as a tested utility.
4. **Owner GPU pass for N1+N2:** `cd apps/web && npm run dev` (port 5180) on the owner's Windows GPU — confirm the stepper/number widgets (N1) and especially the **one-canvas select/link feel** (N2: arm "link two bars", click two dots, see the tie appear). `v1.0.6_owner_tasks.md` will be created when N3/N4 land (it's where the per-phase GPU acceptance checklist lives per the spec's companion note).
5. **PUSHED** — the owner said "then push," so N1 + N2 are committed and pushed to `origin/1.0.5-&-up` this session (unlike the earlier "hand back unpushed" default). All of v1.0.5 M1–M7 was already on origin (M7 = `d853188`).

### 2026-07-10 — v1.0.5 **VALIDATED** + v1.0.6 **Phase N1 (Track U6 — control-type pass)** — **CODE DONE, tested, green — NOT pushed** — by **Zayd** (Hetzner dev box)

**Branch / baseline / sync.** On `1.0.5-&-up`. `git fetch && git pull --ff-only` fast-forwarded `5b80092 → d853188` — **Amer's M7 is now COMMITTED + PUSHED on origin** (`d853188`), so the M7 §9 flag #5 below ("committed-ready but uncommitted") was self-referential inside that same commit; reconciled in §1 + the summary block. Read `cross_projects_policy.md` (devbox, one level up), `core_logic.md`, `architecture_breakdown.md`, `current_state.md §1/§6/§7/§8/§9`, `v1.0.5_spec.md` + `v1.0.5_impl_plan.md`, and `v1.0.6_spec.md`.

**Part 1 — validated v1.0.5's ending (M7).** Ran the honest gate `npm run check` on this dev box: **781 tests / 145 files pass**, matching Amer's claimed `781/145` exactly. Inspected the M7 diff (`d853188`, 12 files): the `PLACED_BAR` contract extension (`types/reinforcing-element.ts`) is **additive and forward-compat-safe** — it joins the `ReinforcingElement` union, `isBarGroup` is unchanged, and the `UnknownReinforcingElement` path is intact (an older reader preserves a `PLACED_BAR` verbatim). `placedBarsFromResult` (`exporters/canonical.ts`) is pure/deterministic, **skips `removed` bars**, spreads optional fields cleanly, and leaves the group-based `reinforcementFromResult` untouched (the two arrays are additive). The `interchange_lossless.spec.ts` test genuinely proves losslessness — it **deletes `meta.app_document`** and asserts the canonical arrays alone carry per-bar geometry across all 8 elements + curtailment stations + live seismic + code pack, and that the double-solve is gone. **Verdict: M7 (and all of v1.0.5 M1–M7) is VALIDATED — code-complete, green, additive, no golden moved.** (Engineering sign-offs + owner GPU/data items remain people-gated, unchanged — those gate *acceptance*, not code; §6/§1.)

**Part 2 — v1.0.6 Phase N1 (Track U6, control-type pass) — the first drawing-board phase.** Chose N1 per the spec's own build sequencing (`v1.0.6_spec.md §7`): it depends on nothing, is "cheap polish," and is **fully headless/RTL-testable** — the right first phase for this dev box (no GPU here; WebGL phases N2–N6 each need the owner's visual pass). This answers UI-review Finding U4 ("`NumberField` = slider+number was used for *everything*, tripping `dragMode` needlessly").

**What N1 delivers (plain language).** The long left-hand form now uses the *right kind of control for each field*. A **count** (how many bars on a face, how many stirrup legs, the seismic zone) is now a small **− N +** stepper, not a drag-slider. A **required-steel input** (`As,req` — a number you copy from your analysis, never "swept") is now a **plain number box**. The **slider is kept only** for the few dimensions you genuinely tune while watching the 3D update — the section width/height/length and the cover. Diameter stays a dropdown. Net effect: fewer accidental "live-drag" slowdowns, and each control matches what the value actually is. Nothing about the bars, checks, drawings, schedule or saved file changed — this is purely *how you type the numbers*.

**How (files).** New **`ui/Stepper.tsx`** (− N +, middle stays an editable number = the numeric/keyboard twin, invariant §0.3.3; no `dragMode`) and **`ui/NumberInput.tsx`** (plain number, no slider, no `dragMode`, optional `unit` suffix). **`ui/Sidebar.tsx`** reclassified every control: counts → `Stepper`; As,req/Asw,req/provided-area + spacing/anchorage/chapeau length/support width/splice station/material grades → `NumberInput`; **b/h/H/L/cover + generic geometry KEEP `NumberField`** (slider + `dragMode`); diameter already `DiameterSelect` (untouched). **`AddressableBars`/`CrossTieEditor`/`RegionEditor`** rewired `NumberField→NumberInput` (drops their needless `dragMode`). **`FaconnageEditor` intentionally left on `NumberField`** — its shape params are genuinely tuned against the live 3D bar + sketch, so its slider earns its place. New CSS `.stepper-btn` / `.field-num-wide` / `.field-unit` (`styles.css`). See **D-V106-1**.

**Gate (green, this dev box).** `npm run check` ✓ **786 / 146** (+1 file = `ui/controls.spec.tsx`, +5 tests, over M7's 781/145); `npm run build:web` ✓. **No golden moved** — N1 is `apps/web` presentation-only, purity untouched (no `packages/core`/exporter/manifest change). Did not run `coverage`/`coverage:adapter` (N1 adds only UI components, well above the ≥90 core/adapter thresholds already met at M7; `check` is the honest gate).

**Flags / handoff for the next turn.**
1. **Next work is v1.0.6 Phase N2 (Track U2)** — collapse the three embedded `SectionPicker` copies into **one persistent section canvas** whose active tool decides what a click means (Select/Link first). Depends on the v1.0.5 placed-bar model (present) + N1 (done). **Needs an owner GPU pass** (headless can't drive the canvas feel). Then N3 (inspector) → N4 (shell) → N5 (placement tools) → N6 (editable elevation).
2. **Owner-dependent, flagged not guessed:** N1 moved stirrup/cadre **spacing** off the slider to a plain number (it isn't one of the b/h/L/cover geometry dimensions the spec names for live-3D tuning). Sweeping spacing *does* visibly change the stirrup cadence in the 3D, so this is a genuine borderline — **if the owner prefers a live-sweep slider on spacing, it's a one-line `NumberInput→NumberField` swap.** Raised in D-V106-1 too. Similarly, material grades `f_c28`/`f_e` became plain numbers; a future nicety is a `<select>` of standard grades (C25/C30…, FeE400/500), deferred — not in N1's DoD.
3. **Per-bar Ø in `AddressableBars`** is a `NumberInput`, not the `<select>` the spec prefers for diameters — `DiameterSelect` is a Sidebar-local (unexported) helper. Converting it is cleaner to do when `AddressableBars` becomes an **inspector mode** in N3 (exporting/sharing the select then). Flagged, deferred.
4. **Owner GPU/visual passes** for the whole v1.0.6 arc: `cd apps/web && npm run dev` (port 5180) on the owner's Windows GPU. N1's stepper/number widgets are simple DOM (low-risk) but a quick look confirms feel; N2+ each need a real pass (`v1.0.6_owner_tasks.md`, to be created when N2 lands).
5. **Not pushed** — handing back per policy (commit/push on owner's word only). All of v1.0.5 M1–M7 is already on origin (M7 = `d853188`); N1 sits in the working tree.

### 2026-07-10 — v1.0.5 **Phase 7 (M7) — lossless canonical file (Track E)** — **CODE DONE, tested, green — NOT pushed** — by **Amer** (owner's Windows PC)

**Branch / baseline.** Continued on `1.0.5-&-up`. On `git pull` fast-forwarded `36ba036 → 5b80092` — **M6 is now COMMITTED on origin** (`5b80092`, `faconnage_schedule.spec.ts` + the façonnage labelling layer), reconciled in §1 (the M6 row + summary now read PUSHED). Read `core_logic.md` + `architecture_breakdown.md` + `current_state.md §1/§7/§8/§9` + `v1.0.5_spec.md` (Part VI / Track E) + `v1.0.5_impl_plan.md` (Phase 7), then implemented **Phase 7 ONLY** (M7). Nothing beyond it. *(`cross_projects_policy.md` lives only on the devbox — owner said to proceed without it, as at M5.)*

**What M7 delivers (plain language).** When you save a project to a `.rcfg` file, the file now records **every bar you detailed** — bundles, counted rows, extra layers, side-face skin, curtailed bars — in the file's *open, standard* section (the "canonical arrays"), not only in a private SmartBar-only blob. It also records the **seismic regime** and the **design code** you picked. The upshot: a future server, a BIM/IFC exporter, or a different program can read the real steel out of a SmartBar file without understanding SmartBar's internal document — the file is **faithful to the design, not just to this app**. Nothing about the bars, the drawings, or the schedule changed; this is about what the *saved file* carries. (SmartBar itself still reloads its own files from the private blob — that convenience stays; the point is the standard part is now complete too.)

**How (the four spec items, all done).**
1. **Contract extension (audit B4) — core `types/reinforcing-element.ts`.** New `PlacedBarElement` interface, `kind:"PLACED_BAR"`, added to the `ReinforcingElement` union with an `isPlacedBarElement` guard. It expresses one resolved bar's as-built geometry (id · role · shapeArchetypeId · Ø · position{u,v} · axisStart · cutLength · start/endStation · anchorage · placedKind/placedParentId · splice{segments,couplerCount,lapLength}) — the bundle/row/layer/curtailed-bar detail a `BarGroup`'s `distribution`+`placement` cannot hold. Anchorage is **inlined** as `{start?,end?}` strings (not the `BarEndAnchorage` import) to avoid a `reinforcing-element.ts`↔`placed-bar.ts` type cycle.
2. **Mapping — `exporters/canonical.ts`.** New pure `placedBarsFromResult(result)` → maps each `result.longBars` (`PlacedLongBar`) to a `PLACED_BAR`, **skipping `removed`** bars. The existing group-based `reinforcementFromResult` (→ `baseGroups`/`supplementalGroups`) is untouched — it still carries the transverse steel + scheme summary; the two arrays are **additive** (a consumer prefers `placedBars` when present).
3. **Populate + stop the double-solve — `web/rcfgDoc.ts` (+ `projectRcfg.ts`, `useAutosave.ts`).** `reinforcement.placedBars[]` (new optional array on the core `RcfgReinforcement`) is filled; **`seismic` is now written** from `doc.seismic` via `seismicOf` (was hardcoded `null`); `docToRcfg` gained an optional 4th arg `result?` — when passed it's used verbatim (**no re-solve**), threaded via `projectToRcfg(…, resultsById?)` and `useAutosave` (the active instance's live store `result`). `codePack` already followed the picker.
4. **Version bump — `exporters/rcfg.ts`.** `CURRENT_RCFG_VERSION` **1.2 → 1.3**; the `MIGRATIONS` steps re-pointed to 1.3 + a new `"1.2"→"1.3"` step (all additive/idempotent, version-only). `rcfg.schema.json` gained a loose `placedBars` array prop (forward-compat by construction: `PLACED_BAR` needs only id+kind, `additionalProperties:true`).

**Proven end-to-end.** New `apps/web/src/engine/interchange_lossless.spec.ts` (13): for **all 8 elements** a freely-placed bar survives in `reinforcement.placedBars` **with `meta.app_document` deleted** (per-bar shapeArchetypeId/Ø/cutLength/position intact); a curtailed bar's start/endStation survive + its cutLength is < the full member; the live seismic regime is written (gravity → null); codePack BAEL/EC2; and the double-solve is gone (a passed result is used verbatim). `tests/migration_chain.spec.ts` extended: a `PLACED_BAR` file round-trips without dropping the kind or its per-bar detail (new fixture `placed-bar-forward-compat.rcfg.json` — version 1.0.2 with a seismic regime + a curtailed PLACED_BAR — joins the corpus, so it also passes the generic loads/idempotent checks). Forward-compat for our OWN kind is thereby exercised, not just a synthetic vendor kind.

**Gate (green).** `npm run check` ✓ **781 / 145** (+1 file = `interchange_lossless.spec.ts`, over M6's 763/144). `coverage` core **96.59 %** stmts / 99.4 % functions / 96.59 % lines (≥ 90; unchanged — the canonical additions are covered). `coverage:adapter` **97.15 %** (no regression, up from 97.06 %). `build:web` ✓. **No golden moved** (M7 is additive — optional `placedBars[]` + new kind + version bump; every existing golden holds). **Run the two coverage jobs SEQUENTIALLY**; `npm run check` is the honest gate. ⚠ **Windows-box flake:** raw `npm run coverage` trips `perf_heavy.spec.ts:199` (M3 row/bundle perf case reads **21 ms > 16 ms** under v8 instrumentation) — a coverage-timing artefact, NOT a runtime regression (green under `check`); the M5/M6 devbox runs don't show it. Coverage % above captured by excluding that one timing test.

**Flags / handoff for the next turn.**
1. **v1.0.5 is now CODE-COMPLETE (M1 → M7).** Every Part I–VI item is implemented, tested, green. The natural next work is **v1.0.6 — the drawing-board UI** (`v1.0.6_spec.md`): surface the M1–M7 capabilities on an editable section canvas + longitudinal elevation with a tool palette + per-bar inspector. Until now the whole engine/model/validation/schedule/file is proven **headlessly** and driven through the existing side-panel form (spec §10).
2. **`rcfgToDoc` still rebuilds the editable doc from `app_document`** (not from the canonical arrays). That reverse map (canonical arrays → `ElementDoc`) is a separate, larger job and is **spec-acknowledged as out of scope** ("app_document carry may remain as a convenience"). The M7 losslessness guarantee is that the canonical arrays *carry* the detail — proven by stripping `app_document` in the test — not that the SPA reconstructs from them.
3. **Double-solve elimination is scoped to the ACTIVE instance.** Autosave passes the on-screen element's live `result`; other project instances (not being edited, no cached result) still re-solve in `docToRcfg`. Caching a result per `ElementInstance` would remove that too — a small follow-up, not required by the DoD.
4. **Owner-gated items are all unchanged** (M7 added none): engineer sign-offs G-BAEL/EC2/RPS/COUPE/TOL (provisional constants), the §O-1 façonnage table (M6), stock length 12 000 mm (`[OWNER-DEP §C]`), and the owner GPU/visual passes (`cd apps/web && npm run dev`, port 5180). See the §1 standing-open-items block.
5. **Not pushed** — handing back. M7 is committed-ready but uncommitted; the owner controls the push. (M6 = `5b80092` is already on origin.)

### 2026-07-10 — v1.0.5 **Phase 6 (M6) — BAEL / French façonnage schedule (Track O)** — **CODE DONE, tested, green — PUSHED (`5b80092`)** — by **Zayd** (Hetzner dev box)

**Branch / baseline.** Continued on `1.0.5-&-up`. Fast-forwarded `4c636e2 → 36ba036` on `git pull` — **M5 is now COMMITTED on origin** (`36ba036`, `splice_reach.spec.ts` + circular splice threading), not just in the working tree as the M5 entry below said; reconciled. Read `cross_projects_policy.md` (the box-local co-working policy, one level up) + `core_logic.md` + `architecture_breakdown.md` + `current_state.md §1/§7/§8/§9` + `v1.0.5_spec.md` (Part V / Track O) + `v1.0.5_impl_plan.md` (Phase 6), verified the M5 tree green as a baseline (`npm run check` exit 0), then implemented **Phase 6 ONLY** (M6 — Track O). Nothing from M7.

**What M6 delivers (plain language).** The bar-bending schedule and shop drawing now speak the **French *façonnage* convention** — the trade language a French bending shop reads. Before M6 the schedule showed the engine's internal shape codes (`DROITE`, `CHAPEAU`, `ETRIER`); now each is rendered under its French designation (`Droite`, `Chapeau`, `Étrier`) and the schedule columns follow the French BBS layout (*repère / nombre / Ø / longueur de coupe / façonnage / masse*). **Nothing about the steel changed** — same bars, same cut lengths, same counts; only what each shape is *called* and the column order on the paperwork. This is a **pure labelling layer**.

**Owner-dependency (the important flag).** The façonnage nomenclature (the NF A 35-027 / BAEL shape-code table + cadre/étrier/chapeau naming) is a **labelling convention, not structural**, so per spec §0.5 it is **owner-supplied** (`[OWNER-DEP → v1.0.5_owner_tasks.md §O-1]`). The owner has **not yet delivered** the ratified table, so I shipped a **PROVISIONAL** placeholder map — sensible French names for all 16 shipped shapes, every entry flagged `provisional: true`. **When the owner delivers the table, swapping it in = editing the `TABLE` object in `faconnageCodes.ts` (one place); nothing downstream changes.** No engineering sign-off gate is attached (it's a name, not a number — G-BAEL/EC2 don't touch it).

**How (all in `packages/exporters`, no engine/core change, purity trivially held).**
- **`faconnageCodes.ts` (new)** — `FaconnageDesignation { archetypeId, code, label, provisional }`; a `FACONNAGE_PROVISIONAL` flag; a readonly PROVISIONAL `TABLE` (archetype id → `{ code, label }`, 16 entries — one per `apps/web/manifests/shapes/*.json`); pure `faconnageFor(id)` (unknown id → raw id fallback, never throws — forward-compat for a future shape); `faconnageCoverage()` for tests. Exported via `index.ts`.
- **`bbs.ts`** — `BbsLine` gains `faconnage: FaconnageDesignation` (additive), populated `faconnageFor(r.shapeArchetypeId)` in the line map. `shapeArchetypeId` **unchanged** (still the merge key + the field tests/`.rcfg` read).
- **`shopDrawing.ts`** — `BendingRow` gains `faconnage` (additive), copied from the BBS line → so **PDF + DXF share it with the on-screen BBS by construction**.
- **`pdf.ts`** — `drawBbsTable` re-laid to the French columns (`Rep. / Nb / Ø / L. coupe / Façonnage / Total(m) / Masse(kg)`) rendering `l.faconnage.label` in the Façonnage column (was the raw `shapeArchetypeId`).
- **`dxf.ts`** — the façonnage table (already a dimensioned *croquis* sketch) gains a compact **`Fac.` code** column; renders `r.faconnage.code` (ASCII — R12 DXF text is ASCII-only, so the code `D/CH/ET/…`, not the accented `Étrier`).
- **`apps/web`** — `BbsPanel.tsx` re-laid to the same French order, renders `l.faconnage.label` (archetype id kept in the cell `title=` for power users); new i18n key `bbs.faconnage` (FR "Façonnage" / EN "Shaping") in `strings.ts` (both bundles — the completeness test enforces parity).

**Proven end-to-end.** `tests/faconnage_schedule.spec.ts` (7, core-level via `referenceBeam`/`solveElement` + `computeBBS` + `shopDrawing`): every scheduled line carries `faconnageFor(shapeArchetypeId)` with `provisional:true` and the data key preserved; the French labels render (Étrier/Chapeau/Droite), not the raw ids; the table covers ≥16 shapes and an unknown id falls back to itself; a **bundle** (count 3, cut 3000), a **counted row** (count 4, cut 3000), and a **curtailed** bar (cut 2000 = 2500−500 < uncut) each schedule correct count+cut+designation; the shop-drawing bending table's `faconnage` == the BBS line's per mark (PDF/DXF == on-screen). The existing `pdf_smoke`/`project_pdf` suites still build the PDF green (exercises the re-laid table). `.rcfg` unaffected (canonical arrays keyed on `shapeArchetypeId`).

**Gate (green).** `npm run check` ✓ **763 / 144** (+1 file, +7 tests over M5's 756/143). `coverage` core **96.59 %** stmts / 99.4 % functions / 96.59 % lines (≥ 90). `coverage:adapter` **97.06 %** (no regression). `build:web` ✓. **Run the two coverage jobs SEQUENTIALLY** (shared `coverage/.tmp/`); `npm run check` alone is the honest gate. **No golden moved** — `shapeArchetypeId` is preserved, the additive `faconnage` field doesn't touch any frozen assertion, and the BBS determinism/merge goldens hold.

**Flags / handoff for the next turn.**
1. **§O-1 owner deliverable is STILL OPEN.** The shipped façonnage labels are PROVISIONAL placeholders. When the owner supplies the ratified NF/BAEL shape-code + nomenclature table, edit `TABLE` in `faconnageCodes.ts` (and, if they want different short codes on the DXF, the `code` fields). Flip `FACONNAGE_PROVISIONAL` to `false` at that point. This does **not** block M7 or a release *code-wise*, only the schedule's *acceptance* as site-final.
2. **Column-layout choices are conventions, not owner-ratified.** I kept a `Total(m)` column alongside the spec's six French columns (repère/nombre/Ø/longueur de coupe/façonnage/masse) because dropping total length loses useful takeoff info; and the DXF shows the ASCII **code** while PDF/on-screen show the full **label** (R12 can't render accents). If the owner wants the exact six columns or the full label everywhere, that's a small tweak — flag for owner review at the visual pass.
3. **On-screen visual pass is owner-GPU-gated** (like every prior UI item): the re-laid `BbsPanel` is headless-proven but the actual look wants the owner's browser (`cd apps/web && npm run dev`, port 5180).
4. **Next turn: Phase 7 (M7) — Track E, lossless canonical file** (`v1.0.5_spec.md` Part VI, plan Phase 7): extend the `ReinforcingElement` contract with a `PLACED_BAR` kind (or additive bundle/layer/stations fields) so a bundle/row/layer/curtailed bar is expressible in the canonical `.rcfg` arrays (not just the private `meta.app_document` blob); populate `seismic` + `codePack` in `docToRcfg` (currently hardcoded `seismic:null`, `rcfgDoc.ts:77`); stop the double-solve (pass the store's live `result`, `:70`); bump `rcfg_version` (additive, idempotent migration); test forward-compat for our OWN new kind (an old reader round-trips a `PLACED_BAR` file). M7 depends on M2–M6 (all done).
5. **Not pushed** — handing back for the next turn. M6 is committed-ready but uncommitted; the owner controls the push.

### 2026-07-10 — v1.0.5 **Phase 5 (M5) — splice / coupler reach (Track S)** — **CODE DONE, tested, green — NOT pushed** — by **Amer** (owner's Windows PC)

**Branch / baseline.** Continued on `1.0.5-&-up`. Started from `origin/1.0.5-&-up == 4c636e2` (I fast-forwarded `c38cd3f → 4c636e2` on `git pull` — **M2 + M3 + M4 are now COMMITTED on origin**, not just in the working tree as the M4 entry below implied; the current_state text has been reconciled). Read `core_logic.md` + `architecture_breakdown.md` + `current_state.md §1/§8/§9` + `v1.0.5_spec.md` (Part IV) + `v1.0.5_impl_plan.md` (Phase 5), then implemented **Phase 5 ONLY** (M5 — Track S). Nothing from M6/M7. (The `cross_projects_policy.md` lives only on the devbox — owner said to proceed without it.)

**What M5 delivers (plain language).** Before M5, only rectangular columns/beams could split a too-long bar into fabricated pieces joined by an overlap (a "lap") or a mechanical "coupler." M5 gives that reach to **(1)** the round pile / spiral-column cage — a cage taller than a single ~12 m steel bar now auto-splits at the stock length, and each piece is listed on the schedule with the coupler count — and **(2)** any bar the detailer freely places anywhere, on any of the 8 elements. It deliberately leaves **slab/mesh distribution steel** whole (that's ordered in stock lengths this release). No drawing/geometry changed — a bar that isn't split looks and schedules exactly as before.

**How (engine).** Same `spliceBar`/`autoSplices` machinery, no new geometry, all in `packages/core` (purity + no `if(elementType)` held):
- **`section/resolvePlacedBars.ts`** — new shared pure `analyzePlacedBarLaps(longBars, code) → { lapExtents, staggerItems }`, **extracted verbatim** from `element.ts`'s per-bar lap block (so RECT stays byte-identical) and reused by every pipeline. It groups resolved placed bars by zone id, runs `evaluateLapStagger` (EC2 §8.7.2: ≤ ½ of a zone's bars lapping within one 0.3·l0 window → PASS, else WARN), and returns the lap extents that feed the seismic `lap_in_critical_zone` where an overlay exists.
- **`pipeline/element.ts`** — the ~48-line inline per-bar block replaced by a 3-line call to the shared helper (same items, same site, feeding the same `lapExtents` into the seismic overlay). Dropped the now-unused `evaluateLapStagger` import.
- **`pipeline/circular.ts`** — `CircularLongInput` gains `splices?`/`autoSplice?`; the long-zone loop splices off `shape.cutLength`, attaches `SolvedGroup.splice`, and a new group-level `lap_stagger:<zone>` WARN loop runs after validation; the placed-bar path calls `analyzePlacedBarLaps` for its per-bar stagger.
- **`pipeline/{slab,stair,joist}.ts`** — each calls `analyzePlacedBarLaps` on its resolved placed bars (gated on `longBars`, so legacy byte-identical); NO native-zone splice threading (distribution stays whole).
- **Adapter** (`apps/web/src/engine/document.ts` + `solveDoc.ts`) — `ZoneEdit` gains `splices?`/`autoSplice?`; `circularInput` threads them onto the pile/column longitudinal zones. `.rcfg` round-trips for free (rides `meta.app_document`, additive — D-P0-2).

**The BBS did NOT need touching** — it already schedules a group `splice` (`bbs.ts:171–186`) and a per-bar `PlacedLongBar.splice` (`:206–221`) as segments + a `couplers` tally; M5 just makes the circular groups/placed bars carry a `splice`.

**Proven end-to-end.** `tests/splice_reach.spec.ts` (9, core-level via `solveCircular`/`solveSlab` + `computeBBS` + `placeBars`): an 18 m pile auto-splits its native cage into 2 segments (Σ cuts = run + l_r, count × 12, each < stock) + the group stagger WARN; explicit couplers tally (`couplers = 1×12`); a free bar splices on a pile **and** a slab while the mat stays whole; a lone free lap = stagger PASS, three coincident laps under one id = WARN; a default slab has no splice/coupler/stagger and `longBars === undefined` (byte-identical).

**Gate (green).** `npm run check` ✓ **756 / 143** (+1 file, +9 tests over M4's 747/142). `coverage` core **96.59 %** stmts / **99.4 %** functions / 96.59 % lines (≥ 90; ↑ from 96.55). `coverage:adapter` **97.06 %** (no regression). `build:web` ✓. **⚠ Windows-box caveat:** the raw `npm run coverage` exit is polluted by a **pre-existing** perf-timing flake (`perf_heavy.spec.ts:199`, the M3 140-bar row/bundle budget) — coverage instrumentation inflates timing here; that file passes cleanly uninstrumented (407 ms) and under `npm run check`, and the % above is from a run excluding only that timing test. **Not an M5 regression.** No golden moved.

**Flags / handoff for the next turn.**
1. **Seismic × circular is a pre-existing gap, NOT closed by M5.** The RPS overlay is composed on the RECT column/beam pipeline only (D-P4b-2), so `lap_in_critical_zone` applies to RECT splices (unchanged) but is inherently **N/A on the circular pipeline** — a spliced spiral-column cage in a seismic zone gets the stagger WARN but no critical-zone lap check. Wiring a seismic overlay onto E-COL-02 is its own decision (owner-gated), out of Track-S scope. I did NOT collect circular lap extents (no consumer).
2. **Stock length 12 000 mm is PROVISIONAL** (`DEFAULT_STOCK`, `resolvePlacedBars.ts`) — `[OWNER-DEP §C]`, tracked; the lap length rides the provisional BAEL/EC2 `code.l0` (G-BAEL/G-EC2).
3. **No new UI surface** — M5 proves the engine/model/schedule headlessly (spec §10: reach is v1.0.6). The `ZoneEdit.splices`/`autoSplice` adapter fields exist but no Sidebar control writes them yet; a pile-cage auto-split toggle is a v1.0.6 form/drawing-board item (like the existing column/beam `SpliceEditor`).
4. **Amer's pre-existing `beamLayoutBars ↔ engine-layout` mismatch** (M1 flag 1) is untouched — still out of core-placement scope.
5. **Not pushed** — handing back for the next turn (Phase 6 / M6, Track O, owner-gated on the façonnage code table). M5 is uncommitted in the working tree.

### 2026-07-10 — v1.0.5 **Phase 4 (M4) — validation completeness + honest tiers (Track V)** (V-A…V-F) — **CODE DONE, tested, green — NOT pushed** — by **Zayd** (Hetzner dev box)

**Branch.** Continued on `1.0.5-&-up`. Local `HEAD == origin/1.0.5-&-up == c38cd3f` (0/0), with the **M2 + M3 work already in the working tree, uncommitted** (Zayd's prior sessions; the owner controls when this branch is committed/pushed). Picked up mid-flight: read `cross_projects_policy.md` + `core_logic.md` + `architecture_breakdown.md` + `current_state.md §1/§6/§7/§8/§9` + `v1.0.5_spec.md` (Part III) + `v1.0.5_impl_plan.md` (Phase 4), verified the M3 tree green as a baseline (`npm run check` exit 0), then implemented **Phase 4 ONLY** (M4 — Track V). Nothing from M5–M7.

**What M4 delivers.** M2/M3 gave the engine bundles / multi-layer / skin / curtailed placed bars that render + schedule + feed As/`d`. **M4 JUDGES them** with the three honest tiers (🔴 blocks export / 🟠 warns / 🟢 fine) and the **F1 guarantee** (a code-compliant design is never the first thing the user sees turn amber). It is pure-core + two codepack data files — no adapter/UI change, no golden moved.

**The one pure module** (`packages/core/src/validation/placedBarRules.ts`, `validatePlacedBarRules(placed, resolved, ctx, code)`). Reads the **placed-input union** (bundles/layers/skin rows — where `n`/`face`/`skin` survive) for the count/cover/skin/layer rules and the **resolved `PlacedLongBar[]`** (stations/anchorage/position) for curtailment + generic geometry. Wired into the RECT pipeline (`element.ts`, `includeGeometry:false` — RECT already runs the richer `validateAddressableBars`) and **all 4 generic shims** (`circular/slab/stair/joist`, `includeGeometry:true`). Legacy-safe: fires only on real placed content (RECT gate `hasUser && longBars`; generic gate `longBars && input.placed`).

**The rules.**
- **V-B bundle** — `bundle_max`: `n > code.bundleMax` (4), or `> bundleMaxAtLap` (3) when the bundle laps (`splices`/`autoSplice`) → 🔴; at/under → 🟢 (a discrete hard limit, no band). `bundle_cover`: the equivalent Ø `φₙ = code.bundleEquivDiameter` must keep the cover → 🔴 out of concrete / 🟠 approaching (radial for CIRCULAR).
- **V-C skin** — `skin_minimum`: `code.skinReinforcement({b,h})` decides the depth trigger; provided per-face skin (from the `BarRow{skin}` set, governing = the weaker of L/R) < the per-face min, OR absent-where-mandatory → 🟠 **judgement (never blocks export)**; not required at this depth → no item.
- **V-D layers** — `layer_clear_spacing`: between-layer clear = `layerGap` vs floor `max(Ø,20)` (the M3 default `layerGap=max(Ø,20)` meets it **exactly → 🟢**, F1-clean); below → 🔴 clash / 🟠 tight. (Note: the aggregate term `dg+5` is **not** folded into the vertical floor — a deliberate G-TOL provisional choice so the M3 default doesn't false-fail; flagged.)
- **V-E curtailment** — `curtailment_anchorage` (§7.7 via `code.lbd`): for each end that stops **inside** the member, a bare `none` cut develops nothing → 🔴; else the bar's run must cover the summed end development (`straight` = `l_bd`, `hook` = `l_bd·curtailmentHookedFactor`) → 🔴 short, 🟠 marginally short, 🟢 adequate. A full-length / member-end-to-member-end bar → no item (so a default beam is untouched).
- **V-A geometry** — `placed_axial_extent` (bar within `[0, memberLength]`) + `placed_section_bounds` (bar inside the concrete/cover envelope — **radial for CIRCULAR**, rectangular for RECT/SLAB) generalised to the generic pipelines. Both always emit a PASS/FAIL confirmation when there are live placed bars.
- **V-F** — the band shape is `tierByMargin(provided, required, band)`: PASS at `provided ≥ required`, WARN in `[required·(1−band), required)`, FAIL below. So **at exactly the code value = 🟢**, always. Band widths from `code.warnBands` (G-TOL provisional).

**Affected-bar id (audit W4).** Each FAIL emits the offending **resolved placed-bar id(s)** in `affectedGroupIds` — a bundle/layer reddens all its `${parentId}#i` members (built from a parent→member-ids map over the resolved bars), a single/curtailed bar exactly one. This flows through `placeBars` (`groupId = lb.groupId`) → `rebarProps.failingGroupIds` **unchanged** — a standalone placed bar's `groupId` is already unique, so **no `rebarProps.ts` change was needed** (the spec's W4 concern was base-bar overrides reddening a whole group; free/placed bars already carry per-bar ids).

**Correctness fix carried (important).** A legitimate **bundle**'s members are deliberately touching (centre-to-centre = Ø), so the M3 `addressable_clear_spacing` check was firing a **spurious 🔴** on every bundle (verified live before the fix). M4 exempts intra-bundle pairs: `resolvePlacedBars` now tags each resolved bar with `placedParentId`/`placedKind` (new optional fields on `PlacedLongBar`); `element.ts` sets a `bundleId` on the `AddressableBarView`; `predicates.ts finishSpacing` skips a pair sharing a `bundleId`. A bundle-vs-**other**-bar spacing is still judged (using bare Ø; the φₙ-based bundle-to-neighbour clear spacing is a noted refinement — the `bundle_cover` envelope check is the shipped V-B geometry rule).

**New pack extras** (BAEL + EC2, all sourced in `*-constants.json` flagged `_provisional`, ⚠ G-BAEL/EC2/TOL): `bundleMax`/`bundleMaxAtLap` (EC2 §8.9 — 4 / 3-at-lap), `skinReinforcement({b,h}) → {required, minAreaPerFaceMm2, maxSpacingMm}` (EC2 §7.3.3/§9.2.4 analogue — depth threshold **1000 mm**, per-face min **0.1 %·b·h**, spacing **≤ 300 mm** — all PROVISIONAL, owner/engineer to ratify), `curtailmentHookedFactor` (BAEL 0.4 / EC2 0.7). Core `CodePackExtras` mirrors them as optional so the engine stays pack-agnostic (absent → sensible fallback).

**End-to-end proof (not just unit).** `tests/placed_bar_validation.spec.ts` (23 tests): one case per rule at 🔴/🟠/🟢; the **F1 guarantee asserted for every band** (bundle cover, layer spacing, curtailment — an at-limit design is 🟢) + across **BAEL and EC2**; the **affected-bar id** (a bundle FAIL → `[BND#0…]`, a curtailment FAIL → `[CB]`, a slab-geometry FAIL → `[P1]`); the **bundle clear-spacing correctness fix** (a well-placed triple bundle is 🟢 overall); V-A generalised to a **slab** (a non-RECT element); and **legacy-none** (a plain column/beam/slab carries none of the 7 new rules). Drove RECT (column/beam) + a generic slab through `solveElement`/`solveSlab`.

**Gate (2026-07-10).** `npm run check` ✓ — **747 tests / 142 files** (+1 file = `placed_bar_validation.spec.ts`, 23 tests; the M3-recorded baseline was 722/141 — the small extra delta is pre-existing tree count, I added only the one test file). `npm run coverage` core **96.55 % (exit 0)** (↑ vs M3's 96.23 — the new module is well covered). `npm run coverage:adapter` **97.06 % (exit 0)** (no regression — M4 touches no adapter). `npm run build:web` ✓. **Run the two coverage jobs SEQUENTIALLY** (shared `coverage/.tmp/` — parallel = false threshold-fail). Only source touched: `validation/{index,predicates,placedBarRules}`, `pipeline/{element,circular,slab,stair,joist}`, `section/resolvePlacedBars`, `types` (via `PlacedLongBar`), `codepacks/{bael,ec2}/{index,*-constants.json}`, core barrel + 1 new test file. **Purity held; no `if(elementType)`/`if(codePack)`; NOT pushed** (owner controls push).

**Flagged for owner / Amer (non-blocking).**
1. **All new numeric limits are PROVISIONAL and ride the engineer gates** (G-BAEL/EC2 for bundle max/φₙ, skin trigger+min, curtailment development; **G-TOL** for the band widths). The **skin trigger depth (1000 mm), per-face min (0.1 %·b·h), max spacing (300 mm)** and the **between-layer floor `max(Ø,20)` (dg not folded in)** are the judgement calls most worth a structural engineer's eye — they sit in `*-constants.json` for easy ratification.
2. **Bundle-to-neighbour clear spacing uses bare Ø, not φₙ** — the `bundle_cover` envelope check is the shipped V-B geometry rule; a φₙ-based clear-spacing-to-adjacent-bars predicate is a noted refinement (the M3/M4 flag chain).
3. **Generic (non-RECT) placed bars now get GEOMETRY + bundle/layer/curtailment tiers, but still do NOT feed per-metre As/§7** (unchanged from M2/M3 — RECT As/`d` reconciliation is full; extending the As-credit + slab/circular spacing predicates to the placed bars on generic elements is the remaining slice of Track V-A, deferrable). A `Layer` on a CIRCULAR section is still skipped (no face — M3 flag).
4. **Amer's M1 flag 1 (`beamLayoutBars` SUM vs MAX `nTop` cross-tie mis-bind)** remains untouched — out of Track-V scope, still open for a dedicated audit.
5. **No UI yet** — per the spec, v1.0.5 proves the engine/model/validation/schedule/file headlessly; the drawing-board UI to place + see these verdicts is **v1.0.6**. The owner-GPU visual passes (M1-M3 render) still stand.

**Next (→ Phase 5 / M5, `v1.0.5_impl_plan.md` §Phase 5).** Splice / coupler reach (Track S): thread `splices?`/`autoSplice?` into the **circular/pile** pipeline (long cages > 12 m auto-split at PROVISIONAL 12000 stock, `[OWNER-DEP §C]`); let **any** `PlacedBar` carry per-bar splices (the `barSplice` helper in `resolvePlacedBars` already exists → segments + coupler tally in `bbs.ts`, `evaluateLapStagger` + seismic `lap_in_critical_zone` apply); **explicitly skip** slab/mesh distribution steel. Depends on M2 only; ends green with `tests/splice_reach.spec.ts`.

### 2026-07-10 — v1.0.5 **Phase 3 (M3) — rows / layers / bundles / skin + coupe fidelity** (P-C…P-G) — **CODE DONE, tested, green — NOT pushed** — by **Zayd** (Hetzner dev box)

**Branch.** Continued on `1.0.5-&-up`. Local `HEAD == origin/1.0.5-&-up == c38cd3f` (0/0), with the **M2 work already in the working tree, uncommitted** (the owner confirmed M2 was done in Zayd's previous session). So I picked up mid-flight: verified the M2 tree is green (`npm run check` exit 0, 701/136) as a baseline, read `cross_projects_policy.md` + `core_logic.md` + `architecture_breakdown.md` + `current_state.md §1/§7/§8/§9` + `v1.0.5_spec.md` + `v1.0.5_impl_plan.md`, then implemented **Phase 3 ONLY** (M3 — Track P's P-C/P-D/P-E/P-F + P-G). Nothing from M4–M7.

**What M3 delivers.** The four detailer capabilities beyond single bars, each drawn faithfully in the coupe: **counted/spaced rows (P-C), multiple flexural layers (P-D), bundles (P-E), side-face skin (P-F), and coupe fidelity for all of them (P-G).** It rides entirely on the M2 seams — no new placement machinery, just an expander + wiring.

**The load-bearing insight.** A row/bundle/layer is **N `SingleBar`s that share one body**, and the BBS merge key is already **position-independent** (`shape|Ø|cut|legSig`). So the whole phase reduces to: extend the `PlacedBarInput` union, expand each new kind to `SingleBar`s in `resolvePlacedBars`, and let the existing `resolveSingleBar` → `PlacedLongBar` → `placeBars`/`computeBBS`/`sectionAt`/A2-reconciliation pipeline do the rest. Identical bars merge to ONE mark with the real count; distinct positions render/coupe distinctly; the A2 reconciliation credits the steel to the nearest zone.

**P-A — union extension** (`types/placed-bar.ts`). Extracted a shared `PlacedBarBody` (shape/params/Ø/hooks/role/curtailment/splice/removed); `SingleBar` now `extends` it (byte-unchanged). Added **`BarRow`** (anchor + `direction` "u"/"v" + `extent` + `count` OR `spacing` + `skin?`), **`Bundle`** (`position` + `n`), **`Layer`** (`face` + `layerIndex` + `count` + `inset` + `span` + `layerGap?`). `PlacedBarInput = SingleBar | BarRow | Bundle | Layer` (additive → forward-compat, D-P0-2) + `isBarRow`/`isBundle`/`isLayer` guards.

**P-B/C/D/E/F — one expander** (`section/resolvePlacedBars.ts`). `resolvePlacedBars` now `expandPlaced`s each input to `SingleBar`s, then resolves each with a sequential index: a **row** spreads `count` evenly over `extent` (or steps by `spacing` → `floor(extent/spacing)+1`); a **bundle** lays `n` touching bars centred on `position` (centre-to-centre = Ø); a **layer** places `count` bars across `span`, `layerIndex` steps (`Ø + (layerGap ?? max(Ø,20))`) inboard of the face (uses the new `ctx.sectionDims`). Every expanded bar keeps a derived id `${parentId}#i` and goes through the unchanged `resolveSingleBar` (curtailment clip + splice + geometry).

**RECT As/`d` wiring** (`pipeline/element.ts`). `ElementSolveInput.placed?: PlacedBarInput[]` joins `extraBars` in `buildLongBars`'s free-bar resolve, passing `sectionDims:{b,h}`. `hasUserAddressableContent` now also true when `placed` present → the **A2 reconciliation** runs, so rows/bundles/layers feed As,prov + the **area-weighted `d`** (a 2nd bottom layer lowers `d` **exactly** — verified `d = (n₁·d₁ + n₂·d₂)/(n₁+n₂)`). Generic shims (`slab/stair/joist`) pass `sectionDims` (`b=Ly/width/flight_width, h=t`) so layers place there too; **circular has none → a face layer is skipped** (flagged).

**Codepack** (`bael`, `ec2`, core `CodePackExtras`). New `code.bundleEquivDiameter(φ,n) = min(55, φ·√n)` (EC2 §8.9 / BAEL, ⚠ PROVISIONAL — G-BAEL/EC2). M3 ships the FUNCTION; the rules that consume it (bundle >4 / cover-breach 🔴, skin depth-min 🟠, between-layer clear spacing) are **Track V / M4**.

**Adapter** (`document.ts`, `solveDoc.ts`). A doc-level **`PlacedBarDoc`** union (`AddressableBar` = single | `PlacedRowDoc` | `PlacedBundleDoc` | `PlacedLayerDoc`, `kind`-discriminated) + a shared `placedDocBody` mapper (reuses the H11/H2/P2/B1 conventions of `buildExtraBars`). `buildPlacedBars` emits the core union; `GenericDoc.placed` broadened + `ColumnDoc`/`BeamDoc` gain `placed?` threaded via a `placedFor(...)` spread. **`.rcfg` round-trips for free** (rides `meta.app_document`, additive — verified lossless for row+bundle+layer).

**P-G coupe fidelity.** Automatic (every kind becomes a `PlacedLongBar` that `sectionAt` already reads): proven a **bundle** = n touching dots at its `(u,v)`, a **layer** = a distinct row at its offset depth, a **skin row** = side-face dots, and a **curtailed bar** present only at a station it runs through (absent at a station it doesn't). `center.s` = section u, `center.t` = section v.

**End-to-end proof (not just unit).** `tests/bar_row.spec.ts` (row→N bars, one merged mark count N, count↔spacing equivalence, As += N·area), `tests/layers.spec.ts` (2nd layer lowers `d`; exact area-weighted mean; single-layer byte-identical `longBars` undefined), `tests/bundle.spec.ts` (3 touching, one mark count 3, As=3·area, `bundleEquivDiameter` = φ√n capped 55 on BAEL+EC2), `tests/skin_steel.spec.ts` (both side faces render+schedule, As credited), `tests/coupe_placed_bars.spec.ts` (P-G). `apps/web/.../placed_bars.spec.ts` +2 (doc-level row/bundle/layer render+schedule+`.rcfg`). `perf_heavy` +2 (20 rows-of-4 + 20 triple bundles = 140 bars solve + place < 16 ms).

**Gate (2026-07-10).** `npm run check` ✓ — **722 tests / 141 files** (+21 tests / +5 files over M2's 701/136, all new, **no golden snapshot moved**). `npm run coverage` core **96.23 % (exit 0)**; `npm run coverage:adapter` **97.06 % (exit 0)** (↑ vs M2's 96.95). `npm run build:web` ✓ (31 s). ⚠ **Gotcha discovered:** the two coverage jobs share `coverage/.tmp/`, so running `coverage` + `coverage:adapter` **in parallel** clobbers each other → a false 88 % threshold-fail on one + an `ENOENT coverage-42.json` crash on the other. **Run them sequentially** (or just trust `npm run check`). Only source touched: `types/{index,placed-bar}`, `section/resolvePlacedBars`, `pipeline/{element,slab,stair,joist}`, `validation/index`, `codepacks/{bael,ec2}`, `web/{document,solveDoc}` + 5 new core tests + 1 web test + `perf_heavy`. Purity held; no `if(elementType)` added. **NOT pushed** (owner controls push).

**Flagged for owner / Amer (non-blocking, do NOT block M4):**
1. **The new steel is placed but not yet VALIDATED — that is M4 (Track V) by design.** Rows/bundles/layers/skin render + schedule + feed As/`d` + show in the coupe, but the honest tiers (bundle >4 / >3-at-lap 🔴, `φₙ` cover/spacing breach 🔴, skin depth-min 🟠, between-layer clear spacing, curtailment/anchorage bands) and the affected-bar-id red are **not** implemented here. `code.bundleEquivDiameter` is the one new numeric input, provisional (G-BAEL/EC2).
2. **Generic (non-RECT) placed bars still do NOT feed As/validation** (unchanged from the M2 flag). On a slab/circular/stair/joist a row/bundle renders + schedules + coupes, but its steel doesn't yet enter the per-metre As or the tiers — Track V-A ("extend the predicates to all elements"). RECT (column/beam) As/`d` is full.
3. **A `Layer` on a CIRCULAR section is skipped** — a face `Layer` needs `sectionDims` + a face convention that a pitch-circle doesn't have. Rows/bundles work on circular (section-agnostic); layers are a flexural-face concept. Flagged, not a regression.
4. **The skin `BarRow` supersedes the SKIN-supplement fanout for NEW detailing, but I did NOT rip out the legacy supplement path** (`resolveSupplement` SKIN + its 3D anchoring, B3) — both coexist; consolidating them is a follow-up, not required by the spec's P-F ("fold the fanout into the row model" — the row model now exists and is first-class).
5. **Amer's M1 flag 1 (`beamLayoutBars` SUM vs MAX `nTop` cross-tie mis-bind) is still untouched** — a web-adapter cross-tie concern, out of M3's core-placement scope. Still open for a dedicated audit.

**Next (→ Phase 4 / M4, `v1.0.5_impl_plan.md` §Phase 4).** Validation completeness + honest tiers (Track V): generalise the element-agnostic predicates (axial-extent + in-concrete-bounds) to all elements; emit the offending **placed-bar id** in `affectedGroupIds` (`rebarProps.ts:32–36` reds one bar); bundle rules (`code.bundleEquivDiameter` now exists), skin minimum, multi-layer clear spacing, curtailment/anchorage bands; tolerance bands that sit **below** the code value (F1 — a code-compliant design never opens amber). The M3 seams are in place: the `PlacedBarInput` union + resolved `PlacedLongBar`s carry the section geometry each predicate needs; `code.bundleEquivDiameter` is the bundle limit input.

### 2026-07-10 — v1.0.5 **Phase 2 (M2) — canonical `PlacedBar` model + shared placement pass on all 8 elements** (P-A + P-B) — **CODE DONE, tested, green — NOT pushed** — by **Zayd** (Hetzner dev box)

**Branch.** Continued on `1.0.5-&-up` from Amer's M1 (`c38cd3f`, pulled `--ff-only`; his M1 entry below was the newest §9 — sanity check passed). Read `core_logic.md` + `architecture_breakdown.md`, `current_state.md` §1/§7/§8/§9, then `v1.0.5_spec.md` + `v1.0.5_impl_plan.md`. Implemented **Phase 2 ONLY** (M2 — Track P's P-A + P-B). Nothing from M3–M7.

**What M2 delivers.** The engine now has ONE first-class placed-bar unit resolved by ONE shared pass wired into **all 8** pipelines — so a **free bar (any single bar, any position) renders + schedules + appears in the coupe on every element**, not just column/beam. It widens the seams M1 left (`startStation`/`endStation`/`anchorage`, `hasUserAddressableContent`) into a canonical model without moving a golden.

**P-A — the canonical `PlacedBar` model** (`packages/core/src/types/placed-bar.ts`, new). `SingleBar` = the first-class single placed bar (stable `id`, `(u,v)`, shape+params+Ø, curtailment `start/endStation`, per-end `anchorage`, `splices`/`autoSplice`, `removed`, an optional `overrideBarIndex`). `PlacedBarInput = SingleBar` today; M3 extends the union with `BarRow`/`Bundle`/`Layer` (additive → forward-compat, D-P0-2). Moved `EndAnchorageChoice`/`BarEndAnchorage` here (the data contract belongs in the types layer); `element.ts` now imports them (no re-export → no double-export through two barrels; the `@rebarconfig/core` surface is unchanged, verified by the green web typecheck).

**P-B — the shared placement pass** (`packages/core/src/section/resolvePlacedBars.ts`, new) is the **single long-steel truth** for free bars. It owns the per-bar primitives `clipToStations` (curtailment) + `barSplice` (moved out of `element.ts`) and exports `resolveSingleBar`/`resolvePlacedBars`. `element.ts buildLongBars` delegates its extra-bar loop to it — the `ExtraLongBar`→`SingleBar` conversion is byte-preserving (same shape gen + curtail + splice → identical `PlacedLongBar`; every M1 golden held). The RECT base-bar reconstruction (a face's bars distributed across its zones, the two-chapeau expansion) **stays** in `buildLongBars` (the RECT section-aware resolver the plan lists). The four generic shims (`circular/slab/stair/joist`) each gained `placed?: PlacedBarInput[]`, call the shared pass with their native `layoutBars` + `groups`, and **append** the resolved `longBars` + set `hasUserAddressableContent`.

**Additive `placeBars` + `computeBBS` (the key correctness move).** Both used to treat `longBars` as an all-or-nothing REPLACEMENT of the grouped long steel. Now they are additive: a longitudinal base bar/group is skipped (replaced by `longBars`) **only when it is actually represented there** — a non-standalone `PlacedLongBar` carrying its `barIndex` (placeBars) / `groupId` (BBS). A **RECT** element replaces every base bar (each has a non-standalone twin → fully covered → the additive loop emits nothing → **byte-identical**, all RECT goldens held). A **generic** pipeline KEEPS its base mat in `bars[]` and appends only FREE (standalone) bars → the base mat renders/schedules from `bars`/`groups` as before + the free bars are added. Extracted `placeGroupedLongBar` so the grouped render is shared by the pure path and the additive path.

**B3 — two placement modes (section-sensitivity).** A free `(u,v)` bar resolves **section-agnostically** (same position on any section). An `overrideBarIndex` bar resolves **section-aware** — its position comes from `ctx.layoutBars[idx]` (the section's own bar convention). `resolve_placed_bars.spec` proves the split (the same override input lands on different positions for a RECT vs a CIRCULAR layout).

**Adapter** (`solveDoc.ts` + `document.ts`). `GenericDoc` gained `placed?: AddressableBar[]`; new `buildPlacedBars` maps it → `SingleBar[]` (mirrors `buildExtraBars`) and `placedInput(doc)` threads it into all 4 generic input builders. Column/beam free bars already ride the `extraBars` channel (RECT). `.rcfg` round-trips for free (rides `meta.app_document`, additive — verified).

**End-to-end proof (not just unit tests).** `tests/placed_bars_all_elements.spec.ts` drives all **8** elements through `solve* → placeBars → computeBBS → sectionAt(defaultCoupeFor)`: for each, a distinctive Ø32 free bar (a) appears in `placeBars`, (b) schedules as its own BBS line, (c) shows as a section circle in the default coupe — AND every base rendered bar is byte-identical (nothing displaced), a no-placed doc keeps `longBars===undefined`. Plus `tests/resolve_placed_bars.spec.ts` (free vs section-aware override, curtailment shortens run+cut, per-bar splice segments), `apps/web/.../placed_bars.spec.ts` (adapter slab free bar + `.rcfg` round-trip), `perf_heavy` extended (a 60-free-bar slab solves + places < 16 ms).

**Gate (2026-07-10).** `npm run check` ✓ — **701 tests / 136 files** (+30 tests / +4 files over M1's 671/132; all new, **no golden snapshot moved**). `npm run coverage` core **96.21 % (gate exit 0)**. `npm run coverage:adapter` **96.95 %** (↑ vs the 96.76 baseline — the new adapter path is covered; no regression). `npm run build:web` ✓. Only source touched: `types/{index,placed-bar}`, `section/{index,place,resolvePlacedBars}`, `pipeline/{element,circular,slab,stair,joist}`, `exporters/bbs.ts`, `web/{document,solveDoc}` + the 3 new test files + `perf_heavy`. Purity held (core is framework-free); no `if(elementType)` added (dispatch stays section/profile-based). **NOT pushed** (owner controls push).

**Flagged for owner / Amer (non-blocking, do NOT block M3):**
1. **Generic free bars are placement-only in M2 — they do NOT yet feed As/validation.** A free bar on a slab/circular/… renders + schedules + shows in the coupe, but its steel does not yet enter `As,prov`/`d` or the validity tiers. That is **Phase 4 (Track V-A)** — "extend the addressable predicates to all elements" — by design (M2 = the placement model; M4 = honest validation of it). RECT As reconciliation is unchanged (still runs on real user overrides/extras via `hasUserAddressableContent`).
2. **Section-aware override-onto-an-existing-bar on the generic pipelines is deferred to M3.** Free placement is complete on all 8; the resolver supports + unit-tests the override mode, but the generic shims emit **free** bars only. Reason: on a per-metre slab/circular, an override that "covers" a base group would make `computeBBS` skip the whole per-metre group (coverage is per-group) — the correct semantics need the row/layer model (M3, P-C/P-D). RECT overrides work fully (via `buildLongBars`).
3. **Amer's M1 flag 1 (`beamLayoutBars` SUM vs MAX `nTop` cross-tie mis-bind) is NOT addressed here** — it is a web-adapter cross-tie concern, out of M2's core-placement scope. Still open for a dedicated audit.
4. **Naming (worth knowing):** three distinct `*PlacedBar*` concepts coexist — `PlacedBarInput`/`SingleBar` (the input model, `types/placed-bar.ts`), `PlacedLongBar` (the resolved per-bar output on `SolveResult.longBars`, `pipeline/element.ts`), and `PlacedBar` (the world-space render polyline, `section/types.ts`). Kept the render name as-is to avoid churn.

**Next (→ Phase 3 / M3, `v1.0.5_impl_plan.md` §Phase 3).** Rows / layers / bundles / skin + coupe fidelity. Extend the `PlacedBarInput` union (`BarRow`/`Bundle`/`Layer`) + `resolvePlacedBars` (each resolves to N `SingleBar`s / an offset layer / a bundle count); activate `RectLayout.layers`; add `code.bundleEquivDiameter`. The M2 seams are in place: the `PlacedBarInput` union, `resolvePlacedBars`'s per-section ctx, the additive `placeBars`/`computeBBS`, and the generic `placed?` channel.

### 2026-07-10 — v1.0.5 **Phase 1 (M1) — the placement defects D1/D2/D3 are killed** (P1a/P1b/P2a/P2b/P2c) — **CODE DONE, tested, green — NOT pushed** — by **Amer** (owner's local Windows PC)

**Branch.** Pulled `origin/feat/p1-m1-engine` (@ `1204020`, "created 1.0.5/6 spec docs") and branched **`1.0.5-&-up`** off it. First v1.0.5 turn; taking phases turn-by-turn with Zayd. Read `core_logic.md` + `architecture_breakdown.md`, `current_state.md` §1/§7/§8/§9, then `v1.0.5_spec.md` + `v1.0.5_impl_plan.md`. Implemented **Phase 1 ONLY** (M1 — the three confirmed placement defects). Nothing from Tracks P/V/S/O/E (M2–M7 are Zayd's / later turns).

**P1a — single placement path (fixes D1 + D2).** `solveElement` now emits the explicit per-bar `longBars[]` for ANY element whose faces carry **>1 longitudinal zone** (new pure `facesCarryMultipleZones` in `pipeline/element.ts`), not only on user overrides. A default two-chapeau beam (TOP face = montage + left + right chapeau) therefore takes the single per-bar path and draws **BOTH** chapeaux at their true stations — the left at `axisStart 0`, the right at `L − extension` (was: the grouped fast path collapsed every top-face bar onto `longGroups[1]` = the left chapeau, so only the left half drew — D1 — and the right appeared only as a side-effect of the addressable panel — D2). `place.ts` was **not** touched: its grouped branch is now reached only by single-zone-per-face elements (columns, slab main mats), which stay **byte-identical**. `buildLongBars` distributes the section's bars across the face's zones and emits any under-seated zone (the second chapeau) as before, so both chapeaux merge in the BBS to the same `count` the grouped path produced → **BBS byte-identical** (verified). A base bar on the forced path now **inherits its zone's group-level splice** so a spliced beam still schedules its segments on the per-bar path.

**P1b — decouple placement from validation activation (audit A2).** Added a pure `hasUserAddressableContent(input)` (TRUE only for real user overrides / extra bars). The As/`d` **reconciliation**, the `placedCount`/`face_min_bars` path, the per-bar **splice/stagger** block, and `validateAddressableBars` are now gated on **it**, NOT on the bare presence of `longBars`. A new `SolveResult.hasUserAddressableContent` flag drives `sectionAt.defaultCoupeFor`'s station-aware seed. Net: a default multi-zone beam gets correct **placement** while its **validation list + default coupe station stay byte-identical** (proven by `tests/placement_validation_decouple.spec.ts` + every existing beam validation golden holding).

**P2 — per-bar curtailment + runs-through (fixes D3).** **Removed** the inert `continuedToSupport` scalar everywhere (core `ElementLongInput`/`SolvedLongZone`, `BeamDoc.span`, the adapter, the dead `Sidebar` NumberField). Replaced with per-bar **`startStation`/`endStation`** curtailment + a per-end **`anchorage`** choice on `LongBarOverride`/`ExtraLongBar`/`PlacedLongBar` (+ the web `BarOverrideEdit`/`AddressableBar`). A curtailed bar's run is clipped to `[start,end]` and its cut length follows (new `clipToStations` in `buildLongBars`, mirroring H2's leg inversion). The §7.7 `end_support_anchorage` now counts the **`runsThroughAsProv`** — the As of bars whose real geometry reaches BOTH member ends — instead of a fraction (`profiles.ts`); no per-bar list ⇒ falls back to the full As,prov ⇒ byte-identical PASS on a plain span. A curtailed bar is **shorter in the drawing + the BBS cut length** (end-to-end proof: a curtailed-bar beam schedules a distinct shorter line and weighs **77.8 kg vs 81.8 kg** for the full beam).

**Migration (P2c).** `migrateDoc` gained `migrateBeamContinuation`: a legacy `span.continuedToSupport = f` maps to per-bar curtailment — the `round(f·nBottom)` **innermost** bottom bars run through (no override), the rest curtail to `[inset, L−inset]` (⚠ **PROVISIONAL** `CURTAIL_INSET_FRACTION = 0.1`, G-BAEL — a geometric stand-in for the true moment-curtailment point). Idempotent + lossless. It keys off a NEW `engineBeamLayoutBars` helper that mirrors the engine's **`nTop = montage + max(chapeauL,chapeauR)`** layout — **NOT** `beamLayoutBars`, which uses the SUM and therefore yields different bar indices (see flag below).

**The one intentional re-baseline (annotated).** No golden **snapshot file** moved — the DXF/PDF/BBS goldens use a hand-built single-chapeau `referenceBeam` (single-zone-per-face → still grouped → byte-identical). The behaviour change surfaces only as **test-assertion updates**, each annotated `v1.0.5 P1`: a default beam now emits `longBars` (`faconnage_adapter_characterization`, `beam_supports`, `beam_support_precision`). The validation-list + default-coupe goldens **held** — the whole point of the P1b decouple.

**End-to-end proof (not just unit tests).** Drove `defaultBeamDoc` + a curtailed beam through **core → solve → adapter(`solveDoc`) → 3D(`placeBars`) → coupe(`sectionAt`) → BBS → DXF(`buildDxf1`) → PDF(`buildPdf`) → `.rcfg` round-trip(`docToRcfg`→`rcfgToDoc`→re-solve)**: both chapeaux placed 2/2, DXF/PDF bytes emitted, coupe circles/lines produced, `.rcfg` lossless (status PASS, `longBars` count preserved). New suites: `tests/beam_two_support_placement.spec.ts`, `tests/placement_validation_decouple.spec.ts`, `tests/curtailment.spec.ts`, `apps/web/src/engine/curtailment_migration.spec.ts`; reworked `tests/beam_curtailment.spec.ts` (the fraction-driven WARN moved to `curtailment.spec.ts`).

**Gate (2026-07-10).** `npm run check` ✓ — **671 tests / 132 files** (+14 tests / +3 files over 657/129, all new; no golden snapshot moved). `npm run coverage` core **≥90% (gate exit 0)** — the covered count is flaky under the heavy collect (91–96% across runs) but the per-file report is healthy: `element.ts` 97.84 %, `profiles.ts` 92.28 %, `place.ts` 90.58 %, `sectionAt.ts` 95.41 %. `npm run coverage:adapter` **96.91 %** (no regression vs the 96.76 % baseline — added `curtailment_migration` to cover the new migration/curtailment adapter code; a transient exit-1 in one run was a flaky three.js "Multiple instances" worker crash, clean on re-run). `npm run build:web` ✓ (1m16s). **NOT pushed** (owner controls push).

**Flagged for owner / Zayd (non-blocking, do NOT block M2):**
1. **`beamLayoutBars` ↔ engine layout mismatch (pre-existing).** `crossTies.beamLayoutBars` builds the beam layout with `nTop = montage + (chapeauL + chapeauR)` (the SUM), but the engine (`beamInput`) uses `montage + max(…)`. The two produce **different bar indices**, so **cross-tie bindings on a beam may reference a different section than the engine solves**. I side-stepped it for the curtailment migration with `engineBeamLayoutBars`, but the F2 cross-tie path still uses `beamLayoutBars`. Worth an audit (M2/owner) — not introduced by P1.
2. **Curtailment point is provisional (G-BAEL).** `CURTAIL_INSET_FRACTION = 0.1` (and the runs-through 2 % reach tolerance) are engineering stand-ins for the true moment-curtailment cut-off — ratification items, flagged.
3. **Per-bar curtailment UI is minimal (by design).** I removed the dead `continuedToSupport` control; per-bar `startStation`/`endStation` are **engine-complete + reachable via `barOverrides`**, but there is no dedicated "runs-through / curtail" affordance yet — consistent with spec §10 ("1.0.5 ships the capability + its proof; reach = 1.0.6"). A minimal toggle can land with the M-later UI or v1.0.6.

**Next (Zayd → Phase 2 / M2, `v1.0.5_impl_plan.md` §Phase 2).** The `PlacedBar` canonical model (P-A) generalises the P1 station fields; the shared `resolvePlacedBars` pass (P-B) wires per-bar placement into all 8 pipelines. M1's `startStation`/`endStation`/`anchorage` on `LongBarOverride`/`PlacedLongBar` + the `hasUserAddressableContent` flag are the seams M2 widens. No forward dependency on M1 beyond those fields.

### 2026-07-08 (review + fixes) — **Adversarial review of Amer's C1/D1/D2/H10/cover-WARN work + 3 fixes applied** — by **Zayd** (Hetzner dev box) — **CODE DONE, tested, green — NOT pushed**

**Scope.** Owner directed a general adversarial review of Amer's `1d1d161` (14 changed + 6 new files) vs parent
`b0658c6`, **then "make the needed fixes and update all relevant docs incl. owner_tasks."** Pulled `--ff-only` (Amer's
entry below is the newest §9 — sanity check passed). Method: `git diff` the actual code, then drove the **real adapter**
(`defaultDocFor → solveDoc`, not Amer's hand-built `solveSlab(...)` inputs) for all 8 default docs, plus compared each
disputed behaviour against the parent commit's code. Throwaway probes used + deleted. **Suite was green (657/129) —
treated as proof of nothing.** Three findings survived; **all three fixed** (below). New gate: **658 tests / 129 files**,
`npm run check` ✓, coverage core **95.11%** / adapter **96.76%** (no regression), `build:web` ✓. **NOT pushed.**

**Owner decision recorded (F1):** the cover comfort-band WARN was **reverted** — a code decision the owner delegated
via "make the needed fixes"; flagged as reversible (owner_tasks **B-5a**). **F2** moves the slab/stair distribution BBS
count intentionally (owner-directed correctness fix; ratification tracked as owner_tasks **B-10**).

**F1 [HIGH · CONFIRMED] — the DEFAULT column and beam now open AMBER "À vérifier"; §9 under-reported the cover flip.**
- Input → output: open the app's default **E-COL-01** or **E-BEM-01** (both ship cover = 30 mm, reqCover = 30 mm).
  New `coverItem`: `cover < req ? FAIL : cover < req·(1+0.1) ? WARN : PASS` → 30 < 33 → **WARN** → rolls up to overall
  **WARN** → stamps the drawing **"À vérifier / Review required"**. Verified against parent `b0658c6`: pre-change both
  were **PASS 🟢** (`cover < req ? FAIL : PASS` → 30<30 false → PASS). Clean **🟢→🟠 regression on the two flagship
  default docs** — the first thing every user sees.
- Why Amer's tests missed it: `all_elements.spec.ts` asserts only `status !== "FAIL"` (WARN passes), and the §9 entry
  reports "**one status flip** (`pack_swap` fixture)" — the far more important default-doc flip is not mentioned.
- Semantic concern (matches core_logic §4.2): cover *exactly at the code minimum* is **compliant** ("within the rules"),
  not "discouraged / near a limit / unusual". Making the code minimum amber inverts the tier philosophy — and it fires
  for **every** design sitting on a binding minimum (fire-rated, CAST_AGAINST_EARTH covers are usually AT their higher
  minimum), so it is systematically noisy, not a rare nudge.
- Proposed fix (owner to rule — this is a B-5/§7.12 semantics decision, not a pure bug): either (a) keep the FAIL line
  only and DROP the comfort-target WARN band (revert to FAIL/PASS), or (b) keep the band but do **not** let it roll up
  to the drawing stamp (comfort is advisory, not "À vérifier"), or (c) re-baseline the shipped default covers above the
  comfort target so the defaults open 🟢. Recommend (a) or (b): a code-compliant design should not open amber. If the
  band stays, its value + roll-up behaviour is **owner_tasks B-5** and must be signed (G-TOL).
- **✅ FIXED (2026-07-08, option a):** `coverItem` reverted to FAIL/PASS — cover at or above the durability+fire
  minimum is 🟢 PASS. Kept Amer's shared `coverItem` helper (the uniformity win: one cover rule across all validators),
  dropped the `band` param + the WARN branch. Verified live: all 8 default docs back to overall PASS / cover PASS.
  Tests: `cover_warn.spec.ts` → renamed `cover_tier.spec.ts` (at-minimum = PASS + FAIL/PASS uniform across column &
  beam); reverted the `pack_swap` BAEL c=30 assertion to PASS. Reversible per owner_tasks **B-5a**.

**F2 [HIGH · CONFIRMED] — default slab & stair: the drawing/3D shows MORE distribution bars than the BBS schedules;
the steel takeoff undercounts distribution steel.**
- Input → output (real default docs): **E-SLB-01** — `placeBars`/3D renders **20** across-width distribution bars
  (span Lx=5000 ÷ spacing 250); the BBS + shop-drawing bending table + steel summary schedule **count = 13**
  (`floor(Ly/spacing)+1`, width-derived) → Ø8 summary = 13 bars / 15.4 kg where the drawn geometry is ~20 bars /
  ~23.7 kg (**~35 % undercount**). **E-STR-01** — render **16** vs BBS **5** (**~69 % undercount**). The `steelRatio
  kg/m³` (core_logic §7 "standard sanity-check") is understated accordingly.
- This is the inconsistency Amer flagged, but the framing under-sells it: "BBS byte-identical / no golden moved" is
  literally true, yet the **DEFAULT** slab and stair now ship a drawing that **visibly contradicts its own schedule**,
  and the pre-existing distribution count was already the *wrong* number (width-derived, not span-derived). Net: C1
  correctly fixed the per-bar *length* (render now = cutLength = Ly) but converted a latent length inconsistency into a
  visible **count** inconsistency on the default docs. It is a lateral move with the real fix deferred.
- Rule note: the hard rule ("don't move a BBS/cutLength golden for a legacy doc") genuinely *forbade* Amer from
  changing the count unasked, so deferring was rule-correct. The gap is that this needs to be an **explicit owner
  decision item**, not just a §9 flag (see owner_tasks recommendation below), and the severity (default docs, ~35–69 %,
  drawing⇄schedule contradiction) should be stated plainly.
- Proposed fix (owner-gated fabrication-quantity change): reconcile the distribution `count` to the SPAN
  (`solveSlabDistributionBars` already emits the true set — set `group.count = that length`, i.e. `floor((span−2·margin)
  /spacing)+1`) so BBS/table/3D agree. This DOES move the slab/stair distribution BBS count golden → needs owner/
  engineer sign-off (owner_tasks, new C-item proposed).
- **✅ FIXED (2026-07-08):** `pipeline/slab.ts` + `pipeline/stair.ts` now set a linear DISTRIBUTION group's
  `count = solveSlabDistributionBars(...).length` (the span-distributed set actually placed), so the BBS, the bending
  table and the 3D/coupe agree. MAIN/TOP + mesh topping keep the width-derived representative count → unchanged.
  Verified live: default E-SLB-01 count 13→**20** (=rendered), E-STR-01 5→**16** (=rendered). This intentionally moves
  the slab/stair distribution BBS count (the existing slab tests only assert validation + `bars.length`, so no golden
  snapshot moved); ratification tracked as owner_tasks **B-10**. New assertions in `coupe_slab_exactness.spec.ts`.

**F3 [MED · CONFIRMED] — the joist topping WELDED MESH now draws spurious representative lines in the transverse coupe
(contradicts the "mesh topping stays byte-identical" claim).**
- Input → output: default **E-SLB-03** transverse coupe. Parent `b0658c6`: **0** topping-mesh lines. Amer's
  `sectionAt`: **5** `TM` lines. Root cause: the §9.5.3 "nearest-behind" guarantee was generalised to
  `for (const g of result.groups) if (g.role === "DISTRIBUTION") guaranteeGroups.add(g.groupId)` — **not gated by
  `isDistributionLinear`**, so it sweeps in the welded `TREILLIS_MESH` topping (role DISTRIBUTION) that the pipeline
  itself deliberately leaves representative. Amer's §9 says the mesh topping "keeps the representative panel /
  along-span render" and is byte-identical — the coupe is NOT byte-identical for the joist.
- Proposed fix (safe, self-contained, no golden should move for linear-distribution docs): gate the guarantee set with
  the same structural check the pipeline uses — add a DISTRIBUTION group to `guaranteeGroups` only when its shape is a
  discrete linear bar (reuse `isDistributionLinear(g.role, g.shape)`), excluding mesh/coil. Add a joist-coupe assertion
  so the 0→5 change can't recur silently.
- **✅ FIXED (2026-07-08):** `sectionAt.ts` now gates the guarantee set on `isDistributionLinear(g.role, g.shape)`
  (imported from `layout/slab`) instead of `g.role === "DISTRIBUTION"`, so a welded topping MESH is no longer swept in.
  Verified live: default E-SLB-03 topping-mesh coupe lines 5→**0** (back to the pre-C1 render). Regression guard added
  (`coupe_slab_exactness.spec.ts` F3 block: a mesh-topping slab yields 0 guaranteed lines for the mesh group).

**Threads pulled that came back CLEAN (credit to Amer):**
- **C1 geometry is physically correct**: frame is `member.length = Lx` (span, +Y), `member.b = Ly` (width, X); main
  bars run along the span, `across` distribution bars span `member.b` at Lx stations. The adapter (`solveDoc.ts:612`)
  already sets the distribution DROITE `L = Ly` (slab) / `flight_width` (stair) = `member.b`, so the **rendered length
  equals the cutLength** (the one thing C1 *did* fix). The default one-way slab's default coupe **does** reveal the
  distribution as a line (21 main dots + 1 dist line) — not only Amer's hand-built result.
- **`isDistributionLinear` / `meshFields` are honest structural (marker) checks, not disguised element-type branches**:
  the generated mesh shape really exposes `nWiresX`/`pitchX`/`pitchY` and the coil `coilLength`, so the guards match the
  data. Two-way mats (both MAIN) and joist topping (mesh) correctly stay representative in `placeBars`.
- **Float / determinism**: the `for (y += spacing)` loop is IEEE-754-deterministic (bit-stable cross-platform); the
  `1e-6` bound epsilon + the empty-set fallback handle spacing > span, spacing > width, 1-step, and huge spans without
  crash/empty (verified 300 mm and 1 000 000 mm spans, spacing 6000 > span).
- **H10 is correct**: `setHook` commits against `committed` (last-valid params) so a hook edit lands while a *param* is
  mid-invalid; `committed` equals the live buffer whenever valid, so no behaviour change on the happy path. Checked the
  neighbouring drop-paths (shape switch mid-invalid → `pickShape` re-seeds+validates; import/undo → `committedKey`
  resync effect wins). Minor pre-existing edge only: `pickShape` silently no-ops if a shape's default seed is invalid
  under the current hooks (H11 keeps defaults valid, so unreachable today).
- **D1/D2 are viewport-only, nothing leaked into core** (`steppedStair.ts`, `meshGridBars` live in `apps/web`): D1
  landing top aligns with the top tread (`topZ − waist/2`); D2 mesh grid degrades to empty (not an infinite loop) on
  NaN/degenerate `b`/`length` and is `pitchX/Y > 0`-guarded.

**owner_tasks corrections (applied):** (1) added **B-10** — the C1 distribution fabrication-**count** reconciliation
(F2) is now a tracked ratification item, not just a §9 flag; (2) added **B-5a** — the cover comfort-band *semantics*
(should code-minimum be amber / roll up to the stamp — F1) as a distinct owner decision from the numeric band width
(**B-5**); (3) annotated **D-4** that the joist-coupe line-count (F3) was a code bug (now fixed + guarded), not an
acceptance judgment. Also corrected the **spec** C1 paragraph (count now reconciled, no longer "representative /
flagged").

**Gate (after fixes):** `npm run check` ✓ — **658 tests / 129 files** (657→658: `coupe_slab_exactness` +3 F2/F3
assertions, `cover_tier` −2 vs the old `cover_warn`); `npm run coverage` core **95.11%** (≥90 ✓); `coverage:adapter`
**96.76%** (no regression); `build:web` ✓. Only source touched by the fixes: `validation/index.ts`,
`validation/profiles.ts`, `pipeline/slab.ts`, `pipeline/stair.ts`, `section/sectionAt.ts` (all core) + 3 test files.
Tree clean (probes deleted). **NOT pushed** (owner controls push).

### 2026-07-08 — v1.0.4 **C1 slab-coupe exactness + D1/D2 (3D fidelity) + H10 + §7.12 cover WARN tier** — **CODE DONE, tested, green — NOT pushed** — by **Amer** (owner's local Windows PC)

**Context.** Continued from Zayd's `b0658c6` (pulled `--ff-only`; sanity check passed — Zayd's B3-3rd/E1/E2 entry was the newest §9). Owner's task list: C1 slab exactness (the big remaining GEOMETRY re-model), then D1/D2 (viewport 3D fidelity), then the small fill-ins (H10 + cover WARN). All CODE-ONLY; owner/engineer/GPU items left alone. Everything self-gated → **no BBS/cutLength/coupe golden moved**; one validation-status assertion updated (cover, called out).

**C1 — slab-family coupe exactness (`coupe_slab_exactness`), the geometry re-model.** A slab / stair-waist DISTRIBUTION (secondary) LINEAR bar now runs ACROSS the width (world-X) at SPAN stations — its true cross-direction geometry — replacing the pre-C1 representative model (distribution fanned along the span like short main bars → a 2nd row of dots in the coupe).
- **Core:** new `BarPosition.across?`/`axial?` (`types/layout.ts`); new pure `solveSlabDistributionBars(span, spacing, v, faceTag)` + `isDistributionLinear(role, shape)` (`layout/slab.ts`) — a DISTRIBUTION bar that is a discrete linear bar (not a welded MESH, not a coil). `pipeline/slab.ts` + `pipeline/stair.ts` thread the SPAN (`Lx` / `n_steps·g`) into the distribution builder; MAIN/TOP bars + the joist/two-way topping MESH keep the along-span `solveSlabBars` (byte-identical). `place.ts` renders an `across` bar as a cross-width line `[-b/2, axial, v … b/2, axial, v]`. `sectionAt.ts` generalises the §9.5.3 "nearest set behind" guarantee (was transverse-only) to DISTRIBUTION groups, so the transverse coupe ALWAYS reads main bars as dots + the distribution layer as a line — at any cut station, not only when it lands on a bar.
- **Scope/gating:** provided steel stays PER METRE (spacing-driven — validation unchanged). A two-way slab (mats) / any non-slab element has `across` never set → byte-identical. The 3D viewport benefits for free (placeBars is shared).
- **⚠ FLAG (owner/engineer):** the **BBS distribution COUNT** is deliberately LEFT at the pre-C1 representative value (`floor(Ly/spacing)+1`, kept in `group.count`) — **BBS byte-identical, no golden moved** (hard rule: the sanctioned exception is render/coupe, NOT BBS quantity). So the faithful render now shows distribution bars distributed along the SPAN while the BBS count is still width-derived — a known, flagged inconsistency. **Reconciling the distribution fabrication count to the exact along-span geometry is a fabrication-quantity change and wants owner/engineer sign-off** (a follow-up slice, not this turn).
- **⚠ G-COUPE conventions stay PROVISIONAL** (owner_tasks §B-7, visual acceptance §D-4) — proceeded on defaults.
- **Test:** `tests/coupe_slab_exactness.spec.ts` (9 — main-dots/dist-line placement, across-width extent, span stationing, the coupe reads dots+line, the nearest-behind guarantee off-grid, two-way-mesh legacy no-op, stair distribution across the flight width, determinism). **NO golden moved** — the existing slab tests only assert validation + `bars.length>0`, so the expected re-baseline never materialised.

**D1 — stair 3D fidelity (viewport-only).** Extracted the stepped-stair box geometry into the PURE `apps/web/src/viewport/steppedStair.ts` (`steppedStairSpec`, headless-tested) and **modelled the top `landing_L`** (previously ignored, D-V103-8) as a flat waist-thick slab extending past the flight at the top rise level. `Viewport.tsx SteppedStair` now just renders the boxes it returns. Test `apps/web/src/viewport/stepped_stair.spec.ts` (7). ⚠ bars-sit-in-treads / landing visual acceptance is the owner's GPU pass (§D-2).

**D2 — general indicative→faithful (viewport-only).** The welded MESH (two-way slab mats, joist topping) now renders as its true orthogonal WIRE GRID instead of a single mis-oriented panel outline: pure `meshGridBars(result)` (`viewport/rebarProps.ts`) expands a `TREILLIS_MESH` group into X-wires (across width at pitchY) + Y-wires (along span at pitchX) at the mat's layer depth; `buildScene` swaps the group's outline for the grid (non-mesh untouched). Edges + real bent centrelines (hooks/exit-lengths, G1) already render. Test `apps/web/src/viewport/mesh_grid.spec.ts` (7). ⚠ consolidated GPU acceptance across all 8 elements is the owner's pass (§D-3).

**H10 — hook edit never dropped mid-invalid (`hook_not_dropped`).** `FaconnageEditor.setHook` now validates + commits a hook change against the last-VALID params (`committed`), not the in-progress buffer — so a start/end hook edit lands even while a *parameter* is mid-invalid (the invalid param stays in its field, H17). Was: `tryGen(params, …)` threw on the invalid buffer → the hook change was silently swallowed. Test `apps/web/src/ui/hook_not_dropped.spec.tsx` (2). Spec I.C H10 marked LANDED.

**Cover WARN tier — §7.12 comfort-target amber band.** Cover BELOW the durability+fire minimum is 🔴 FAIL (unchanged); cover that MEETS the minimum but is below the comfort target `reqCover·(1+band)` is now 🟠 WARN; comfortably above → 🟢 PASS — same shape as clear_spacing/tie_spacing/Asw. New shared `coverItem(cover, reqCover, band, ref, ids)` (`validation/index.ts`), used UNIFORMLY across ALL element validators (column + the 3 profile validators in `profiles.ts`) so it's one "cover" rule, one behaviour (not column-only). Band = pack `warnBands.cover` (BAEL/EC2 default 0.1 — already reserved, was unused). ⚠ the band is **G-TOL PROVISIONAL** (owner_tasks §B-5). **One status flip:** `pack_swap` BAEL EXTERIOR c=30=min now WARN (at the bare minimum, below comfort 33) — assertion updated (a validation status, NOT a golden). Tests `tests/cover_warn.spec.ts` (7 — FAIL/WARN/PASS transitions + uniform across column & beam) + the `pack_swap` update.

**Gate (2026-07-08).** `npm run check` ✓ — **657 tests / 129 files** (+32 over 625/124: coupe_slab_exactness 9, cover_warn 7, hook_not_dropped 2, stepped_stair 7, mesh_grid 7); `npm run coverage` core **95.12%** (was 94.91% — now hits the 95 target); `npm run coverage:adapter` **96.76%** (no regression); `npm run build:web` ✓ (50.7s). **NOT pushed** (owner controls push).

**Flagged for the owner/engineer (new this turn):** (1) **C1 BBS distribution count** — the render/coupe are now exact but the BBS distribution *quantity* stays the representative width-derived count; reconciling it to the along-span geometry is a fabrication-quantity change needing sign-off. (2) **Cover comfort band** value (G-TOL, §B-5). (3) **G-COUPE** conventions still provisional (§B-7). (4) **D1/D2 GPU acceptance** (§D-2/§D-3). No new engineer sign-off items beyond these.

**Next (remaining to finalize v1.0.4).** Code is essentially complete: façonnage H1–H19, Tracks A2/A3, B1/B2/B3, C1(station-aware + slab-exactness)/C2, D1/D2, E1/E2 all landed. The only C-side follow-up is the *optional* C1 BBS distribution-count reconciliation (owner-gated). Everything else remaining is **people/acceptance** (the true 100% gate): engineer nomination (**B-1** 🔴) → sign-offs **B-2…B-9** (now incl. the cover comfort band B-5) + **B-8** record; owner data **C-1/C-2/C-8**; owner GPU/drawing acceptance **§D**; final UAT **§E**.

### Archive — condensed entries (2026-06-25 → 2026-07-07)

> Older handoffs are summarized here for size. **Full narrative is in git history**; the load-bearing
> decisions live in the §8 ledger (`D-P*`, `D-V101/102/103-*`). Each step below was **green at the time**;
> v1.0.1 + v1.0.3 were **pushed**, v1.0.2 + v1.0.4 landed locally (owner-gated push).

**v1.0.4 — single-tenant product completion (2026-07-03 → 2026-07-07, Amer + Zayd)** *(the two 2026-07-08 entries
above finish it: C1 slab-coupe exactness + D1/D2 + H10 + cover-WARN, then the review that reverted cover-WARN,
reconciled the slab/stair distribution BBS count, and gated the joist mesh coupe lines).* In order: spec
restructured into a complete-product contract + owner/Zayd task split; prep phase (adapter characterization +
`coverage:adapter` safety net). Phase 1 façonnage stability (H1/H3/H4 — crash + data-loss fixes); Phase 2
capability (H11/H9/H2 — manifest defaults + `totalLengthParam` ⇒ `cutLength==length` on every open shape); Phase 3
(H15/H6/H7 — backend↔frontend parity, extras on the picker) + A3/H13 (EC2 reachable via `doc.codePack`); Phase 4
(H8 — addressable-bar geometric validity); **A2** (steel accounting — every add/remove feeds §7 As/`d` via
`computeZoneGeometryWeighted`; per-region Asw; clear-spacing fold) + review + 2 verdict-fixes; **B2** (beam
two-support axial placement + per-support anchorage); **B3** (supplement anchoring: skin fanout, corner-diagonal,
interior-diamond → A2 accounting); **C1** station-aware coupe + slab/stair distribution re-model (`across` bars) +
**C2** spliced-segment BBS sketch; **façonnage Phase-6** (H16–H19/H12 UI) + **H14/B1** per-bar splices; **E1/E2**
canonical `reinforcementFromResult` + `.rcfg` v1.2 + migration corpus. Ledger: `D-V102/103-*`.

**v1.0.3 — detailing depth, FEATURE-COMPLETE + PUSHED (2026-06-30 → 2026-07-01, Amer + Zayd):** G1 true bar
geometry (real bent centrelines, spiral coil, stair profile), G6 cadence continuity + coupe/elevation dims, G5
cross-tie/épingle unification + hook rendering, G2 addressable bars (the `longBars[]` channel this v1.0.5 M1 work
builds on), G3 beam two-support model + relevés + auto-seeded stirrup regions, G4 lap splices/couplers
(`spliceBar`, stagger, seismic lap-in-`l_c`), G7 shop-drawing PDF/DXF (`shopDrawing()` leaders + bending table),
G8 3D fidelity (concrete edges, stepped stair), G9 camera fixes (roll/ViewCube/hand-pan). Ledger `D-V103-1…9`, `7b`.

**v1.0.2 — workspace + detailing UI, FEATURE-COMPLETE (2026-06-27 → 2026-06-28, Zayd):** F7 2D section picker +
face-relative labels + 3D pickability, F2 cross-ties on real bar engagement (anchored épingles, `crossTies` model,
`nLegs` migration), F3 sticky per-zone readout, F4 expandable right column, F5 user transverse regions, F6
façonnage editor + shape catalog (Z/double-crank/stepped), F1 in-plane view roll. Ledger `D-V102-1…7`.

**v1.0.1 — breadth + presentation, PUSHED (2026-06-25, Amer):** Feature C (namespaced BBS, combined PDF +
per-project lock, DXF batch, elevation fiche, reorder) + A (ViewCube + persp/ortho + a11y views) + B (3D coupe
drag-handle). Ledger `D-V101-1/2`. GPU widgets await the owner's visual pass.
