# RebarConfig (SmartBar) v1.0.4 — Owner manual tasks

### Everything the owner (or the nominated structural engineer) must supply — complementary to `v1.0.4_spec.md`

**Purpose.** `v1.0.4_spec.md` is **what Zayd implements**. This doc is **what only the owner can do**. Since the
owner's directive of 2026-07-05 (*"don't provision anything — source accurate data"*), the code constants are now
taken from the **published standards**, compiled with citations in **`structural_data.md`** — so the owner's job is
no longer "ratify guessed numbers." It is three things: **(1) a few product decisions**, **(2) the data values only
you have** (chiefly the official RPS 2011 seismic table + market/business defaults), and **(3) a professional
validation** that the app applies the sourced code correctly.

**Priority key:** 🔴 *blocks coding of its item* · 🟠 *blocks acceptance (coding proceeds on the sourced data)* ·
🟢 *default is fine, confirm at leisure*. Zayd proceeds on the recommended defaults for 🟠/🟢 so nothing stalls.

**Date:** 2026-07-05. Companion to `v1.0.4_spec.md` (`§8`) + `structural_data.md`.

---

## §A — Product design decisions

| # | Decision | Options (rec first) | Unblocks | Prio |
|---|---|---|---|---|
| A-1 | **Unique-length semantic (H2)** | ✅ **DECIDED: `length` = fabricated cut length** (via the principal leg; matches BBS `l=`) | H2 | ✅ |
| A-2 | **Principal leg per symmetric shape (H2)** | ✅ **DECIDED (2026-07-05): recommended legs** — u_bar→`w` · zbar→`run2` · stepped→`run3` | H2 | ✅ |
| A-3 | **Validation-completeness scope (A2/H5)** | ✅ **Resolved by your centroid ruling** — `As` + effective depth `d` are now *exact* geometry (area-weighted centroid), so A2 ships **complete, no placeholder**. Nothing to decide unless you want it narrower. | A2, H5 | ✅ |
| A-4 | **Multi-code scope (A3/H13)** | ✅ **DECIDED (2026-07-05): add the BAEL↔EC2 picker now** (numbers already sourced; EC2 becomes a real selling point) | A3, H13 | ✅ |
| A-5 | **Impossible-bar tiers (H8)** | ✅ **DECIDED (2026-07-05): spacing tiered** — block outside-concrete/past-end; spacing **block below the code minimum, warn if merely tight** (uses `dg`/`k1`/`k2` — proceeds on `structural_data.md` defaults dg=20, k1=1.0, k2=5, owner-confirmable §C-7/§C-2) | H8, A2 | ✅ |
| A-6 | **Solve-error UX (H1.3)** | ✅ **DECIDED (2026-07-05): non-blocking banner + keep last-good** | H1 | ✅ |
| A-7 | **DROITE length field (H12)** | ✅ **DECIDED (2026-07-05, default): read-only + coupled, explicit override toggle** | H12 | ✅ |

---

## §B — Engineer: professional validation (not "ratify guesses" — the numbers are sourced)

*The constants now come from BAEL 91-99 / EN 1992-1-1 / RPS 2011, cited in `structural_data.md`. What a qualified
engineer adds is a signature that the app **applies** them correctly — via the reference-case suite Zayd builds
(spec A1). Start B-1 now; it is the only long-lead people-dependency left.*

| # | Task | Why only you/the engineer | Unblocks | Prio |
|---|---|---|---|---|
| B-1 | **Nominate the structural engineer** | a person, not code | all of §B | 🔴 for acceptance |
| B-2 | **Validate the BAEL reference-case suite** (Zayd builds it from `structural_data.md §3`) | professional judgment on application | A1 acceptance | 🟠 |
| B-3 | **Validate the EC2 reference-case suite** (`structural_data.md §2`) | as above | A3/A1 acceptance | 🟠 |
| B-4 | **Validate the RPS 2011 reference cases** (`structural_data.md §4`) — needs the A/g table from §C-1 first | seismic judgment | seismic verdicts | 🟠 |
| B-5 | **Confirm the WARN-vs-PASS tolerance bands** (G-TOL) — how far past a limit is "amber" vs "red" | judgment call | the 🟠 tier | 🟠 |
| B-6 | **Confirm the lap-stagger rule for the market** (EC2 default = ≤50 %/section, 0.3·l0 offset; confirm BAEL/RPS qualifiers) | code judgment | B1 acceptance | 🟠 |
| B-7 | **Confirm the coupe drawing conventions** (G-COUPE: near-parallel angle, look-behind depth, cutting-line/tag style, default station) | drawing-standard judgment | C1 acceptance | 🟠 |
| B-8 | **Sign the reference-case suites + the record** (`ratification.json`) once B-2…B-7 hold | the acceptance signature | release | 🟠 |

---

## §C — Data values only you can give (Zayd cannot source these accurately)

*These are market/business/project facts, not published constants. Item C-1 is the important one Zayd could not
extract cleanly from the web.*

| # | Data needed | Detail / default | Unblocks | Prio |
|---|---|---|---|---|
| C-1 | **RPS 2011 acceleration table** | the A/g ratio per zone (Za 0–4) + soil coefficients + importance-class→ND mapping + behaviour factor K. *(Velocity zones already sourced: 0.07/0.10/0.13/0.17 m/s. You have the official MHPV document — the web copies are scanned/blocked.)* | RPS overlay, B-4 | 🟠 |
| C-2 | **Primary market + default code** | Morocco / France? → **BAEL** default? and which **National Annex** for EC2 (sets k1/k2, cover classes) | A3, cover, spacing | 🟠 |
| C-3 | **BAEL `ls` default** | computed **44Ø** (from `ls = Ø·fe/4τsu`) vs tabulated **40Ø** — which is the app default? | A1 (may re-baseline a golden) | 🟠 |
| C-4 | **Diameter set** | the Ø menu, e.g. 6/8/10/12/14/16/20/25/32/40 mm | UI + BBS | 🟢 |
| C-5 | **Default materials** | concrete fc28 (e.g. 25 MPa), steel fe (e.g. 500 MPa), steel type = HA (ribbed) default | new-doc defaults | 🟢 |
| C-6 | **Exposure / cover policy** | which exposure classes to ship + cover per class (or "EC2 default + structural class S4" / "BAEL 3 cm exterior, 5 cm aggressive") | cover logic | 🟠 |
| C-7 | **Default aggregate size `dg`** | for clear spacing (e.g. 20 or 25 mm) | A2 spacing | 🟢 |
| C-8 | **Stock bar length** | for auto-split / laps (12 m standard? also 6 m) | H14, B1 | 🟢 |
| C-9 | **Partial safety factors** | confirm γc = 1.5, γs = 1.15 (fundamental); accidental 1.15 / 1.0 | all packs | 🟢 |
| C-10 | **Cartouche / title-block fields** | project, client, drawer, index… | PDF/DXF sheet | 🟠 |
| C-11 | **Starter scheme list** | which reinforcement schemes ship per element | catalog | 🟢 |

---

## §D — Visual / GPU & drawing acceptance (headless can't verify)

*On your Windows machine: `cd apps/web && npm run dev`, port 5180.*

| # | Acceptance pass | Covers | Prio |
|---|---|---|---|
| D-1 | **v1.0.3 GPU pass** — true geometry, spiral coil, épingle hooks, addressable bars, beam supports/relevés, concrete edges, stepped stair, ViewCube no-spin + roll auto-level + hand-pan (the ⏳ "owner GPU pass" flags in `current_state §1`) | finalize v1.0.3 | 🟠 |
| D-2 | **Stair 3D acceptance** (D1) — bars sit in the treads, landing reads right | D1 | 🟠 |
| D-3 | **General 3D fidelity** (D2) — edges/exit-lengths/hooks/mesh across all 8 elements | D2 | 🟠 |
| D-4 | **Coupe acceptance** (C1) — the conventions look right; the default station shows the detailing | C1 | 🟠 |
| D-5 | **Shop-drawing acceptance** (C2) — leaders/marks/dims/bending-table read like a real fabrication drawing | C2 | 🟠 |
| D-6 | **a11y spot-check** — keyboard-only walkthrough (Zayd runs axe; you confirm the feel) | §0.4 | 🟢 |

---

## §E — Final acceptance / release

| # | Task | Prio |
|---|---|---|
| E-1 | **End-to-end UAT** — a real column + beam project, full PDF/DXF/BBS export, confirm site-usable | 🔴 for release |
| E-2 | **Sign the release** once §B validations + §D acceptances hold | 🔴 for release |
| E-3 | **Decide the push/hosting moment** (per `cross_projects_policy §10`, Zayd pushes only on your word) | 🟠 |

---

## The short version — what to do first
1. **§C-1 — the RPS 2011 A/g acceleration table** (you have the official document; Zayd couldn't extract it cleanly).
2. **§C-2/§C-3 — primary market + default code + the BAEL `ls` default** (drives the National-Annex choices).
3. **§B-1 — nominate the engineer** so the reference-case validation can start in parallel.
4. **§A-4, §A-5** (🟠) when convenient; everything else Zayd proceeds on the recommended defaults.
