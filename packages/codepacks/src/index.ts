/**
 * @rebarconfig/codepacks — pluggable code packs behind the core `CodePack` interface.
 * v1.0 ships BAEL-FR (P1, default). EC2 arrives in P4a behind the SAME `code.*` names.
 */
export * from "./bael/index";
